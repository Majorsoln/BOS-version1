# Changelog

> Records every change to the Charter and to cross-Term decisions.
> Managed by Term 7 — Integration & Coherence (Charter §11).

## 2026-06-06 — 🎯 ALL CONCRETE VERTICALS COMPLETE + Framework Test #4 PASSED

**Term 6 — Vertical Engines (9/12 — ALL 4 CONCRETE VERTICALS MERGED)**
- **Term 6 CN-6-004 (Workshop Engine)** ratified and merged (commit `1d46489`) — Term 6's ninth doc and **FINAL CONCRETE VERTICAL** + **Framework Test #4 PASSED** (the decisive test). 1,767 lines, 23 sections. Most complex vertical per Brief §6.1 (parametric Style + Formula Engine + Cut Optimization + offcut tracking + long-lifecycle Workflow + cross-vertical sell-via-POS). Mantra ya doc: *"System haimuulizi fundi ahesabu — inamwambia fundi akateje."* Mzee Hassan tells the system the Style + W×H + variables; system tells him exactly which bars to cut at which lengths + which offcuts to expect — BOS at its finest serving real fundi reality. **Eleven doctrines WS1-WS11:** WS1 workshop.* covers built-to-spec parametric fabrication (windows + doors + gates + fixed panels + partitions + cabinets + tables + chairs); WS2 Style = **tenant-scope event-sourced template, NOT pack content** (Term 6 critical BD distinction: REST4 recipes + HOT9 rate cards ARE pack content because jurisdiction policy from Term 1; Style is tenant craft created by tenant — Mzee Hassan designs HIS style HIS way; BD5 split-it Style content + Formula Engine + Cut Optimizer); WS3 Material Group tenant-scope catalog + Material Specifications via Inventory primitive item metadata (N2 boundary); WS4 Formula Engine dependency chain + Frame TU null rule (only is_frame: true may have formula: null; non-frame lines must reference other lines/variables); WS5 Variable input at POS (X/Y/Z); WS6 Cut List Generated at "in_progress" transition (quote phase = preview not persisted; persisted cut list only at project commit); WS7 BFD + Offcut algorithm Steps 1-5 (Grouping per profile_type → Get specs from Inventory metadata → Sort decreasing → Best Fit Decreasing matching → Compute remaining); WS8 Offcut tracking via Inventory primitive lot model (no new primitive; CN-5-003 native); WS9 Project Workflow long-lifecycle (12 states: enquiry → measured → quoted → accepted → in_progress → cutting → assembly → finishing → ready_for_delivery → delivered → completed → archived + branches: quote_rejected / cancelled / blocked / disputed); WS10 Style sharing v1 = duplicate-into-receiver (export/import; NO platform marketplace v1 per BD8 — deferred until ≥2 case demonstration); WS11 Mixed-Vertical workshop+retail sell-via-POS via Foundation Inventory primitive (VE2 + BD7 honoured — no direct workshop↔retail call). **N-improvements N1-N6 all integrated:** N1 §13.3 2D guillotine for sheet materials (BFD-2D parallel mechanism for glass/board/panel; pack.workshop.cut_optimization.2d_strategy; Architect-phase implementation); N2 §3 + §14 material specs boundary explicit (stock_length + offcut_length + kerf_width LIVE in Inventory primitive item metadata per CN-5-003 native; workshop NEVER duplicates in pack.workshop.*; BD4 push-down); N3 §10 formula evaluation timing precision (POS-input = preview not persisted; cut-list-generation at in_progress = persisted; determinism per CN-4-001 replay); N4 §20 WS11 sell-via-POS 8-step concrete chain (workshop.project.completed → inventory.lot.added → tenant Mixed-Vertical activation per CN-6-105 → retail.catalog item → retail.basket.opened → retail.bill.ready → Checkout settled → Inventory Pattern A deduction; Foundation Inventory primitive as carrier); N5 §18 WS10 style sharing 5-step concrete mechanism (workshop.style.export.request → workshop.style.exported.v1 with Document hash-signed payload → workshop.style.import.request → bus validates → workshop.style.imported.v1 with new receiver_style_id → receiver replays imported event sequence locally creating Style copy in B's library); N6 §22 WP1 anchor solidified — Mwemas (Mama na Bwana Mwema, Mwanza couple) commissioning casement windows for Mwanza home renovation (cross-doc continuity from CN-6-102 WP1 + CN-6-103 + CN-6-003 — natural narrative arc settling back home after travels). **§1.5 Parsimony — the mantra in concrete form** — Mzee Hassan in his Arusha karakana does not write math; he drew the window once as a Style in the CAD-like designer (lines for frame + sash + mullion; shapes for glass + net; named each line Wframe/Hframe/Hsash/Wsash; wrote formulas (w01+1)/2 + h01-9; set offcuts 10cm frames 7cm sashes; saved as "Casement 2P"). Today customer arrives wanting 140×201cm casement window. Salma at till opens "Casement 2P" + enters W=140 H=201. System computes Style holds wisdom + Formula Engine does arithmetic + Cut List Generator produces instructions + Cut Optimizer packs onto bars with minimal waste. Mzee Hassan cuts. "Parsimony is the bar — every doctrine justified against does this match how the real fundi in real Tanzanian workshops thinks?" **§4 Manifest** with 4 Workflow instances (workshop.style + workshop.material_group + workshop.project billable per HO9 + workshop.cut) including ~30 emit events with NC9 compensation per each, 13+ pack hooks. **§9 The Style Designer Reality** — line properties (line_id + named + profile + formula + offcut + position + endpoints + is_frame + is_variable); shape properties (shape_id + named + material + material_type + width_formula + height_formula + clear); endpoint types (Mater-Mater 45° miter + Mater-Square + Square-Square); naming repetition rule (lines with same `named` value share all properties — automatically counts repetitions per cut). **§10 The Formula Engine** dependency chain evaluator + Frame TU null rule + grammar safety constraint via pack.workshop.formula.allowed_operators. **§13 Cut Optimization** Steps 1-5 BFD + Offcut from field PDF 1; concrete Casement 2P W=140 H=201 worked through algorithm step-by-step. **§17 Material Shortage** Procurement triggers + workshop.project.blocked.v1 state per CN-6-100 §3.13 pattern + pack-driven recovery + WS9 blocked branch. **§18 Style Sharing** 5-step concrete mechanism per N5 + Document hash-sign + bus validation + replay-locally; no platform marketplace v1 per BD8. **§19 Disputes + Rework + Warranty** post-delivery defect via workshop.project.disputed.v1; warranty_days per pack; rework cycle. **§20 Mixed-Vertical Workshop+Retail Sell-Via-POS** 8-step Foundation Inventory primitive chain per N4 + WS11; CN-6-005 Bridges (future) catalogs fuller pattern; CN-6-105 Mixed-Vertical Tenants (future) elaborates activation flow. **§21 Conflict Event Family** VI-03 rare in workshop (e.g., two fundi claim same uncut bar) workshop.bar.conflict.detected.v1. **§22 Eight Worked Patterns from PDFs reality:** WP1 Casement window 2-panel W=140 H=201 (PDF 2 Mfano 1; **Mwemas Mwanza renovation per N6 anchor — cross-doc continuity**); WP2 Sliding window 2-panel W=180 H=150 (PDF 2 Mfano 2); WP3 Fixed panel showcase W=100 H=120 (PDF 2 Mfano 3); WP4 Hinged door with glass+board W=90 H=220 (PDF 2 Mfano 4); WP5 Variable window with X=45 W=120 H=180 trapezoid top order (PDF 2 Mfano 5; demonstrates WS5 variable input flow); WP6 Multi-item commercial project (4 windows + 2 doors single project with cross-item BFD optimization across all profile groups); WP7 **Style sharing — Mzee Hassan + Fundi Athumani NEW Mwanza workshop owner** (WS10 + N5 5-step mechanism; Fundi Athumani receives Mzee Hassan's exported Casement 2P style + replays it locally creating own copy in Mwanza style_library; Mwanza geographical continuity with Mama Halima Logistics broker); WP8 Mixed-Vertical showroom sell-via-POS (Mzee Hassan showroom with walk-in customer; completed window leaves workshop.project.completed → inventory.lot.added → retail.bill.ready settles per WS11 + N4 8-step chain). **Character map for Term 6 corpus extended:** Mzee Hassan (existing Arusha karakana owner — central character throughout Term 6 from CN-6-101 onwards) + Mwemas (cross-doc from CN-6-102/103/003 — Mwanza couple settling home renovation) + Fundi Athumani NEW (Mwanza workshop owner — receives Mzee Hassan's style export; Mwanza geographical continuity with Mama Halima) + Salma cross-doc (Mama Amina's till operator now operating workshop POS for Mzee Hassan example). **Payment-method abstraction (RE11 inherited from CN-6-001/002/003)** verified throughout — ZERO M-Pesa/Tigo/Airtel/specific-provider names in body (perfect compliance); payment language method-category abstract per CTR-028 + CTR-049 + CTR-050 throughout WP1-WP8 + B2B project bill settlement (per Brief §7.1 may use bank push notification per regional adapter set). **NO new CTRs** filed by CN-6-004. Cumulative CTR-046 queue unchanged (DC-NN-e + DC-NN-f). CTRs cited: 018, 002, 024, 026, 027, 030, 038, 044, 045, 046, 049, 050. **Framework v1 — PROVEN ACROSS FULL VERTICAL SPECTRUM:** Test #1 CN-6-001 Retail (simplest basket POS) PASS; Test #2 CN-6-002 Restaurant F&B cluster (medium — kitchen workflow + recipes + cross-vertical) PASS; Test #3 CN-6-003 Hotel Hospitality Stays cluster (high — multi-day folio + chain profile + tourism licensing + cross-vertical confirmation) PASS; **Test #4 CN-6-004 Workshop (most complex — parametric Style + Formula Engine + BFD optimization + offcut tracking + long-lifecycle + cross-vertical sell-via-POS)** PASS. **Framework v1 (CN-6-100 + 101 + 102 + 103 + 104) stays unchanged through all 4 concrete vertical tests.** No cross-cutting amendment required. **Concept Lead bar honoured definitively:** Bibi Khadija mama ntilie + Mzee Karim bucha + Bibi Sauda Iringa guesthouse + Mzee Lyimo lodge manager + Bibi Mwajuma receptionist + Mzee Hamisi pub owner + Mzee Hassan karakana fundi + Mzee Juma duka customer + Mama Amina Kariakoo + Salma till operator + Faraja pharmacist + Mama Halima logistics broker + Mwemas Mwanza couple + Mama na Bwana Mwema + Fundi Athumani Mwanza workshop + Asha floor server + Lodge Serengeti management + Kilimanjaro Lodge Moshi management + Nina mobile-order customer + Bibi Asha — full Tanzanian SME reality spectrum from street food simplest to safari-lodge complex to parametric-fabrication most demanding all served by single concept-phase framework. Term 6 progress: 9/12 docs (5 cross-cutting + **4 concrete verticals ALL COMPLETE**). Brief §13.10-12 remaining (3 docs): CN-6-005 Vertical Bridges (pattern catalog post-WS11/REST7/HOT5 — concrete patterns established cleanly via Foundation primitives); CN-6-105 Mixed-Vertical Tenants (Mama Amina retail+pharmacy + Mzee Karim bucha+BBQ + Mzee Hassan workshop+retail showroom precedents); CN-6-901..904 stress sketches (Insurance + Healthcare + Education + Marketing Agency — Flexibility Test §14 of Brief). No Charter conflict; Laws 1-6 all honoured (state from events for Style event-sourcing + immutability for cut history; engine isolation via Foundation Inventory for sell-via-POS; advisory-only workshop-fundi-advisor + workshop-manager-advisor; flexibility via pack-driven 2D strategy + cut optimization extensions; compliance configured via pack hooks; distribution regional via existing CTR-049). **Proof-of-Framework #4 PASSED — Framework v1 DEFINITIVELY PROVEN.**

## 2026-06-05 — Framework Test #3 PASSED

**Term 6 — Vertical Engines (8/12)**
- **Term 6 CN-6-003 (Hotel Engine — Hospitality Stays Cluster)** ratified and merged (commit `ee4ec10`) — Term 6's eighth doc and **third concrete vertical** (1,421 lines, 20 sections). First application of **Hospitality Stays cluster doctrine** ratified between CN-6-002 + CN-6-003 (parallel F&B cluster pattern): `hotel.*` namespace covers Hospitality Stays cluster (hotel full-service + lodge safari/eco + guesthouse/B&B + tented camp + serviced apartment + hostel + short-term rental) via BD5 split-it + BD6 3-of-3 cluster pattern. Hotel = anchor (BD6 3-of-3 pass: multi-day reservation + folio aggregation + tourism licensing); other styles fail BD6 individually but cluster passes via pack-driven configuration. **Framework Test #3 PASSED:** CN-6-003 written without amending CN-6-100/101/102/103/104; cluster doctrine + folio mechanics + room-as-Workflow-primitive + HOT9 rate-as-pack-content + REST7 Hotel-side confirmation all derive cleanly; Bibi Sauda's three-room Iringa guesthouse + Lodge Serengeti chain operation served by same engine via pack configuration. **Doctrine HOT0 + HOT1–HOT9:** HOT0 Hospitality Stays cluster scope + placement criterion (hotel.* covers operations centered on multi-day overnight stay with bookable spaces + folio aggregation; NOT day-only operations or long-term residential leases) + regulated-layer-as-pack-concern (tourism licensing + fire safety + environmental compliance cross-vertical pack NOT vertical-defining); HOT1 multi-day reservation Workflow distinct from per-visit (time dimension fundamental F&B-vs-Hotel difference); **HOT2 folio site-scope per stay vs guest profile tenant-scope persistent** (Term 6 critical save: I conflated; they separated — folio aggregates THIS stay's charges then settles; guest profile aggregates chain-wide stay history persistently); HOT3 room state machine (vacant → reserved → occupied → cleaning cyclic; per VE7 Workflow primitive); HOT4 chain guest profile (tenant-scope SP3 exception per CN-6-103 §6 + WP2 from hotel side); HOT5 Hotel-side Obligation subscription confirming REST7; HOT6 tourism licensing pack-driven parallel CN-6-002 §16 doctrine; HOT7 self-service check-in (D-DISC-002 partial closure parallel RE10/REST8); HOT8 walk-in path-convergence (same Workflow + `pre_reservation: false` flag — walk-in enters at confirmed state directly); **HOT9 rate management as pack content** (Term 6 new doctrine — BD4 push-down parallel REST4 recipes-as-pack-content; `pack.hotel.rate_cards.<room_type>.<season>.<rate_basis>` declares base_rate + currency + applicable_dates + customer_tier_overrides + length_of_stay_discount; vertical INDICATES via rate_card_ref + Checkout K2 + Promotion PR1 RESOLVE; BD5 split-it Universal mechanism + pack content). **§1.5 Parsimony** — Tanzania travels: Mwemas weekend Kilimanjaro from Mwanza, safari guests Lodge Serengeti, Mama Halima overnight stop Iringa at Bibi Sauda's guesthouse, Dodoma government meetings mid-range hotels, Moshi trekkers fill lodges, Arusha conference attendees — all guests of Hospitality Stays tenants; same engine different pack configuration; framework must serve Bibi Sauda's three-room guesthouse (single owner) + Lodge Serengeti chain (multi-staff full operation) with equal correctness. **N-improvements N1-N6 all integrated:** N1 §7 explicit room-as-Workflow-primitive boundary (CN-4-011 Workflow per VE7; NOT Inventory item — preserves CN-5-003 boundary; availability query via Workflow projection not Inventory state); N2 §4 manifest billable=true only on hotel.folio (HO9 strict mechanizable); hotel.reservation billable=false + subscribes to folio.completed for transitions_to: checked_out; N3 §11 6-step settlement loop-back (subscribe obligation.created → folio.charge_added → folio tracks included_obligation_refs[] → bill.ready carries refs → Foundation Obligation primitive resolves on checkout.settled → Restaurant table_session.completed; Foundation primitive coordinator; VE2 honoured); N4 §10 + WP1 concrete TZ folio aggregation (Mwemas 3 nights Lodge Serengeti: room 3×200k=600k + tourism levy 5% per TZ pack=30k + restaurant in-stay via REST7 obligations=145k + mini-bar=25k + spa=80k → sub-total 880k → +VAT 18%=~150k → total ~1,030,000 TZS settlement via tenant's enabled tender method); N5 §8 HOT4 guest profile update mechanism (on hotel.folio.completed.v1 → Foundation Party primitive updates profile + Hotel emits hotel.guest_profile.updated.v1 tenant-scope + downstream Universal Promotion loyalty + Reporting + manager-advisor subscriptions); N6 §8 short-term rental future extension note (`pack.hotel.host_managed: bool` placeholder; current v1 staff-managed; full mechanism deferred per BD8 ≥2 cases threshold). **§4 Manifest** with 5 Workflow instances (hotel.reservation + hotel.folio + hotel.room + hotel.guest_profile tenant-scope SP3 + hotel.rate_card_management tenant-scope), HO9 settlement_subscription on hotel.folio (billable=true), hotel.reservation internal subscription to folio.completed for state transition, ~25 emit events with NC9 compensation per each, 11+ pack hooks (rate_cards + no_show + stay_extension + cancellation + tourism_licensing + host_managed + amenities + charge_to_room_enabled + self_service_check_in_enabled). **§8 Pack-Driven Hospitality Stays Variations** matrix demonstrating cluster scope concretely: Bibi Sauda Iringa Guesthouse (guesthouse + basic+with-bath rooms + per-room-per-night + simplified TLB + no walk-in default + no charge-to-room + no self-service); Kilimanjaro Lodge Moshi (lodge + mid-range room types + per-room-per-night + full TLB + walk-in supported + restaurant integration + mobile check-in option per Nina WP8); Lodge Serengeti (lodge + premium tent_luxury + complex rate cards seasonal + full TLB + chain guest profile linked + restaurant integration + full self-service options). **§9 Rate Management as Pack Content** (HOT9 elaborated; rate cards per room type per season per rate basis; tier overrides + length-of-stay discounts; pack-driven not primitive). **§10 Folio Aggregation Mechanics** (multi-day accumulation; sources: room nights + restaurant in-stay via REST7 + mini-bar + spa + tourism levy + tax via K2; N4 concrete TZ totals referenced WP1). **§11 Charge-to-Room Hotel-Side Subscription** with N3 6-step settlement loop-back fully traced (Restaurant emits obligation.created → Hotel folio subscribes → folio.charge_added events → folio tracks included_obligation_refs → bill.ready carries refs → Foundation primitive resolves obligation.settled on checkout.settled → Restaurant table_session.completed). **§12 Multi-Leg Booking** (Q2 closure — bundled by booking_id site-scope NOT multi-site-by-nature per CN-6-103 SP4). **§13 Walk-In vs Reservation Paths** (Q5 closure — HOT8 path-convergence; same Workflow + flag; lifecycle entry varies; Bibi Sauda's guesthouse default walk-in pack). **§14 Stay Extensions + Modifications** (Q6 closure — original_rate default customer guarantee; pack-overridable). **§15 Cancellation + No-Show Handling** (Q4 + Q7 closure — pack-driven refund schedules + no-show charge policies + grace periods). **§16 Self-Service Check-In** (HOT7 — D-DISC-002 partial closure for hotel parallel RE10 retail.fulfillment + REST8 restaurant.qr_order; customer-as-actor per CN-4-007; Business ID/QR tenant identification per CTR-049/050). **§17 Conflict Event Family** (VI-03 — Q3 overbooking via `hotel.room.conflict.detected.v1` + bus single-acceptance + pack-driven auto-upgrade resolution). **§18 Tourism Licensing + Compliance** with BOS-is-not-licensing-authority doctrine (parallel CN-6-002 §16) — three-layer responsibility (TLB-TZ + tenant + regional agent + BOS); licensing authority issues licenses; tenant complies; regional agent captures evidence at onboarding per CTR-045; BOS records as Foundation Document, computes tourism levy per pack, surfaces license-expiry advisory alerts per Law 3 non-autonomous; never auto-blocks sales on license expiry. **§19 Eight Worked Patterns:** WP1 Mwemas canonical Kilimanjaro Lodge Moshi reservation (Mzee Lyimo manager + Bibi Mwajuma receptionist NEW characters; full lifecycle + N4 concrete folio ~1.07M TZS settlement); WP2 chain guest profile Mwemas across Moshi + Serengeti (HOT4 + SP3 tenant-scope; Bibi Mwajuma sees full history + preferences); WP3 Hotel-Restaurant charge-to-room Hotel side (N3 6-step settlement loop-back mirroring CN-6-002 §13 REST7 from other side); WP4 overbooking conflict (two channels VI-03 + auto-upgrade per pack); WP5 multi-leg booking (Mwemas 3 nights Moshi + 2 nights Serengeti bundled site-scope per CN-6-103 SP4); **WP6 Mama Halima walk-in at Bibi Sauda's Iringa Guesthouse NEW character** (Q5 walk-in path + HOT0 guesthouse cluster simplest config + cross-doc Mama Halima continuity from Logistics CN-6-100 §12; framework holds at extreme simple — single owner three-room guesthouse); WP7 Mwemas stay extension +1 night (Q6 original_rate default; folio absorbs); WP8 Nina self-service mobile check-in Kilimanjaro Lodge Moshi (HOT7 + D-DISC-002 partial closure; cross-doc Nina continuity from CN-6-001 RE10 WP6 + CN-6-002 REST8 WP6 — parallel pattern across three verticals). **Character map for Term 6 corpus extended:** Mzee Lyimo (Chagga regional naming + Moshi local manager) + Bibi Mwajuma (Kilimanjaro Lodge Moshi receptionist) + Bibi Sauda (Iringa guesthouse owner; new geography Iringa) all new; cross-doc continuity Mwemas + Lodge Serengeti + Kilimanjaro Lodge Moshi + Mama Halima + Nina all referenced. **Payment-method abstraction (RE11 inherited from CN-6-001 + CN-6-002)** verified throughout — ZERO M-Pesa/Tigo/Airtel/specific-provider names in body (perfect compliance); payment language method-category abstract per CTR-028 + CTR-049 + CTR-050 throughout WP1-WP8 + folio settlement + self-service mobile check-in. **NO new CTRs** filed by CN-6-003. Cumulative CTR-046 queue unchanged (DC-NN-e + DC-NN-f). CTRs cited: 018, 002, 024, 026, 027, 030, 038, 044, 045, 046, 049, 050. Term 6 progress: 8/12 docs (5 cross-cutting + 3 concrete verticals). Next per Brief §13.9: **CN-6-004 Workshop Engine** — final concrete vertical and most complex per Brief; Mzee Hassan Arusha (+ hypothetical Moshi per CN-6-103 WP1) anchor; parametric geometry + cut lists + offcut tracking + multi-item project quotes + glass guillotine optimization + style library tenant-scope + long-lifecycle project Workflow. **Framework Test #4** ahead — final concrete vertical with most demanding mechanics; framework v1 face strongest test. **Concept Lead bar honoured:** Bibi Sauda three-room guesthouse Iringa (most modest accommodation reality) + Mwemas mid-range Kilimanjaro lodge stay (typical SME guest reality) + Lodge Serengeti premium safari (high-end tourism reality) — full Hospitality Stays spectrum covered; cluster doctrine + HOT9 rate-as-pack + N1 room-Workflow-not-Inventory + N3 6-step loop-back + N5 guest profile mechanism all real-world grounded. No Charter conflict; Law 1 immutability honoured (forward-only Workflow state); Law 2 isolation preserved (Hotel-Restaurant only via Foundation Obligation primitive — never direct call); Law 3 advisory-only preserved (license expiry alerts non-autonomous); Law 5 compliance configured (pack-driven licensing + rate cards + no-show + cancellation); Law 6 distribution regional (regional agent curates payment methods + tourism license authority per region per CTR-049). **Proof-of-Framework #3 PASSED.**

## 2026-06-04 (continued — F&B Cluster Doctrine + Framework Test #2 PASS)

**Term 6 — Vertical Engines (7/12)**
- **Term 6 CN-6-002 (Restaurant Engine — F&B Cluster)** ratified and merged (commit `932c98e`) — Term 6's seventh doc and **second concrete vertical** (1,257 lines, 19 sections). First application of **F&B Cluster Doctrine** ratified between CN-6-001 + CN-6-002 (Concept-Lead-led discussion): restaurant.* namespace covers full Food & Beverage cluster (restaurant sit-down + café + BBQ/nyama choma + pub + bar + fast-food + buffet + mama ntilie + chai-jamia + hotel-attached restaurant + catering operation) via BD5 split-it + BD6 3-of-3 fail individually for café/BBQ/pub. Hotel remains separate vertical (BD6 3-of-3 pass: multi-day reservation + folio + tourism licensing). Option C namespace ratified (keep `restaurant.*` per CTR-038 + CN-5-103 §16; semantic broadening via explicit REST0 doctrine). **Framework Test #2 PASSED:** CN-6-002 written without amending CN-6-100/101/102/103/104; cluster doctrine derives cleanly from BD5; eight worked patterns from simplest (Bibi Khadija mama ntilie single charcoal stove) to most complex (Lodge Serengeti multi-station fine dining 80 covers 20 tables) operate under same engine via pack-driven configuration. **Doctrine REST0 + REST1–REST8:** REST0 F&B cluster scope + preparation-step criterion (restaurant.* covers operations where preparation happens between order and serve; retail.* covers transfer of existing items without preparation; edge cases default restaurant.* when tenant identifies as F&B) + regulated-layer-as-pack-concern (alcohol licensing + food hygiene cross-vertical pack concerns NOT vertical-defining); REST1 spectrum coverage (street food → fine dining all `restaurant.*`); REST2 two-Workflow architecture `restaurant.table_session` + `restaurant.order` + `restaurant.kitchen_ticket` (or station equivalents per pack); REST3 ticket lifecycle `fired → cooking → ready → picked_up → served`; REST4 recipe Pattern B consumption per CN-5-003 N1 with recipes as **pack content** `pack.restaurant.recipes.<menu_item_id>` (BD4 push-down — NO Foundation primitive); REST5 split billing pack-driven methods (equal_share + by_share + by_item + by_item_with_shared); REST6 tip handling per CN-5-005 commission pattern with `pack.restaurant.tip_pool_rules` (universal_pool vs per_waiter); **REST7 Hotel-Restaurant charge-to-room via Obligation primitive kind: hospitality_charge concretizing CN-6-101 §11.4 + CN-6-104 §10 doctrine — full 5-step chain demonstrated in WP3**; REST8 self-service + QR-table-ordering (D-DISC-002 partial closure via `restaurant.qr_order` Workflow parallel to retail.fulfillment). **§8 Pack-Driven F&B Variations** with concrete tenant-config matrix: Lodge Serengeti (table_service + kitchen+bar routing + table_session + complex menu + alcohol true) vs Mzee Karim BBQ pivot (grill_to_table + grill + counter_order + simple + alcohol false) vs Mzee Hamisi pub (bar_only + bar + bar_tab + simple_bar_food + alcohol true) vs Bibi Khadija mama ntilie (counter_service + no_ticket + takeaway_only + simple + alcohol false) — each row same engine different pack config. **§13 Hotel-Restaurant Charge-to-Room** full 5-step Obligation chain with `pack.restaurant.hotel_charge_to_room_enabled` flag activation; Lodge Serengeti enables (sister hotel.* tenant); standalone restaurants (Mzee Hamisi pub, Bibi Khadija mama ntilie) leave false. **§16 BOS Is Not a Licensing Authority** dedicated doctrine section per Concept Lead instruction — three-layer responsibility model (licensing authority issues licenses; tenant complies; regional agent captures evidence at onboarding per Charter Law 6 + D-003 + CTR-045; BOS records as Foundation Document CN-4-012, computes tax including excise per pack rules, surfaces license-expiry advisory alerts via CN-5-010 framework per Law 3 advisory-only); flag does NOT autonomously block sales on license expiry — BOS records continued operations; regulatory authority + regional agent responsible for next steps. **§17 Catering + Food Courts** (Term 6 Sharpening 2 + 3): catering = restaurant.* + `pack.restaurant.fulfilment_model: catering` (preparation at restaurant site + delivery/serve at client site via future Logistics vertical integration; avoids overloading multi-site-by-nature SP4); food courts per legal ownership (multi-tenant default; one-tenant-multi-site supported by existing framework). **§18 Eight Worked Patterns** WP1 Lodge Serengeti dinner canonical (table_session lifecycle + multi-station kitchen+bar tickets + REST4 recipe Pattern B + HO9 settlement-back); WP2 VI-03 table conflict (Asha + Salma simultaneous tap demonstrating CN-6-101 §12.1 conflict event family); WP3 Mwemas charge-to-room (REST7 full 5-step Obligation chain Lodge Serengeti restaurant + hotel cross-vertical concretized — `restaurant.charge.obligation_emitted.v1` → Foundation `obligation.created.v1 {kind: hospitality_charge}` → Hotel folio subscribes → folio aggregates → `checkout.settled.v1` at hotel checkout → `obligation.settled.v1` → restaurant table_session.completed); WP4 split billing 5 payers by_share (REST5 + REST6 tip pool + each payer different tender method per RE11); WP5 Mzee Karim BBQ pivot (Mixed-Vertical: bucha retail.* + nyama-choma restaurant.*; Inventory primitive handles meat flow natively via existing lot/offcut tracking per CN-5-003); WP6 QR-table at Lodge Serengeti café-bar (REST8 + Nina cross-doc continuation; D-DISC-002 partial closure); **WP7 Bibi Khadija mama ntilie NEW character** Kariakoo street corner serving ugali na samaki to office workers (simplest F&B config: counter_service + no_ticket + takeaway_only + simple + alcohol false; framework parsimony bar honoured at extreme simple); **WP8 Mzee Hamisi pub NEW character** Kariakoo Baa ya Mzee Hamisi (bar_tab lifecycle + alcohol licensing pack flag + bar-only routing + §16 doctrine demonstrated concretely with license expiry advisory alert without autonomous block). **Character map for Term 6 corpus extended**: Bibi Khadija (mama ntilie Kariakoo) + Mzee Hamisi (pub Kariakoo) + Asha (floor server) new; cross-doc continuity Mama Halima + Mama Amina + Salma + Lodge Serengeti + Mzee Hassan + Mwemas + Mzee Karim + Nina all referenced. **Payment-method abstraction (RE11 inherited)** verified throughout — ZERO M-Pesa/Tigo/Airtel/specific-provider names in body; one DOES/DOES NOT prohibition reference in §1.2 = correct usage; payment language method-category abstract per CTR-028 + CTR-049 regional curation + CTR-050 adapter coverage. **NO new CTRs** filed by CN-6-002. Cumulative CTR-046 queue unchanged (DC-NN-e from CN-6-103 + DC-NN-f from CN-6-104). CTRs cited: 018, 002, 024, 026, 030, 038, 044, 045, 046, 049, 050. **Concept Lead bar honoured** ("real business + lives"): Bibi Khadija + Mzee Hamisi + Mzee Karim + Lodge Serengeti + Mwemas + Mama Halima + Mzee Hassan + Nina all concrete Tanzanian F&B reality; mama ntilie (most common Tanzania food business) gets first-class framework support without specialized engine; pub gets alcohol-licensing pack flag without separate vertical; BBQ corner extends bucha business without architectural change. Term 6 progress: 7/12 docs (5 cross-cutting + 2 concrete verticals). Next per Brief §13.8: **CN-6-003 Hotel Engine** — Kilimanjaro Lodge Moshi + Lodge Serengeti chain anchor; reservation lifecycle + folio aggregation + room state machine + chain guest profile + Hotel-side Obligation subscription confirming REST7 from CN-6-002. **Proof-of-Framework #2 PASSED** — cluster doctrine derives cleanly from BD5/BD6; eight patterns from simplest to most complex hold under single engine; framework v1 stays stable on second concrete test.

## 2026-06-04 (continued — FIRST CONCRETE VERTICAL)

**Term 6 — Vertical Engines (6/12 — FIRST CONCRETE VERTICAL)**
- **Term 6 CN-6-001 (Retail Engine)** ratified and merged (commit `cf616a3`) — Term 6's sixth doc and **first concrete vertical** applying the cross-cutting framework (CN-6-100/101/102/103/104) to a real engine (1,123 lines, 19 sections). **Proof-of-framework:** doc written without amending cross-cutting docs; all 11 RE doctrines derive cleanly from VE/BD/NC/SP/HO framework; framework v1 holds on first concrete test. **Eleven Retail Engine doctrines RE1-RE11:** retail.* covers full basket-POS spectrum kiosk→duka→mini-mart→supermarket→hypermarket (RE1 — concretizes CN-6-101 §11.1.1); single canonical `retail.sale` Workflow with refined `completed` state (RE2 — Term 6 fix: `settled` renamed `completed` to avoid semantic collision with universal `checkout.settled.v1`); multi-price 5-layer resolution vertical INDICATES + Checkout K2 RESOLVES (RE3 — base + branch_override + active_promo + loyalty + customer_specific); refunds use UI-02 compensation per G5 (RE4 — full + partial mechanisms); voided baskets emit events per observability mandate (RE5 — Brief §7.1); B2C default + B2B via Party metadata no primitive extension (RE6 — BD4 push-down); retail handles ANY item type via item primitive category metadata, specialized by selling mechanism not item type (RE7 — concretizes CN-6-101 §11.1.1); basket idle timeout pre-checkout per pack rule distinct from CN-6-104 N3 post-bill timeout (RE8 — `pack.retail.basket.idle_timeout_minutes` default 30); **bulk-splittable items priced by measurement unit** (RE9 NEW — Concept-Lead-raised: rice/sukari/oil/nyama/fabric sold by kg/litre/metre from bulk lots; pack.retail.bulk_splittable_categories + pack.retail.measurement_units; Inventory native lot/offcut tracking per CN-5-003 supports natively; tax computes per pack rule per measured quantity); **remote ordering via customer mobile app** (RE10 NEW — Concept-Lead-raised: `pack.retail.remote_ordering_enabled` + customer-as-actor per CN-4-007 + `retail.fulfillment` Workflow distinct from `retail.sale` + Business ID/QR tenant identification + RE10 fulfillment lifecycle placed_by_customer → confirmed_by_tenant → prepared → ready_for_pickup_or_delivery → completed; partial D-DISC-002 closure for retail vertical); **payment method abstraction — three-layer governance** (RE11 NEW — Concept-Lead-raised CRITICAL correction on M-Pesa-specific framing: Universal Checkout CN-5-009 + CTR-028 abstract registry owns method vocabulary; regional agent per Charter Law 6 + D-003 + CTR-049 curates enabled methods per region via `pack.region.payment_methods_enabled`; tenant configures subset via `pack.tenant.payment_methods_subset`; customer selects from tenant's enabled set at tender; **retail vertical doctrine method-agnostic — NEVER hardcodes provider names; specific providers belong to pack content regional + tenant only**; ZERO M-Pesa/Tigo/Airtel/card-brand mentions in retail doctrine throughout doc — verified). **§16 RE11 dedicated section** with three-layer governance table + Why-Non-Negotiable (Law 5 compliance configured; Law 6 distribution regional; D-003 regional agent accountability; CTR-028 abstract registry consistency across all verticals) + worked-pattern language guidelines (always "mobile money push" never "M-Pesa Push"; always "tenant's enabled tender" never specific provider). **§4 Manifest** with 3 Workflow instances (retail.sale + retail.fulfillment per RE10 + retail.catalog), HO9 settlement_subscription declared on both billable workflows, 14 emit events with NC9 compensation_pair/basis_none per each, 16 pack hooks (multi_till + barcode + aisle + self_service + pricing.layer_order + pricing.combination_rules + refund.window_days + inventory_expansion_mode + basket.idle_timeout + basket.transfer_enabled + basket.depleted_item_handling + item_categories + bulk_splittable_categories + measurement_units + remote_ordering_enabled + fulfillment.delivery_enabled). **§5 retail.sale state machine** (basket_opened → items_added → checkout_initiated → bill_ready → completed via HO9; abandoned: voided | refunded; archived terminal). **§6 retail.fulfillment state machine (RE10)** (placed_by_customer → confirmed_by_tenant → prepared → ready_for_pickup/delivery → completed via HO9; abandoned: cancelled_by_customer | declined_by_tenant). **§9 Bulk-Splittable Items (RE9) + Measurement-Unit Pricing** — Mzee Karim bucha narrative; sukari rice oil concrete examples; Inventory native support via existing lot/offcut mechanism (no CN-5-003 amendment needed); tax computation per measured quantity. **§11 Refunds** with full + partial mechanism (`retail.sale.partially_refunded.v1` compensation pair; original sale stays `completed` until all items refunded; multiple partials within window allowed). **§14 Tax Integration** with concrete tenant_tax_profile gate examples — Mama Amina informal (`vat_registered: false` → Checkout K3 skips VAT computation; receipts show "VAT exempt — tenant not registered") vs Mzee Hassan VAT-registered (`vat_registered: true` → 18% TZ VAT line added; receipt shows base+VAT total). **§17 Mixed-Vertical Activation** with WP4 forward pointer reaffirming TWO-transaction canonical per N1 of mjadala (OTC retail.sale + prescription pharmacy.prescription = two separate bills + Universal Promotion loyalty linking via Party primitive; NO bundling-via-Obligation alternative; NO hybrid engine — VE2/BD7 honoured). **§18 Six Worked Patterns:** WP1 Salma's morning at Mama Amina's till — canonical happy path RE9+RE11+HO9+HO5 fan-out + tender via tenant's enabled mobile money method; WP2 Mzee Juma soap refund (NEW Kariakoo recurring character) — RE4 UI-02 compensation + pack 30-day window + cross-doc availability per N6; WP3 Mama Halima Friday-promo multi-price 5-layer resolution (cross-doc cameo continuity from CN-6-102 §11.4 + CN-6-104 WP3 — Logistics business owner appears as retail customer demonstrating Party primitive cross-vertical linking); WP4 Mixed-Vertical OTC paracetamol vs prescription dispensing at Mama Amina + Faraja (BD6 3-of-3 + CN-6-105 forward pointer + TWO-transaction canonical); WP5 Mzee Karim bucha (NEW character) — RE9 full bulk-splittable nyama lifecycle (carcass arrives 178kg → customer requests "kilo mbili za fillet" → Inventory lot-tracked deduction → bill computation per kg × measurement_value → settlement); WP6 Nina mobile app order (NEW character — Kariakoo office worker ordering rice 5kg via tenant's mobile app for pickup) — RE10 retail.fulfillment Workflow + customer-as-actor + Business ID/QR + mobile money push tender abstraction per RE11 + CTR-050 adapter dependency; Bibi Asha tertiary mention. **N-improvements N1-N6 (mjadala-ratified) all integrated** + additional Concept-Lead corrections (RE9 + RE10 + RE11 + payment terminology refactoring throughout). **CTR Strategy:** Two new CTRs filed by Overseer pre-merge per Concept-Lead correction (commit `99725fd`): **CTR-049** (Term 6 → Term 2): Regional agent role in payment method curation per region — extends D-003 with chartered review per provider per region (TZ, KE, UG, Nigeria, Ghana, etc. each have own enabled set); approved provider catalog per region; tenant onboarding selects subset from regional set; tenant deactivation lifecycle parallel CTR-045; extends CTR-028 abstract registry with regional-agent governance layer. **CTR-050** (Term 6 → Term 7): Payment adapter coverage for retail in-store + remote ordering — confirms CTR-006 covers in-store tender flow (CN-5-009 K2 + adapter callback) + remote ordering push payments per RE10 (customer-initiated mobile money push from app, card-not-present, bank push notification, QR-presented-to-customer for self-scan); Term 7 confirms framework coverage or files CTR-006 extension. CTRs cited in §19: 018, 002, 024, 026, 030, 038, 044, 045, 046, 049 NEW, 050 NEW. Cumulative CTR-046 queue unchanged (DC-NN-e from CN-6-103 + DC-NN-f from CN-6-104). **Term 6 progress: 6/12 docs** (5 cross-cutting + 1 concrete vertical). **Next per Brief §13.7: CN-6-002 Restaurant Engine** — Lodge Serengeti restaurant anchor; kitchen workflow + VI-03 table conflict (concrete demonstration) + recipe Pattern B consumption per CN-5-003 N1 + tip handling per CN-5-005 commission pattern + split billing + cross-vertical Hotel-Restaurant Obligation chain (CN-6-101 §11.4 + CN-6-104 WP3 prefigured). **Character map for Term 6 corpus extended** — new characters Mzee Karim (bucha owner, Kariakoo) + Nina (Kariakoo office worker, mobile app customer) + Bibi Asha mention — all available for cross-doc cameos in CN-6-002..004 per Mama Halima/Faraja precedent. Existing characters Mama Amina + Salma + Mzee Juma + Mama Halima + Faraja + Lodge Serengeti + Mzee Hassan + Mama na Bwana Mwema + Kilimanjaro Lodge Moshi all continue. **Proof-of-Framework Test PASSED:** CN-6-001 written without amending CN-6-100/101/102/103/104; all RE1-RE11 doctrines derive from cross-cutting framework cleanly; Mama Amina + Salma + Mzee Karim + Mzee Juma + Mama Halima + Faraja + Nina each operate within doctrine without exceptions. **Concept Lead bar honoured:** "real business + lives" — Salma's morning + Mzee Juma's allergy refund + Mama Halima's Friday-promo + Faraja's Mixed-Vertical counter + Mzee Karim's nyama-by-the-kilo + Nina's office mobile order all concrete Tanzanian SME reality. **M-Pesa correction honoured** — payment abstraction throughout; doctrine is method-agnostic; regional-agent + tenant + customer three-layer governance per RE11 + CTR-049 + CTR-050. No Charter conflict; CN-5-009 K1/K2 idempotency preserved; Law 5 compliance configured; Law 6 distribution regional; D-003 regional agent accountability honoured. No new CTRs beyond CTR-049/050; no cross-cutting framework amendment.

## 2026-06-04 (continued — Term 6 Cross-Cutting Cluster COMPLETE)

**Term 6 — Vertical Engines (5/12 — Cross-Cutting Cluster COMPLETE)**
- **Term 6 CN-6-104 (Vertical-to-Universal Hand-Off Pattern)** ratified and merged (commit `8389480`) — Term 6's fifth doc and **closing doc of the cross-cutting cluster** (770 lines, 13 sections; over 650-line target but length justified by §6 UI-01 causation chain concrete + §10 Cross-Vertical Obligation 5-step doctrine walkthrough + §11 worked patterns with full backward audit traces + 6-branch fan-out). **Nine Handoff laws HO1–HO9:** canonical handoff `<vertical>.bill.ready.v1` → Universal Checkout (HO1 — no exceptions; Insurance premiums, Workshop advance/balance, pharmacy auto-refill all emit bill.ready); vertical owns content + universal owns tender (HO2 — BD5 split-it at handoff layer); **hand-off is one-way command emission; vertical receives synchronous command-bus accept/reject; downstream universal consumption asynchronous + not awaited** (HO3 — Term 6 sync/async distinction critical for compensation semantics); UI-01 causation chain spans every handoff (HO4 — audit walks backward from receipt to originating human action; concrete event_id chain demonstrated §6); multiple universals may consume same vertical event fan-out per CN-5-100 P1 (HO5 — restaurant.bill.ready → Checkout + Accounting + Inventory + Promotion + Reporting + Advisor simultaneously); **universal-to-vertical handoff has one CANONICAL pattern (`checkout.settled.v1` → Workflow finalization for billable workflows; subscription mandatory per HO9) and several situational patterns (period close, pack effective, inventory depletion — pack-rule-dependent)** (HO6 — Term 6 critical refinement: settlement-back-subscription IS canonical not rare; without it billable Workflows stall at bill_emitted forever); compensation pattern at handoff failure (HO7 — vertical bill.recalled + Workflow → pre-bill state on universal reject; extended to customer walk-out + orphan settlement scenarios); **vertical never reaches into universal state** (HO8 — verticals read Foundation primitive projections ONLY: Party, Document, Obligation, Workflow, Consent, Identity; never other engine internal state); **settlement signal subscription is mandatory for billable workflows** (HO9 NEW — every vertical Workflow emitting `<vertical>.bill.ready.v1` MUST subscribe to `checkout.settled.v1` filtered by `originating_workflow_ref`; doctrine gate at CN-4-020 verifies; mechanizable as DC-NN-f queued for CTR-046 next amendment alongside DC-NN-e from CN-6-103). **§1.5 Parsimony — handoff is where business event becomes ledger truth** (Concept Lead bar honoured) — Salma's till tap becomes Mama Amina's books update; Mzee Hassan's project completion becomes invoiced revenue; Lodge Serengeti restaurant in-stay charges become folio entries; Mama Halima's trip closed becomes settled bill. **N-improvements (6):** N1 §3 HO9 + §9 manifest schema declarative within Workflow primitive instance — `settlement_subscription` field with event + filter + transitions_to + compensation_on_settlement_failure; doctrine gate rejects billable workflow without subscription. N2 §3 HO8 + §6 explicit Foundation projection allowlist (Party, Document, Obligation, Workflow, Consent, Identity readable; other engine state prohibited; Foundation primitives are tenant-shared infrastructure vs engine internal state). N3 §8 Failure Modes customer walk-out timeout pack defaults per vertical (Restaurant 0.5h, Hotel 24h, Workshop 168h/1wk, Pharmacy 0h, Logistics 720h/30d, Retail 0h, Insurance pack-defined); `pack.<vertical>.bill_settlement_timeout_action` ∈ {recall, convert_to_obligation} recovery branch. N4 §6+§11.1 WP1 Lodge Serengeti restaurant full UI-01 audit walk-back trace — 6-event backward chain (receipt → settled → bill.ready → table_session.bill_requested → order.added → table_session.opened → human:waiter command); 6-branch forward fan-out (settled → table_session.closed via HO9 + accounting.journal.posted + cash.tender.received + inventory.stock.deducted + promotion.rule.applied + reporting projections); concretizes UI-01 + HO4 + HO5 + HO9 in one trace. N5 §10 Cross-Vertical via Obligation primitive concrete 5-step doctrine walkthrough — obligation.created (Vertical A emits Foundation primitive event) → Vertical B subscribes via Foundation primitive (NOT calling A) → Vertical B incorporates into own Workflow → Vertical B aggregates at bill.ready → checkout.settled triggers obligation.settled → Vertical A finalizes own Workflow; demonstrates VE2/BD7 honoured (Foundation primitive is carrier; pattern repeats for charge-to-room, sell-via-POS, in-stay dining; CN-6-005 enumerates specifics). N6 §13 closing-cluster character map handoff to CN-6-001..004 authors — per-vertical anchor mapping (CN-6-001 Mama Amina's Kariakoo duka; CN-6-002 Lodge Serengeti restaurant; CN-6-003 Kilimanjaro Lodge Moshi + chain guest profile; CN-6-004 Karakana ya Mzee Hassan Arusha); after 001..004: CN-6-005 Bridges pattern catalog; CN-6-105 Mixed-Vertical Tenants; CN-6-901..904 stress-test sketches for Flexibility Test. **Per-Universal-Engine Handoff Catalog §5** (8 + Advisor row): Checkout canonical + HO9 settlement-back; Accounting projects from bill.ready + workflow events per CTR-030; **Cash INDIRECT no direct vertical handoff** (subscribes to checkout.settled + tender events); Inventory Pattern A (auto from Checkout) OR Pattern B (vertical_managed) per `pack.<vertical>.inventory_expansion_mode.<item_category>`; Procurement optional per CTR-030; HR/Payroll per CTR-030; **Reporting massive fan-in; vertical NEVER tailors emission for Reporting** (CN-5-006 N3 read-side); Promotion subscribes bill.ready + customer events; **Advisor row NEW** — vertical emits normal events + advisors subscribe to read-only projections per CN-5-010; NO autonomous advisor action (Law 3 + CTR-010). **§8 Failure Modes Catalog** (4+2 rows): universal rejects bill.ready (UI-09 unknown site_id); downstream consumer fails (universal handles internally); vertical malformed payload (bus rejects at command-time, Workflow blocked); vertical Workflow cancels pre-billing (no handoff needed); customer walks out post-bill pre-settle (pack timeout → recall OR convert_to_obligation per N3); orphan settlement no preceding bill.ready (bus rejects per K1 idempotency; doctrine-violation if observed). **Q1-Q8 rulings all closed:** Q1 one bill.ready per Workflow (layby = Obligation primitive partial settlement; recurring = new Workflow per cycle); Q2 discount_refs vertical INDICATES intent + Checkout authoritatively recomputes per K2 + PR1; Q3 Pattern A/B pack-driven per item category; Q4 `billable: false` Workflow flag + cost_recognized.v1 may still emit direct to Accounting; Q5 CN-6-104 §10 doctrine + CN-6-005 catalog split; Q6 vertical does NOT retry post-acceptance per Law 1 immutability (read-back-projection before retry); Q7 HO9 canonical + 3 situational signals; Q8 system-initiated actor `system:<scheduler_id>` + originating_obligation_ref. **§11 Worked Patterns WP1-WP4** all established characters no new hypotheticals: WP1 Lodge Serengeti restaurant full HO5 fan-out + HO9 settlement-back + complete UI-01 audit chain; WP2 Mzee Hassan workshop dual-channel handoff (bill.ready + Pattern B cut.executed) demonstrating Q3 pack-driven; WP3 Lodge Serengeti hotel + restaurant in-stay charge-to-room via Obligation primitive (Q5 closure + full 5-step Obligation chain demonstrated + Mama Halima customer cameo cross-doc continuity); WP4 Mama Halima Logistics Dar→Mwanza trip closed → bill.ready (freight + fuel surcharge + demurrage) → Checkout/Accounting with fuel as Pattern B consumption (SP4 3-of-3 multi-site-by-nature demonstrated). **CTR Strategy: ZERO new CTRs filed** — Concept Lead parsimony bar honoured. **Cumulative CTR-046 expansion queue documented §12.2:** DC-NN-e (SP8 subscription-side platform-prohibition from CN-6-103) + DC-NN-f (HO9 settlement-back-subscription mandatory from CN-6-104); both queue together for Term 4 next CTR-046 amendment cycle; both follow existing CTR-046 ratification pattern. CTRs cited: 018, 002, 024, 026, 030, 038, 044, 045, 046. D-DISC-001 + D-DISC-002 cross-referenced §12 Open Items.

### 🎯 Term 6 Cross-Cutting Cluster — COMPLETE

| Doc | Mission | Lines | Sections |
|-----|---------|-------|----------|
| CN-6-100 | The recipe (manifest + namespace + payload conformance) | 671 | 14 |
| CN-6-101 | Boundary doctrine (vertical vs universal vs Foundation) | 640 | 14 |
| CN-6-102 | Event naming (NC1-NC9 + per-family patterns) | 622 | 13 |
| CN-6-103 | Scope policy (site / tenant / multi-site-by-nature; no platform) | 636 | 12 |
| **CN-6-104** | **Hand-off pattern (HO1-HO9; canonical + situational; Obligation cross-vertical)** | **770** | **13** |
| **Total** | **3,339 lines, 66 sections; 5/5 cross-cutting docs complete** | | |

**Term 6 progress: 5/12 docs.** Next per Brief §13: **CN-6-001 Retail Engine** — first concrete vertical with Mama Amina's Duka la Kariakoo as anchor. Cross-cutting framework will be applied + tested by concrete verticals (Retail simplest baseline; Restaurant + Hotel + Workshop progressively complex; per Brief §13.6-13.9 ordering).

**Character map handed off to CN-6-001..004 authors** (per CN-6-104 §13 + N6):
- CN-6-001 Retail → Mama Amina (Kariakoo duka), Salma (cashier daughter), retail spectrum (kiosk→duka→supermarket per CN-6-101 §11.1.1)
- CN-6-002 Restaurant → Lodge Serengeti restaurant, in-stay dining patterns
- CN-6-003 Hotel → Kilimanjaro Lodge Moshi (Mama na Bwana Mwema guests), Lodge Serengeti (chain guest profile cross-reference)
- CN-6-004 Workshop → Mzee Hassan Arusha (+ hypothetical Moshi per CN-6-103 WP1), Faraja (pharmacy cross-vertical patterns)

Cross-Term: Mama Halima (Logistics from CN-6-100 §12; cross-doc customer cameos demonstrating Party primitive cross-vertical linking) continues as exemplar of multi-site-by-nature pattern. Pendo (Mama Amina's hypothetical Mwenge cousin per CN-6-103 WP3) flagged as Q1 demonstration only; not v1 commitment.

No Charter conflict; CN-5-009 K1/K2 idempotency preserved at handoff layer; Law 1 immutability honoured (forward-only scope migration); Law 2 isolation preserved (BD7 no bridge engines; cross-vertical via Foundation Obligation primitive); Law 3 advisory-only preserved (Advisor row read-only subscription); Law 4 flexibility honoured (HO1 canonical handoff + HO6 situational signals); Law 5 compliance-configured honoured (pack-driven timeouts + Pattern A/B + situational signal selection); Charter §1.2 legal-defensibility honoured (UI-01 causation chain across handoff; concrete event_id traces). Cross-cutting cluster COMPLETE.

## 2026-06-04

**Term 6 — Vertical Engines (4/12)**
- **Term 6 CN-6-103 (Vertical Scope Policy)** ratified and merged (commit `02915fe`) — Term 6's fourth doc (636 lines, 12 sections). Operational application of CN-5-101 site/tenant/platform scope doctrine to verticals specifically. Site default + tenant-scope exception catalog + multi-site-by-nature pattern + platform prohibition + per-operation `scope_ref` override + scope migration discipline + site deactivation lifecycle. **Eight Scope Policy laws SP1–SP8:** vertical default scope is site (SP1 — real business operations happen at physical locations; duka, kitchen, room, fundi station, cabinet, depot); verticals NEVER declare platform scope (SP2 — Law 2 + BD3 + AP1 enforcement; platform reserved for Foundation/Term 1 billing aggregation per CN-4-006 §2); tenant-scope is explicit opt-in per operation via CTR-023 `scope_ref` override (SP3 — not default; business semantics must genuinely span sites); **multi-site-by-nature requires 3-of-3 criteria test as gate** (SP4 — Term 6 sharpening per §7 sequential AND discipline: primary entity has identity traversing sites + operation needs BOTH origin and destination as authoritative payload at emission time + downstream universal engines need site disambiguation in payload not subscription; any one fails → fall back SP1 site-scope + read-side projection; criteria are gate not heuristic; fail cases walked include restaurant-in-hotel, cross-site loyalty, sequential workshop project); site_id payload conformance mandatory for site-scope events (SP5 — already CTR-024); site_id registry resolution via CTR-027 (SP6 — already UI-09); scope migration is forward-only manifest amendment per CN-4-020 manifest-update flow (SP7 — historical events stay at original scope per Law 1 immutability; read-side projections span boundary; runtime mutation prohibited); verticals NEVER subscribe at platform scope (SP8 — parallel SP2; cross-tenant data routes through Universal Reporting tenant-scope projections or Term 1 platform-aggregator subscriptions per CN-5-101 §7). **§1.5 Parsimony — scope is where the number shows up** (Concept Lead bar honoured) — Salma at Mama Amina's till asking "today's revenue" gets site-scope retail; Mama Amina asking "all my counters today" crosses two verticals at same site (still site-scope aggregated reads); Mzee Hassan reading "active projects across both my workshops" needs tenant-scope workflows; Lodge Serengeti GM checking "tonight's occupancy" sees site-scope hotel reservations; Faraja printing monthly TFDA controlled-substance report gets site-scope per premises registration. "Parsimony is the bar. Each SP1-SP8 is justified against the question: does this match how the real person at the till, the front desk, the controlled-substance cabinet, the fundi station thinks?" **N-improvements (6):** N1 §3+§7 SP4 sequential AND discipline ("gate, not heuristic"; any one fails → SP1 site-scope + read-side projection); N2 §11+§10.2 hypothetical character flagging on Mzee Hassan Moshi fabrication branch + Mama Amina Mwenge expansion under cousin Pendo's TFDA license + cousin Pendo character — all marked "(hypothetical for Q# demonstration; not a v1 commitment)"; N3 §10 site deactivation event namespace TBD pending Term 1 activation (likely `platform.*` or `tenant.*` per future CN-5-103 §16 amendment; CN-6-103 documents vertical-side subscription contract only — transitions to `<workflow>.site_deactivating.v1` blocked state per CN-6-100 §3.13); N4 §10.2 concrete migration walkthrough — Day 1 single-site Arusha emits `workshop.style.registered.v1 {style_id, site_id: arusha-karakana}` at site-scope; Day 90 Moshi branch opens triggering manifest amendment to migrate `workshop.style_library.entry.added.v1` from site to tenant scope citing SP3 tenant-IP exception; Day 91 manifest amendment activates at `pack_version_ref: workshop-pack-2026.06` and forward-only tenant-scope emissions begin; Day 92 onward read-side projection spans pre-boundary site-scoped events + post-boundary tenant-scoped events for unified "all my styles" view; historical Arusha-scoped events unchanged per Law 1; N5 §1.5 Parsimony scope-flavored examples (Salma + Mama Amina + Mzee Hassan + Lodge Serengeti GM + Faraja with concrete question-and-answer pattern); N6 §5.4 SP8 mechanizable check spec — DC-NN-e proposed "for any engine_kind: vertical, no subscription scope_ref: platform allowed" queued for CTR-046 next amendment cycle (no new CTR; pair with DC-NN-b for emission-side closure). **§6 Tenant-Scope Exception Catalog refined** per Term 6 mjadala refinements: hotel guest profile NOT folio pool (folios remain site-scope per stay; profile tenant-scope aggregator with chain-wide history); workshop style library (Mzee Hassan parametric formulas + cut-optimization templates + fundi training references = tenant-wide IP); retail tenant-wide pricing catalog (sales remain site-scope; catalog itself tenant-wide); pharmacy controlled-substance audit site-default + pack-driven tenant opt-in via `pack.pharmacy.controlled_substance.audit_scope_default` ∈ {site, tenant} (TZ TFDA = per-premises ledger per TFDA premises registration; Kenya configs may opt tenant-scope; Law 5 multi-jurisdiction honoured); customer loyalty REMOVED from catalog (Universal Promotion + Party primitive handle per CN-6-102 §11.4 walkthrough). §6.1 The justification test — three questions before adding tenant-scope exception: does operation genuinely span sites in business semantics? is the entity tenant-shared by nature? would a real user say "this number lives at my business level"? **§7 Multi-Site-by-Nature Pattern Catalog refined** per Term 6 mjadala: Logistics trip canonical (Mama Halima — origin_site_id + destination_site_id authoritative at emission); inter-site Inventory transfer (universal engine pattern cited for completeness); hotel chain reservation REMOVED (bundled site-scope by booking_id Workflow reference, NOT multi-site-by-nature — pattern-discipline preserved; pattern overload prevented); workshop multi-site project deferred to CN-6-004 (placeholder only in v1). **§11 Worked Patterns WP1-WP4:** WP1 Mzee Hassan workshop mixed-scope manifest demonstrating Q4 per-operation `scope_ref` override (style library tenant-scope + cuts site-scope same manifest); WP2 Hotel chain split-by-event-type (Lodge Serengeti + Kilimanjaro Lodge Moshi same tenant — reservations site-scope + guest profile tenant-scope; rejection of multi-site-by-nature for chains); WP3 Faraja pharmacy controlled-substance site-default (Kariakoo premises ledger demonstrating Q1 TZ TFDA practice + Mama Amina Mwenge hypothetical Q1 demo); WP4 Mama Halima Logistics multi-site-by-nature (Dar → Mwanza trip demonstrating SP4 3-of-3 all-pass — trip entity traverses sites + origin+destination authoritative + universal Inventory + Cash need disambiguation in payload). **Q1-Q8 rulings closed:** Q1 pharmacy site-default + pack opt-in per TZ TFDA practice (per-premises ledger reality); Q2 hotel chain split-by-event-type (reservation=site; profile=tenant; NOT multi-site-by-nature); Q3 workshop multi-site project deferred to CN-6-004; Q4 mixed-scope manifest supported via CTR-023 (WP1 anchor); Q5 multi-site-by-nature when both sites authoritative at emission; Q6 single-jurisdiction v1 per CTR-027; Q7 site deactivation doctrine + Term 1 governance + CN-6-001..004 elaboration; Q8 forward-only scope migration per Law 1 immutability (concrete walkthrough §10.2). **Character map for Term 6 corpus extended:** Mzee Hassan adds hypothetical Moshi fabrication branch; Pendo (Mama Amina's hypothetical Mwenge cousin under TFDA license) added as hypothetical Q1 anchor — both marked "(hypothetical for [Q#] demonstration; not a v1 commitment)" per N2 flagging discipline; existing characters (Salma, Faraja, Lodge Serengeti+Kilimanjaro Lodge Moshi, Mama Halima) continue corpus continuity from CN-6-100/101/102. **CTR Strategy: ZERO new CTRs filed by this doc** — Concept Lead parsimony bar honoured. CTRs cited: CTR-023 (per-operation scope_ref; closed/delivered), CTR-024 (site_id payload; Term 6 ratified), CTR-026 (UI-03+UI-09 vertical-side; Term 6 ratified), CTR-027 (site registry + currency registry; Term 1 governance pending), CTR-044 (engine_kind flag; Term 4 pending), CTR-045 (vertical onboarding governance; Term 1 pending), CTR-046 (mechanizable anti-patterns; SP8 subscription-side check DC-NN-e queued for next amendment cycle). Term 6 progress: 4/12 docs. Next per Brief §13: CN-6-104 Vertical-to-Universal Hand-Off Pattern (closes cross-cutting docs cluster before CN-6-001..004 verticals concrete). No Charter conflict; CN-5-101 operational subordination preserved; SP4 sharpening prevents multi-site-by-nature creep.

## 2026-06-03 (continued)

**Term 6 — Vertical Engines (3/12)**
- **Term 6 CN-6-102 (Vertical Event Naming Conventions)** ratified and merged (commit `9df72fa`) — Term 6's third doc (622 lines, 13 sections; longer than ~400-line target but content fully warranted by N1-N6 substantive integration + 4 worked-pattern applications + concrete schemas). Operational reference doc applying CN-5-103 G1-G9 doctrine to vertical-specific naming patterns. Establishes the conventions every CN-6-001..004 vertical and every future vertical (CN-6-901..904) follows when authoring events + commands. **Nine Naming Convention laws NC1–NC9:** verticals follow CN-5-103 G1-G9 verbatim (NC1 — no naming exceptions; CN-6-102 is operationalization, not override); 3-segment minimum, 4-segment when grouped logically, 5+ prohibited (NC2 — Term 6 fix dropping 2-segment option entirely because 2-segment violates G1 categorically); bill.ready uniformity exactly 3-segment `<vertical>.bill.ready.v1` for Checkout subscription canonical detection (NC3); conflict family `<vertical>.<noun>.conflict.detected.v1` 4-segment per VI-03 (NC4); Workflow lifecycle events use 4-segment `<vertical>.<workflow>.<state_verb>.v<n>` (NC5 — transition INTO state semantic; state nouns and present-participles prohibited per G9); **regulatory events use jurisdiction-neutral `regulated_subject`** (NC6 — Term 6 critical pushback: `pharmacy.controlled_substance.dispensed.v1` NOT `pharmacy.tfda.*`; jurisdiction binding via pack honours CN-5-105 §11 multi-jurisdiction framework + Law 5 compliance-configured-not-coded); versioning inherits CN-5-103 G6 verbatim (NC7); reserved-keys catalog prohibits `<vertical>.kernel.*`, `<vertical>.pack.*`, `<vertical>.test.*`, `<vertical>.<other_vertical>.*`, `<vertical>.system.*`, `<vertical>.checkout.*`; discourages `<vertical>.advisor.*` (NC8); **compensation pair declaration is mandatory in the manifest at registration** (NC9 new doctrine — operationalises G5; mechanizable via CTR-046 expansion DC-NN-d). **§1.5 Parsimony Paragraph** (Concept Lead bar honoured) — naming patterns are spoken by Salma at Mama Amina's till in Kariakoo; Mzee Hassan reading audit trail in Arusha; Faraja the pharmacist trusting `pharmacy.controlled_substance.dispensed.v1` means what it sounds like; Kilimanjaro Lodge Moshi front desk onboarding seasonal staff every dry season; **patterns that use ordinary business words, version cleanly, reject ambiguity — patterns that require a glossary die in the first month — Parsimony is the bar.** **N-improvements (6):** N1 NC9 manifest schema inline `compensation_pair: <event>` OR `compensation_basis_none: <rationale>` per emit entry; bus check at CN-4-020 registration rejects manifests missing both fields (special case advisor suggestions + projection-only emissions documented with compensation_basis_none rationale). N2 NC6 regulated_subject pack hook schema in §8.3 — `pack.<vertical>.<regulated_subject>.{regulatory_body_ref, reporting_template_ref, audit_retention_days, notification_required, notification_channel_ref, controlled_categories}` with concrete TZ-pharmacy values (TFDA, 7-year audit retention, schedule_1-3 controlled categories). N3 cross-vertical reference concrete schema §11 — CORRECT (party_ref query-time resolution via Party primitive) vs INCORRECT (embedded `retail_history_refs: [event_ids]`); pharmacy queries Party projection at read-time, never carries upstream vertical event refs in own payload. N4 NC5 state-as-verb examples §6 — CORRECT (`hotel.reservation.confirmed.v1`, `restaurant.table.opened.v1`, `workshop.project.quoted.v1`) vs INCORRECT (`hotel.reservation.in_house.v1` state noun; `restaurant.table.occupied.v1` state adjective; `workshop.project.cutting.v1` present-participle violating G9); steady-state rule "use `<state>_started.v1` past-tense if entry event needed." N5 Mama Halima Logistics cross-reference §12 intro — character continuity bridge from CN-6-100 §12 (Mama Halima freight broker Dar) to CN-6-102 WP1-WP4. N6 Mama Amina + Faraja Mixed-Vertical loyalty concrete walkthrough §11.4 — full event chain (retail.sale.completed.v1 with party_ref → Promotion subscribes → promotion.loyalty.earned.v1 → later pharmacy.prescription.bill.ready.v1 with loyalty_redeem_intent → Promotion subscribes via Checkout → promotion.loyalty.redeemed.v1 → checkout.settled.v1 reflects discounted total) demonstrating NO `cross_vertical.*` events anywhere; pure BD5 split-it via Promotion universal + Party primitive linking; bonus character cameo Mama Halima (Logistics business owner from CN-6-100) appears as Party customer in the walkthrough. **§9 Pattern Catalog** with 12 rows including 3 additions per mjadala (document amendment `<vertical>.<doc_kind>.amended.v1`; workflow blocked state `<vertical>.<workflow>.blocked.v1` with reason; customer interaction party_ref discipline `<vertical>.customer.<verb>.v<n>` always carries `party_ref` to existing Party primitive — verticals NEVER invent customer rows); explicit **no `cross_vertical.*` row** with §11 walkthrough cross-reference. **Q1-Q10 rulings** (all Term 6 answers endorsed): Q1 4-segment cap; Q2 transition-verb past-participle; Q3 jurisdiction-neutral regulated_subject; Q4 `<vertical>.system.*` prohibited per NC8; Q5 NO `cross_vertical.*` namespace (Brief §11.5 closure via Promotion universal split-it + Party primitive); Q6 inherit G6 verbatim; Q7 test events prohibited (Developer-AI sandbox per D-002B + CN-4-023); Q8 cross-vertical via Party/Obligation primitives ONLY (direct event_id refs anti-pattern); Q9 compound noun heuristic; Q10 payload `kind` discriminator parsimoniously per CN-5-007 precedent. **§12 Worked Patterns WP1-WP4:** WP1 Kilimanjaro Lodge Moshi hotel reservation (Mama na Bwana Mwema from Mwanza booking weekend — confirmed → checked_in → checked_out lifecycle + conflict detection on overbooking + cancelled compensation pair); WP2 Faraja pharmacy prescription dispensing (NC6 regulated_subject controlled_substance + NC9 compensation pair dispensed↔recalled + NC5 lifecycle received→validated→dispensed→archived); WP3 Lodge Serengeti restaurant table session (NC4 conflict on two-waiters-same-table + NC5 lifecycle + compound noun kitchen_ticket); WP4 Mzee Hassan Arusha workshop project quote-to-cut (NC5 lifecycle through long workflow + NC9 compensation cut.executed↔cut.reversed + NC7 versioning when style amendment mid-project). **Character map for Term 6 corpus** established: Mama Halima (Logistics, CN-6-100), Mama Amina + Faraja + Salma (retail+pharmacy Mixed-Vertical, CN-6-101+CN-6-102), Mzee Hassan (workshop, CN-6-102 WP4), Lodge Serengeti (hotel+restaurant cross-vertical, CN-6-101+CN-6-102 WP3), Kilimanjaro Lodge Moshi (hotel, CN-6-102 WP1), Mama na Bwana Mwema (hotel guests, CN-6-102 WP1) — long-term continuity across CN-6-001..004 future docs. **CTR-046 expanded** (commit `2361014`) per pre-merge — appended DC-NN-d "every vertical event family declares compensation_pair OR compensation_basis_none at manifest registration" for NC9 enforcement. **NO new CTRs filed by this doc** — Concept Lead parsimony bar honoured; CN-6-102 is pure operationalization. Pending references: CTR-044 (engine_kind flag), CTR-045 (vertical onboarding), CTR-047 (data_sensitivity_tier), CTR-037 (pack.* namespace). D-DISC-001 (tenant-customer UX) + D-DISC-002 (POS self-service) cross-referenced in §13 Open Items. Term 6 progress: 3/12 docs. Next per Brief §13: CN-6-103 Vertical Scope Policy. No Charter conflict; Charter §10 (no code) honoured; CN-5-103 G1-G9 operational subordination preserved.

## 2026-06-03

**Term 6 — Vertical Engines (2/12)**
- **Term 6 CN-6-101 (Vertical Boundary Doctrine)** ratified and merged (commit `a0e943d`) — Term 6's second doc (640 lines, 14 sections). The decision-tree closure of Brief §6.2 Mission Question: *"What belongs in a vertical vs universal vs foundation?"* Parent boundary doctrine answering "should this thing BE a vertical at all?" — complementary to CN-6-100's recipe ("how to add"). **Eight Boundary Doctrine laws BD1–BD8:** Foundation owns what every engine needs and never changes per business domain (BD1 — primitives, mechanism, doctrine enforcement); Universal Engines own what every business needs regardless of vertical (BD2 — the "all 4 current verticals use it" test); Verticals own what is unique to one business type (BD3 — anti-test: if multiple verticals want it, push up); **when in doubt, push DOWN toward Foundation** (BD4 — reuse maximization; recursive application within each layer per N1 — within Foundation prefer existing primitives like Obligation kind-tagging; within Universal prefer extending engines via pack hooks; within Vertical prefer Workflow primitive + pack rules over sub-engines); **Split-It Pattern** (BD5 — when verticals share a concept, split: universal mechanism + per-vertical context; documented examples: Promotion universal + per-vertical triggers per CTR-030; Tax universal doctrine + per-vertical `tax_treatment_ref`; Inventory universal + per-vertical Pattern B emissions); **regulated specificity does not automatically make a vertical** (BD6 — explicit 3-of-3 test per N3: own namespace + own Workflow lifecycle + own regulatory/domain events; all three required; Pharmacy passes for prescription side, Salon fails 1.5-of-3); **no bridge engines** (BD7 — cross-vertical = Obligation primitive + universal-engine event intermediary, NEVER new engine; closes Brief §11.2); **no hypothetical-driven up-promotion** (BD8 — YAGNI applied to layers; ≥2 current verticals must demonstrate need before Universal promotion). **Doctrine closures of Brief open questions:** Brief §11.2 (vertical bridges as first-class) closed by BD7 — CN-6-005 becomes pattern catalog, not engine doc; Brief §11.7 (Pharmacy = vertical or retail-with-attributes?) closed by BD6 3-of-3 — Pharmacy IS a vertical for prescription side via `pharmacy.*` namespace + `pharmacy.prescription` Workflow + TFDA controlled-substance events; OTC operations remain retail (Mixed-Vertical Tenant per CN-6-105); Brief §11.9 (salon/car wash "almost-a-vertical") framed by BD6 1.5-of-3 fail + BD8 ≥2 threshold met — cluster (salon + kinyozi + nail + car wash + boda boda mechanic + cobbler + fundi simu) elevated to high-priority CN-6-905 stress-test (NOT indefinite defer); concrete sketch decision = post-CN-6-905 Concept Lead market-priority ruling. **Five worked examples §11 grounded in real Tanzanian business reality** (per Concept Lead bar — "kuleta huwalisia katika real business na lifes"): §11.1 Pharmacy + R1+R4 hybrid closure (BD6 3-of-3 pass for prescription; OTC remains retail; Mixed-Vertical pattern); **§11.1.1 Retail spectrum clarification** (per Concept Lead R8 — `retail.*` covers full basket-POS spectrum: kiosk, duka, mini-mart, supermarket, hypermarket, department store; same Workflow `retail.sale` across all; differences via pack hooks `multi_till`/`barcode_required`/`aisle_layout`/`self_service_enabled` and operational scale; Mama Amina Kariakoo duka and Nakumatt Mlimani City supermarket share same vertical engine; retail handles any item type — rice, sugar, soap, OTC drugs, cooking oil, sundries, clothing, electronics, hardware; item primitive carries category metadata, pack rules drive per-category treatment; retail specialises by selling mechanism not by item type); §11.2 Salon + light services cluster (BD6 fail + BD8 met framing per R2+R3); §11.3 Insurance Claim Ledger (BD5 split-it — universal Accounting + vertical claim context); **§11.4 Lodge Serengeti Hotel+Restaurant in-stay charge** (R5 — BD7 closure concrete demonstration: charge-to-room via Obligation primitive `kind: hospitality_charge`; restaurant Workflow + hotel folio Workflow communicate via Foundation primitive; no bridge engine); §11.5 Workshop Cut List (BD3 parametric/numeric vertical-uniqueness); **§11.6 Mama Amina expansion narrative (R4 grounding)** — Duka la Mama Amina Kariakoo expanding to add prescription counter with TFDA-licensed pharmacist (cousin Faraja); doctrine walks through Steps 1-4 (BD3/BD6/BD4+BD5/CN-6-105 Mixed-Vertical activation); day-to-day experience demonstrated (cashier daughter Salma routes paracetamol via retail SKU + prescription via Faraja's pharmacy workflow; separate revenue tracking; pack-driven tax treatment per item category; TFDA license at pharmacy activation per CTR-045 regulatory_evidence_refs; period close per CN-5-104 same tenant tax profile per CN-5-105 N7); Charter §1.1 honoured — Mama Amina expanded without losing duka identity; Mixed-Vertical Tenant is the right answer, CN-6-101 is why we know that. **Anti-Patterns Catalog §10 with concrete BOS examples (R6)** — AP1 Premature Universalization: Procurement was NOT promoted to "Universal Supply Chain" (freight features → future Logistics vertical per CN-6-100 §12); Tax-Awareness was scoped to engines NOT a "Tax Engine" (pack content + per-engine wiring per BD5 split-it); Cash didn't become "Universal Treasury" (FX hedging + bank loans + investments = future verticals). **§12 VI-NN Ratifications** — VI-03 (conflict event family — `<vertical>.<noun>.conflict.detected.v1` for bus single-acceptance rejections; concrete in Restaurant table double-booking, Hotel room conflicts, Workshop project re-quotes) and VI-04 (regulated vertical pre-activation gates — `regulatory_evidence_refs[]` at CN-4-020 registration per CTR-045) ratified with boundary-doctrine basis. VI-05 (`data_sensitivity_tier`) deferred per primitive-level discussion required — files CTR-047 to Term 4. **§14 Open Items** — D-DISC-001 (tenant-customer promotion UX → Term 3 territory per R7) and D-DISC-002 (POS self-service expansion → CN-6-100 §3 Step 5 Workflow primitive accommodates; concrete activation deferred) cross-referenced; existing-vertical retrofit out of scope (Architect-phase concern per Q7); Charter §4 pure-elaboration framing (Q9 — no architecture amendment). **Two new CTRs opened (`a0e943d` + register update):** CTR-046 (Term 6 → Term 4) mechanizable anti-patterns as CN-4-019 doctrine checks — DC-NN-a (no vertical-vertical subscription per VE2+BD7), DC-NN-b (no vertical platform-scope per BD3), DC-NN-c (≥2 vertical subscriptions before Universal-engine introduction per BD8 partial); CTR-047 (Term 6 → Term 4) `data_sensitivity_tier` primitive extension per VI-05 deferral — Party/Document/new-primitive options for Term 4 ratification; pairs with CN-6-902 Healthcare Clinic stress-test prerequisite. CTRs ratified Term 6 side: CTR-018, CTR-002, CTR-024, CTR-026, CTR-030, CTR-038. CTR-044/045 still in-flight upstream (cited). Term 6 progress: 2/12 docs. Next per Brief §13: CN-6-102 Vertical Event Naming Conventions. No Charter conflict; Charter §4 pure elaboration.

## 2026-06-02 (continued — Term 6 begins)

**Term 6 — Vertical Engines (1/12 — first doc)**
- **Term 6 CN-6-100 (Vertical Extension Framework)** ratified and merged (commit `be67b1d`) — Term 6's first doc and the foundation of all Term 6 work (671 lines, 14 sections). Meta-framework — "the recipe" answering Brief §2 Mission Question: how to add Insurance 2027 or Hospital 2028 following the same clean pattern without touching what exists. Sits as parent of CN-6-101 (Boundary Doctrine), CN-6-102 (Event Naming), CN-6-103 (Scope Policy), CN-6-104 (Hand-Off Pattern); CN-6-001..004 existing verticals (Retail/Restaurant/Hotel/Workshop) instantiate it; CN-6-005 (Bridges), CN-6-105 (Mixed-Vertical Tenants), CN-6-901..904 (stress-test sketches) extend it. **Seven Vertical Extension laws VE1–VE7:** vertical is engine first per CN-4-005 + CN-4-020 with `engine_kind: vertical` per CTR-044 (VE1 — no special primitive; kernel doesn't branch on vertical identity); vertical never calls another vertical (VE2 — cross-vertical via Obligation primitive OR universal-engine event intermediary, NEVER direct call); vertical owns its namespace exclusively (VE3 — `retail.*`/`restaurant.*`/`hotel.*`/`workshop.*`/`pharmacy.*`/`clinic.*` per CTR-038; never `kernel.*` per CTR-036 or `pack.*` per CTR-037 or other vertical's namespace); vertical emits saleable lines, not tender (VE4 — `<vertical>.bill.ready.v1` to Universal Checkout per D-001 + CTR-002; vertical never knows "M-Pesa" or "split"); vertical payloads conform to universal contracts (VE5 — CTR-024 site_id, CTR-026 UI-03+UI-09, CTR-030 Accounting-sufficient; conformance is vertical's responsibility; universal hard-fails non-conformant); adding a vertical never modifies Foundation/Universal/other vertical (VE6 — additive only; Brief §8.6 mechanizable via CN-4-019 DC); **stateful vertical processes use the Workflow primitive (CN-4-011), not bespoke state machines** (VE7 — Hotel reservation, Workshop project, Restaurant table session, Insurance policy, Healthcare appointment all become Workflow instances; vertical contributes state vocabulary + transitions + guards; mechanism is Foundation Workflow; bespoke state machines = lost traceability + bug-prone compensation = recurring root cause of Brief §12 gaps). **The Recipe §3 — 12 mechanical+human-readable steps** for adding a new vertical: reserve namespace; declare manifest with engine_kind: vertical; emit `<vertical>.bill.ready.v1`; conform to payload contracts; use Workflow primitive for stateful processes; declare scope policy; declare pack-hook namespace (`pack.<vertical>.<sub_domain>.<key>` 4-segment per N6); declare tax_treatment_ref paths via `pack.tax.lookup()` (never compute rates); declare advisors via CN-4-022+CN-5-010; declare regulated-vertical hooks; submit via CN-4-020 registration; pass the Flexibility Test (Brief §14). Each step has a mechanical artifact AND a human-readable check. **§4 Vertical Manifest Delta** (additions on top of CN-4-005): `engine_kind: vertical`, mandatory namespace declaration, mandatory `<vertical>.bill.ready.v1` emission if billable, optional `workflow_instances[]` declaration (mandatory if any stateful process), `multi_site_capable: bool` for verticals like Logistics, `pack_hook_root: pack.<vertical>`, optional `regulated: true` with `activation_evidence_requirements[]`. **§5 Namespace + Naming Discipline** — namespace ownership per CTR-038; G1–G9 conventions inherited from CN-5-103; sub-namespaces 4-segment for grouped events; G2 (`kernel.*` reserved) honoured without waiting for DC-042 ratification. **§6 Mandatory Payload Conformance** — 7 vertical-side commitments documented including Term 5 hooks (CN-5-105 N7 tenant_tax_profile respect; CN-5-104 PC11 no cascade-close; CN-5-006 N3 Statements ≠ vertical emissions; CN-5-009 K1/K2 Checkout idempotency; CN-5-103 G1-G9; CN-5-102 UI-NN respect). **§7 Subscription Model** — verticals subscribe only to Foundation primitives or Universal engine emissions; verticals never subscribe to other verticals; cross-vertical communication via Obligation primitive (charge-to-room pattern) or universal-engine event intermediary (Restaurant subscribes to `inventory.stock.depleted.v1`, not `retail.sale.completed.v1`). **§8 Scope Discipline** — site default; `multi_site_capable: true` exception for verticals operating across sites by nature (Logistics trips; future Insurance multi-branch); tenant scope reserved for tenant-property events. **§9 Vertical-side Invariants — VI-NN Catalog (Starter)** — N2 boundary clarification: UI-NN (CN-5-102) = cross-engine state shape enforced at subscription dispatch (Term 5 owns); VI-NN (CN-6-100) = vertical-emission discipline enforced at vertical command/event boundary (Term 6 owns); complementary catalogs. V1 ratifies VI-01 (every billable workflow emits `<vertical>.bill.ready.v1`) + VI-02 (every stateful process is a Workflow instance — VE7 runtime check). Candidates VI-03 (conflict event family), VI-04 (regulated vertical pre-activation gates pending CTR-045), VI-05 (data_sensitivity_tier pending CN-6-101 ruling) listed as placeholders; Term 6 augments as CN-6-001..004 surface real cases. **§10 Vertical Onboarding Governance Schema** (Term 1 fills per CTR-045) — vertical catalog per subscription tier; per-tenant activation with optional chartered review (D-003 regional agent confirms business type); combo rules per pack; deactivation lifecycle (in-flight Workflow handling); retirement migration path. **§11 The Flexibility Test (Brief §14)** — Term 6 commits to running test post-CN-6-100; 10-question pass criteria specified (namespace, primary Workflow, bill emission, subscriptions, pack hooks, scope, regulated hooks, Charter audit, mixed-vertical, edge case); unfamiliar reader must answer all 10 with reference only to CN-6-100. **§12 Worked Walkthrough — Logistics Vertical** (pre-emptive Flexibility Test demonstration per N3) — Logistics chosen because it lies outside both Term 6 planned scope AND Brief §6.3 stress-sketch list (Insurance/Healthcare/Education/Marketing); its multi-site-by-nature character (vehicles, trips, depot/destination sites) stress-tests §8.3 — the only new pattern CN-6-100 introduces. §12.1 answers all 10 Flexibility Test questions (namespace `logistics.*`; primary Workflow `logistics.trip` with planned→dispatched→in_transit→arrived→closed lifecycle; bill `logistics.bill.ready.v1` with freight+fuel+demurrage lines; subscriptions to Obligation/Workflow/Consent/Inventory/Accounting/Checkout/Pack; 5 pack hooks; multi-site-by-nature with `multi_site_capable: true`; regulated hooks for vehicle licensing + driver certification + cargo manifest; Charter Law 4 audit demonstrated; mixed-vertical Workshop→Logistics via Obligation; mid-trip cargo damage stress-test with UI-02 compensation). §12.2 Mama Halima freight broker Dar es Salaam 5-day narrative (Day 1 onboarding via regional agent with TLB transport license per D-003; Day 2 first trip Dar→Arusha with Workflow lifecycle + Pattern B fuel consumption; Day 3 arrival, billing, M-Pesa settlement via CTR-006; Day 5 disputed cargo damage with UI-02 compensation + Law 3 confirmed no AI auto-decision). **§13 Boundaries column mapping** — explicit "CN-6-100 owns / CN-6-101..104 owns / Term 4 owns / Term 5 owns / Term 1 owns / Term 3 owns / Term 7 owns" table preventing scope creep. **§14 Open Items + Cross-Term Hooks** — 6 subsections: CTRs ratified Term 6 side (CTR-018/002/024/026/030/038); CTRs opened in flight (CTR-044/045 per `c757c7a`); CTRs pending upstream (CTR-036/037/041 Term 4); CTRs pending downstream (CTR-004 Term 3, CTR-005 Term 1, CTR-010 Term 7); open items inside Term 6 scope (VI-NN augmentation, Pharmacy criteria → CN-6-101, multi-jurisdiction v2, three-way bridges → CN-6-005, state migration Architect-phase, self-service POS per D-DISC-002 deferred); **existing-vertical irregularities the framework prevents** (Brief §12 Gap B) — explicit 7-row prevention table mapping each irregularity (bespoke state machines, cross-vertical direct calls, schema modifications, inconsistent payload, independent tax computation, vertical Statement issuance, pre-computed tender) to the VE that prevents recurrence. **N-improvements N1–N6 all integrated** (N1 VE7 Workflow §2; N2 VI-NN↔UI-NN boundary §9.1; N3 Logistics sketch + narrative §12; N4 DOES/DOES NOT §1.2; N5 CTR linkage §14; N6 pack 4-segment §3 Step 7). Charter §10 (no code) honoured. Brief §13 ordering honoured (CN-6-100 first). Brief §14 Flexibility Test commitment + pre-emptive proof. Term 5 doctrine respected (N7 tenant_tax_profile, PC11 period independence, N3 statements, K1/K2 Checkout). Term 6 progress: 1/12 docs. Next per Brief §13: **CN-6-101 Vertical Boundary Doctrine**. No new CTRs beyond CTR-044/045 already open; no Charter conflict.

## 2026-06-02 (continued — TERM 5 SCOPE COMPLETE)

**Term 5 — Universal Engines (15/15 — SCOPE COMPLETE)**
- **Term 5 CN-5-105 (Tax-Aware Engines)** + **3 cosmetic amendments (CN-5-001, CN-5-010, CN-5-102)** ratified and merged (commit `302450a`) — Term 5's sixth and **final** cross-cutting doc (1,169 lines, 19 sections). **TERM 5 SCOPE COMPLETE — 15 docs total** (6 cross-cutting + 9 engine-numbered; tax-advisor as 9th advisor in CN-5-010 v2). Cross-engine tax-aware wiring leveraging existing primitives: `tax_treatment_ref` (CN-4-021 input), Obligation kinds (CN-4-011 with 4 new tax kinds per TX12), Compliance DSL (CN-4-015), Document Engine (CN-4-012 for tax return Statements). **Twelve laws TX1–TX12:** tax = pack-content + engine-mechanism, **no tax law in engine code** (TX1 — Law 5); `tax_treatment_ref` input mechanism set by source engine, resolved by Accounting at journal, respected by Reporting at Statement (TX2 — CN-4-021); tax events carry causation chain to originating economic event (TX3 — UI-01); tax rates pack-versioned per D-009 freeze (TX4 — mid-period rate change deterministic); output VAT at Checkout settlement (TX5 — CN-5-009 K3); input VAT at Procurement invoice with pack-driven recoverability (TX6); withholding tax computed at deduction time creating distinct `wht_payable` Obligation (TX7); reverse-charge VAT per pack rule with simultaneous output+input (TX8); tax-period vs financial-period distinct (TX9 — separate close mechanism); multi-jurisdiction with primary + secondary jurisdictions framework (TX10 — v1 single-jurisdiction; v2 deferred); tax-advisor as 9th advisor (TX11 — CN-5-010 amendment); **tax obligations use Obligation primitive with 4 kinds** (TX12 — `tax_authority_payable`, `tax_authority_receivable`, `wht_payable` distinct from output_vat due to settlement cadence + per-employee tracking + separate statutory return, `tax_assessment_payable` for backdated audit per Q13). **Eight N-improvements (N1–N8 with N7/N8 Concept-Lead-raised doctrine-critical):** N1 TX12 wht_payable distinctness footnote; **N2 UI-11 Closed Tax-Period Inviolability** added to CN-5-102 living catalog per Charter §11 (parallel UI-05 but on tax dimension); N3 verified all 5 new accounting events G1/G3 compliant; N4 tax-advisor pack hooks formalized (`pack.advisor.tax.min_tax_periods_with_activity` default 2, `min_tax_returns_filed` default 1, `tier_range: [medium, high]`, `schedule_default`); N5 jurisdiction_ref implicit v1 / mandatory v2 framework; N6 Cash `tax_treatment_ref` set ONLY when payment is originating tax event (WHT-at-payment); **N7 `tenant_tax_profile` mechanism** (Concept-Lead doctrine-critical addition — pack declares per-jurisdiction defaults: `vat_registered` bool default false, `has_formal_employees` bool default false, `wht_obligated`, `tax_exemptions[]`, `informal_sector` bool default true, `tax_authority_ref`, `vat_threshold_status`, `primary_jurisdiction`. **Conservative default = informal sector** until tenant upgrades — critical for ~80% of African SME target tenants below VAT threshold + lacking formal employees. Without N7, mjadala would have designed for VAT-registered enterprise minority only. Engine behavior driven by profile: vat_registered=false skips output VAT; has_formal_employees=false skips PAYE; informal_sector=true skips tax-period close); **N8 Charter §1.3 strengthening** (Concept-Lead doctrine-critical — explicit "BOS DOES vs BOS DOES NOT" table §1 showing operational line: BOS computes/generates/tracks/reminds/aware; BOS does NOT file/submit/pay/represent/interpret; tax-advisor wording precision throughout — "Suggested: download Statement" not "Filing now"; advisor `recommended_command_draft` data-only per CN-5-010 N1; human as authorised actor never BOS-as-agent). **`tenant_tax_profile` precondition §3** before any tax computation — gate; `tax_treatment_ref` resolution chain §4 per-engine catalogue (Checkout/Procurement/HR/Promotion/Cash sets ref; Accounting resolves at journal; Reporting reads at Statement); Output VAT §5; Input VAT recoverable/non-recoverable §6; Withholding Tax PAYE+services §7; Reverse-Charge §8; **Tax-Period Close Choreography §9** (6-phase parallel to CN-5-104 — distinct from financial close because tax periods may differ; lighter signals — Cash/Procurement/HR only — 3 vs 4; tax computation gate Phase 3 replaces UI-07 trial-balance gate; UI-11 inviolability replaces UI-05); Tax Return Statement Documents §10 per CN-5-006 N3 (VAT Return, PAYE Remittance, Annual PAYE Certificate, statutory bundles); Multi-jurisdiction framework §11 (v1 implicit primary; v2 explicit); Mid-period rate change §12 deterministic via pack_version_ref at event timestamp; Tax-Period Failure Modes §13 (Q11 computation gate + Q13 backdated assessment via `accounting.tax.assessed.v1` with `references_closed_period` parallel PC8); Tax Exemptions §14 pack-only handling; **Tax-Advisor §15 with N8 wording precision throughout** (suggestions phrased as recommendations to download/review/submit-cash — never autonomous file action); UI Invariants §16 UI-01/05/06/07/08/**11**; **§17 Dual Worked Example** (Mama Amina informal sector — no VAT, no PAYE, only annual summary Document; **Mzee Hassan VAT-registered full lifecycle** — output VAT TZS 2,700, input VAT TZS 9,000 recoverable, net credit TZS 6,300, PAYE TZS 14,400, monthly VAT return + Annual PAYE certificate as Documents, settlement via Cash AR/AP to TRA, +Q13 backdated assessment from 2027 audit). **Three cosmetic amendments bundled in same commit (parallel CN-5-103 + 10 cosmetic edits AND CN-5-104 + CN-5-001 amendment pattern):** (1) **CN-5-001 +5 tax_period events** to manifest — `accounting.tax_period.opened.v1`, `accounting.tax_period.close.initiated.v1`, `accounting.tax_period.close.rejected.v1`, `accounting.tax_period.closed.v1`, `accounting.tax.assessed.v1` (all 3- or 4-segment per CN-5-103 G1/G3); §9 paragraph updated to describe tax-period close as parallel mechanism. (2) **CN-5-010 amendment** — 8 advisors → 9; new **§4.9 tax-advisor** subsection with full wiring (Phase 1; audience accountant+tax_officer+owner; data_scope tax_period_state + tax_return_summary + tax_authority_obligations + input_vat_recoverable + statutory_deductions_summary; `includes_decision_journal: true` opt-in per N5 of CN-5-010; scheduled pre-close + post-filing + event-triggered on close.closed + tax.assessed; pack thresholds 2 periods + 1 return filed; medium-tier default + high-tier ceiling because tax is regulatory); pack hooks `pack.advisor.tax.{min_tax_periods_with_activity, min_tax_returns_filed, cost_ceiling, schedule_default, tier_range, default_tier}`. (3) **CN-5-102 +UI-11** to living invariant catalog per Charter §11 — Closed Tax-Period Inviolability distinct from UI-05; scope tenant + jurisdiction; spans Accounting + Checkout/Procurement/HR/Cash/Promotion; enforcement runtime bus engine-policy + CI replay test; Phase 1 DC mapping pending. **CTR-043 opened (commit `b8bae19`)** Term 5 → Term 1 — tax pack content governance + per-jurisdiction tenant_tax_profile defaults. **CTR-029 expansion note added** (`tax:` pack subsection with rates, item-category recoverability, OECD reverse-charge, tax_calendar, return templates, tax_balance_epsilon, tenant_tax_profile_defaults, WHT pack rules, exemption catalogue). **CTR-014 expansion note added** (tax-advisor pack hooks + cost ceiling structure). **CTR-015 expansion note added** (tax UI elements — tenant_tax_profile surface, tax-period close workflow, tax-return Statement feed, tax-advisor suggestions, informal-sector dashboard variant; N8 wording precision throughout). **Term 5 progress: 15/15 docs — SCOPE COMPLETE.** Cross-cutting (6): CN-5-100/101/102/103/104/105. Engine-numbered (9): CN-5-009/001/002/003/004/005/006/007/010 (10 with tax-advisor in CN-5-010 v2). Universal-engines layer complete. Term 6 (Vertical Engines) + Terms 1/3/7 may proceed from this foundation. No new CTRs beyond CTR-043; no Charter conflict; Charter §1.3 boundary explicitly strengthened via N8.

## 2026-06-02 (continued)

**Term 5 — Universal Engines (14/~15)**
- **Term 5 CN-5-104 (Period-Close Choreography)** + **CN-5-001 cosmetic amendment** ratified and merged (commit `804ebf0`) — Term 5's fifth cross-cutting doc (967 lines, 17 sections). Cross-engine fan-in choreography (P5 per CN-5-100) of period-close: 4 core engines self-signal readiness → authorised human submits close.request → Accounting verifies UI-07 trial balance → atomic close emission with freeze window + UI-05 activation → Statement Documents issued post-close per CN-5-006 N3. **Eleven laws PC1–PC11:** Period-close is **choreography**, not orchestration — Accounting coordinates Documents but not engines (PC1); period is tenant-property + pack-driven calendar (PC2 + CTR-042); human-initiated trigger via Approval primitive (PC3); engines-with-financial-state declare readiness (PC4 — Promotion/Reporting/Checkout don't signal); Accounting coordinates UI-07 + emission (PC5); Statement Documents emit post-close per CN-5-006 N3 (PC6); **freeze window doctrine** — `accounting.period.close.initiated.v1` activates pre-emptive UI-05 between close.request acceptance and close.closed emission (PC7 — 24h default expiry); **forward-period correction-of-closed-period** per CN-5-103 E5/N1 — closed period truth never changes (PC8); period-close replayable (PC9); tenant-level close only v1 (PC10); **periods are independent — no cascade close** (PC11 — quarterly/annual reports = aggregations over closed monthlies, not separate cascade decisions). **4 core signalling engines** (Cash, Inventory, Procurement, HR) + pack-driven inclusion (omit per tenant configuration); **Promotion does NOT signal** (events flow individually; cost-share Obligations part of trial balance); Reporting (R1 read-side) and Checkout (event-driven settlement) likewise don't signal. **Six-phase choreography flow** (§5): Phase 1 engines complete books → Phase 2 close.request + freeze window activation → Phase 3 UI-07 verification → Phase 4 atomic close emission with summary payload + UI-05 activation → Phase 5 Statement Document issuance (P&L, Balance Sheet, Cash Flow, Statutory Summaries) → Phase 6 post-close projections + advisor suggestions + platform aggregation. **CN-5-001 cosmetic amendment** (bundled in same commit, parallel to CN-5-103 + 10 cosmetic edits pattern): added two new Accounting events to manifest — `accounting.period.close.initiated.v1` (close.request accepted, freeze window active per PC7); `accounting.period.close.rejected.v1` (close.request denied — missing signals OR UI-07 fail OR other policy violation). 4-segment sub-namespace per CN-5-103 G1/G3; G9 past-participle. §9 updated to elaborate close state machine (request → initiated → [closed | rejected]). Multi-currency single-close + FX bridged at close moment (§11; pack `forex.revaluation_frequency` drives revaluation cadence — every_close / monthly / quarterly / year_end_only per jurisdiction). Pre-close validation defence-in-depth (§10 — engine self-validates before ready signal; Accounting cross-validates UI-07 + UI-08 + UI-10 + UI-06 before close.closed emission; advisor pre-close optional helper). Year-end close = same six-phase mechanism + expanded Statement bundle per CTR-029 expansion (annual PAYE cert, VAT annual return, pension annual summary, statutory financial statements per jurisdiction); PC11 reaffirmed — year-end is its own close decision, not a cascade. **§16 Worked Example — Karakana ya Mzee Hassan October 2026** (full 6 phases from engines completing books through close.request, freeze window activation, UI-07 balanced verification, close.closed atomic emission, 3 Statement Documents issued, post-close advisor suggestions; plus **forward-correction scenario Nov 12** demonstrating PC8 — duplicate sale discovered post-close, posted to November via `accounting.adjustment.post.request` with `posting_period_ref: 2026-11` + `compensates_event_id: <Oct 28 original>`; October Statement STMT-PL-MZH-2026-10 remains hash-frozen unchanged at TZS 5.3M; November reflects correction; audit walks Nov 12 reversal → Mzee Hassan command → Oct 28 duplicate). **CTR-042 opened (commit `b8e7f7b`)** Term 5 → Term 1 — Period calendar governance + per-jurisdiction close approval gates + freeze window expiry overrides. **CTR-029 expansion note added** — `period_close:` pack subsection with period_calendar variants, required_signals + tenant overrides, period_close_statements per period kind (monthly/quarterly/annual + year-end statutory bundle), forex.revaluation_frequency (N4 simplified), freeze window expiry, approval thresholds. **CTR-014 expansion note added** — period-close advisors (accounting-advisor + bi-advisor pre/post-close hooks; operational helpers, not gates per Q11). **CTR-015 expansion note added** — period-close UI elements (close.request submission surface with checklist; freeze-window indicator; close-rejection display with `rejected_by_policy` + `suspect_accounts[]`; Statement Document feed post-close). **CTR-016 expansion note added** — platform billing aggregation subscribes `accounting.period.closed.v1` cross-tenant; reads `payload.summary` for billing roll-up; dual-trail audit (CN-4-006 §2). Term 5 progress: 14/~15 docs (5 cross-cutting + 9 engine-numbered). Inayofuata: CN-5-105 Tax-Aware Engines (final cross-cutting). No new CTRs beyond CTR-042; no Charter conflict.

## 2026-06-02

**Term 5 — Universal Engines (13/~15)**
- **Term 5 CN-5-010 (AI Advisors Wiring)** ratified and merged (commit `88bae69`) — Term 5's ninth engine-numbered doc (988 lines, 18 sections). Wires **8 concrete runtime AI Advisors** atop CN-4-022 framework (Foundation) using CN-4-013 Decision Journal as recording mechanism. **Wiring doc, not framework** — composes existing primitives without inventing new doctrine. **Ten laws A1–A10:** Advisor = 3-tuple `{audience, data_scope, model}` per D-002A + CN-4-022 §3 (A1); **Advisors NEVER emit business events (Law 3)** — suggestions land in CN-4-013 only; **strengthened by N1** — even `recommended_command_draft` carried on suggestion is **data, not submission** — humans build/submit command via normal bus path (A2 + N1, prevents "soft autonomy" via UI shortcut); read scope = CN-5-006 projections + CN-4-012 Documents, NEVER raw event store (A3); suggestion journalled as **`kernel.advisor.suggestion.recorded.v1`** (A4 — required fields incl. `advisor_id`, `advisor_version`, `audience`, `evidence_refs[]`, `confidence_score`, `confidence_tier` derived per N2, `recommended_command_draft?` data-only, `model_id/version`, `pack_version_ref`); human decision journalled as `kernel.advisor.decision.recorded.v1` (A5 — closes audit chain); **async fire-and-forget; bus does not wait** — for real-time advisors (checkout-advisor), if pack-defined latency budget exceeded, suggestion **dropped** and recorded as **`kernel.advisor.suggestion.dropped.v1`** (Foundation extension per Term 4 CN-4-013 living-catalog authority — NOT a CTR; settlement integrity > advisor opinion); A6 + N4 graceful degradation; per-tenant + per-advisor activation pack/tenant governed (A7 — conservative default: no advisor enabled until tenant opts in); tenant + site scope (A8 — v1 single-tenant; cross-tenant benchmarking deferred v2); **advisor versioning + freeze** (A9 — D-009 analog parallel CN-5-103 G6; replay determinism; "what would advisor have said?" legally defensible); **stateless within framework (per N5 clarification)** (A10 — no engine-internal learning state machine; reading Decision Journal as input projection allowed via pack hook `data_scope.includes_decision_journal: bool` default `false`; observation vs learning distinction). **8 advisor catalogue with Phase 0/Phase 1 split**: **Phase 0 (5 immediately activatable)** — `bi-advisor` (manager/accountant — Reporting cross-engine projections + Statements; scheduled+on-demand; medium tier); `inventory-advisor` (manager/stock_keeper — stock-on-hand, ROP, slow-movers, lot expiry; scheduled+event-triggered; low tier); `cash-advisor` (manager/cashier_supervisor — variance trends, AR/AP ageing, deposit pipeline, MM reconciliation; scheduled+event-triggered; low tier); `hr-advisor` (manager/hr_officer — leave balances, payroll history, employee loans, statutory; monthly+quarterly; low tier); `accounting-advisor` (accountant/bookkeeper — trial balance, period state, account ageing; scheduled pre-close + on-demand; medium tier). **Phase 1 (3 activatable on pack-defined thresholds per N3)** — `procurement-advisor` requires `min_supplier_count` (5) + `min_invoice_history_per_supplier` (3) over `min_invoice_history_window_days` (90); `promotion-advisor` requires `min_campaign_count_settled` (2) + `min_cost_share_cycles_completed` (1); `checkout-advisor` requires `real_time_mechanism_ratified` flag (false default; flips on Architect ratification). All thresholds pack-overridable. Activation rejected at command time if thresholds not met. Per-advisor wiring §§4.1–4.8 (compact YAML-style template per advisor: `advisor_id`, `audience`, `data_scope` with `tenant_scope` + `includes_decision_journal`, `trigger.{kind,schedule_ref,subscribed_events}`, `suggestion_shape.{template_ref,required_fields}`, `audience_routing.{ui,channels}`, `activation.{pack_default,tenant_opt_in,cost_ceiling_ref}`, `model.{tier_range,default_tier}`, `phase`). Decision Journal Integration §5 (suggestion → human → command audit chain; N1 note `recommended_command_draft` is data). Cross-Tenant Boundary §6 (v1 single-tenant; benchmarking v2 explicit). Activation Governance §7 (pack+tenant opt-in default; CTR-014 cost; CTR-041 registry). Suggestion Delivery §8 (UI per CTR-039 + channels per CTR-040 consent-gated as new Consent purpose `advisor_digest`). Evidence + Confidence Conventions §9 (mandatory `evidence_refs[]`; score 0.0-1.0 + derived tier per `pack.advisor.confidence_tier_cutoffs` default `{low<0.5, medium<0.8, high}`). Feedback Loop + Stateless §10 (A10 + N5 elaborated). Advisor Versioning §11 (A9 freeze mechanism). Failure Modes §12 (advisor unavailable; model timeout; data scope incomplete; consent absent; **real-time latency-exceeded → `kernel.advisor.suggestion.dropped.v1`**). UI Invariants §13 (UI-01 suggestion causation; UI-04 if real-time tender chain read — checkout-advisor only; UI-05/06/08 per applicable advisor data scope). Adapter/Subscription Map §14 (all `kind: projection` per A2 — advisors are projection consumers only). **Worked Example §15** — §15.1 Karakana inventory-advisor full 8-step lifecycle (trigger via `inventory.stock.deducted.v1` → projection scope query → compute → emit `suggestion.recorded` with `recommended_command_draft` → UI surface → human acts via Term 3 → submits real `procurement.requisition.create.request` → bus emits chain → causation walks backward through `decision.recorded` to original deduction; demonstrates A2/A3/A4/A5/A6/A8/A9 + UI-01 causation + Law 3 chain); §15.2 contrast — `checkout-advisor` latency exceeded; `kernel.advisor.suggestion.dropped.v1` recorded; settlement completes normally (demonstrates A6/N4 graceful degradation). §18 Open Items (D-DISC-001 explicit per Concept Lead pattern; Phase 1 activation conditions; cross-tenant benchmarking v2; learning/feedback Architect future; `kernel.advisor.suggestion.dropped.v1` Foundation extension pending Term 4 confirmation upon CN-5-010 ratification). **Three new CTRs referenced (commit `13c481a`):** CTR-039 (Term 5 → Term 3 — UI surface for advisor suggestions); CTR-040 (Term 5 → Term 7 — channel routing for advisor digest/alerts, consent-gated with `advisor_digest` Consent purpose); CTR-041 (Term 5 → Term 4 — `advisor_id` registry mechanism parallel CN-4-020). **CTR-014 expansion note added** (per-advisor cost ceilings + per-tenant model-tier elections + Phase 1 activation thresholds + confidence tier cutoffs + checkout-advisor latency budget). **CTR-015 expansion note added** (advisor-suggestion-feed primitive in AI Mode dashboard pattern per CN-5-010 A1 audience contract). **Foundation extension (NOT a CTR — Term 4 living-catalog authority):** `kernel.advisor.suggestion.dropped.v1` for N4 latency-exceeded path; Term 4 confirms via CN-4-013 living catalog upon CN-5-010 ratification. CN-5-103 G2 honoured throughout (all advisor events under `kernel.advisor.*` — Foundation namespace, no engine.advisor.* — intentional per A2/A4 boundary). No new CTRs filed by this doc; no Charter conflict.

## 2026-06-01

**Term 5 — Universal Engines (12/~15)**
- **Term 5 CN-5-103 (Universal Event Glossary)** ratified and merged (commit `6219ea6`) — Term 5's fourth cross-cutting doc (822 lines, 21 sections). The authoritative registry + governance for every command and event across Term 5 docs (~190 entries). Cross-cutting (not engine): no manifest, no own commands, no new events invented — only catalogues existing emissions, enforces naming doctrine, and locks governance for additions. **Nine laws G1–G9:** `<engine>.<noun>.<verb>.v<n>` format per Charter §8.1 (G1); **`kernel.*` namespace RESERVED for Kernel meta-events only — universal engines NEVER emit `kernel.*`** (G2 — enforcement via CTR-036 → DC-042 in CN-4-019 living catalog); adapter events `<adapter-id>.<noun>.<verb>.v<n>` with neutral placeholder resolved at deployment (G3); **`system.*` namespace NOT ALLOWED** — scheduler-emitted commands use engine namespace + `system:scheduler` actor (G4, parallel to CN-5-001 N1 / CN-5-005 N2 / CN-5-007); compensation verb-pair convention (`applied`/`reversed`, `issued`/`burned`, `received`/`refunded`, etc.) for UI-02 symmetry (G5); additive `.v1`-baseline + `.v2`-only-for-breaking versioning per CN-4-002 §3 (G6); event-name addition post-ratification requires CTR or amendment (G7); cross-engine references use producer's authoritative name, no aliasing (G8); past or past-participle tense for events (G9, present-progressive prohibited). Naming Conventions §3 (command `.request` unversioned imperative + event `.v<n>` versioned past; singular nouns; compound nouns with underscore; 4-segment sub-namespaces). **N4 4-state lifecycle**: `emitted` / `pending` / `future` / **`deprecated`** (added G6-deprecation cycle); governed transitions per G7. **Master Catalogue §§5–14** with **N3 UI invariants column** — Foundation citations (§5), cross-cutting (§6), Universal Checkout (§7), Accounting (§8), Cash (§9 — largest at ~30 emit), Inventory (§10), Procurement (§11), HR & Payroll (§12), Reporting (§13), Promotion (§14); each row: producer, consumers, doc-ref, state, **UI invariants touched**, notes. **§15 Cross-Engine Subscription Matrix** with **N5 UI-04 tender chain marker** — backward-walkable from `checkout.settled.v1`. **§16 Vertical Namespace Pre-Allocation** (Q6 (c) hybrid per CTR-038): `retail.*`, `restaurant.*`, `hotel.*`, `workshop.*`, `pharmacy.*`, `clinic.*` reserved for Term 6 (namespace ownership locked, specific event names deferred to Term 6 each-vertical doc); current placeholder references in Term 5 docs catalogued. **§17 Adapter Namespace Convention** (G3 — `<mm-adapter>`, `<bank-adapter>`, `<fx-adapter>`, `<channel-adapter>`, `<card-adapter>` registry; concrete adapter-id deployment placeholder); **Pack namespace pending CTR-037** Term 4 resolution. **§18 Naming Audit Findings E1–E10 status** — all 10 resolved: E1 `payroll.*` → `hr.payroll.*` (cosmetic, 9 occurrences); E2 `inventory.movement.transferred.v1` → `inventory.transfer.initiated.v1` (cosmetic); E3 `cash.position.snapshot.v1` → `cash.tender.received.v1` (cosmetic, CN-5-101 example); **E4 FX split**: `<fx-adapter>.fx.rate.recorded.v1` (adapter) + `procurement.invoice.fx_applied.v1` (procurement application); **E5/N1 `posting_period_ref` payload field** added to `accounting.journal.reversed.v1` (explicit forward-period anchor; no new event type for forward-period correction); E6 concrete vertical names → `<vertical>.*` angle-bracket; **E7/N2** `promotion.applied.v1` → **`promotion.rule.applied.v1`** (Charter §8.1 strict format compliance — engine.verb.v1 was violation; 6 occurrences in CN-5-007); also `reporting.metrics.monthly.v1` → `reporting.metrics.published.v1` (G9 past-tense); other E7 candidates accepted as well-formed; E8 vertical-placeholder events per Q6/CTR-038; E9 adapter placeholders clean; E10 `kernel.*` references clean (only legitimate Foundation citations). **§19 Versioning & Deprecation Policy** (G6 elaboration, deprecation lifecycle, pack-driven compatibility window). **§20 Governance — Adding New Events Post-Ratification** (G7 elaboration, CTR-or-amendment path, engine-owner responsibilities). **§21 Worked Example — Karakana Mwanza Sale** (~13-event chronological trace from `<vertical>.bill.ready.v1` through `checkout.receipt.issued.v1` demonstrating UI-01 causation chain + UI-04 tender chain + UI-06 currency + UI-07 trial balance + UI-03 source-ref + UI-09 site_id + UI-08 obligation bounds + UI-10 cost-share + G2 no-kernel + G3 adapter + G4 no-system + G5 compensation + G7 governance + G8 producer-authoritative + G9 past-tense). **Three new CTRs opened (commit `69f365d`):** CTR-036 (Term 5 → Term 4 — `kernel.*` reservation as DC-042); CTR-037 (Term 5 → Term 4 — `pack.*` namespace clarification); CTR-038 (Term 5 → Term 6 — vertical namespace pre-allocation). **10 cosmetic alignment edits bundled** in same commit: CN-5-001 (4 edits — payroll renames, inventory transfer fix, posting_period_ref addition); CN-5-002 (2 edits — payroll sweep, workshop → `<vertical>` placeholder); CN-5-007 (1 edit — promotion.rule.applied 6 occurrences); CN-5-101 (2 edits — cash.tender.received example, reporting.metrics.published); CN-5-102 (1 edit — FX bridge split). No new CTR expansion notes (governance doc, no engine impact on CTR-021/024/029/030). No Charter conflict.

## 2026-05-31

**Term 5 — Universal Engines (11/~15)**
- **Term 5 CN-5-007 (Promotion Engine)** ratified and merged (commit `75275dd`) — Term 5's eighth engine (912 lines, 20 sections). The commerce-promotion truth layer: discounts, vouchers, loyalty, cost-share, outreach — composing Foundation primitives, **no new primitives invented**. **Seven laws PR1–PR7:** Promotion populates rules / Checkout applies them (PR1 — single-source-of-truth at settlement per CN-5-009 K2); **vouchers ARE Documents** (PR2 — hash-verified, immutable, template-versioned, pack-frozen per CN-4-012, same doctrine as CN-5-006 N3 statements); **loyalty balances ARE Obligations** (PR3 — `kind: loyalty_credit` via CN-4-011 extension, unifies with AR/AP/loans/delivery/advance/layby/cost-share/credit-tender across all Term 5 engines; UI-08 bounds); **4-source cost-share splits to total_discount** (PR4 — `bos_share + agent_share + supplier_share + tenant_share = total_discount`, first three create `cost_share_receivable` Obligations settled via Cash AR flow, `tenant_share` absorbed; UI-10); **consent-gated outreach hard-block at command time** (PR5 — D-008 + CTR-021; commands without valid Consent record are rejected, not soft-warned); pack-driven tax treatment (PR6 — `tax_treatment_ref` per CN-4-021); pack-driven calculation precedence (PR7 — default `[line, bundle, bill, voucher, loyalty]`, region/tenant reorderable). Multi-scope manifest (tenant default + site overrides per CN-5-101 §6). **N1 consolidated handler `apply_promotions_at_settlement`** — single entry point with internal routing on `promotion.kind`; atomic batch emission of `promotion.applied.v1` events (UI-01 causation) + cost-share lines (UI-10); new kinds = new router branch, not new command (parallel to CN-5-004 N1 / CN-5-005 N1). **N2 namespace audit** — all 13 emit events under `promotion.*` (incl. `promotion.loyalty.*`, `promotion.outreach.*`); no `kernel.*` leakage; only inbound subscription is `cash.collection.received.v1` (correct cross-engine read). **N3 voucher restoration pack-driven** — `promotion.voucher.restoration.requested.v1` on refund; pack rule resolves to `restored | burned | partial_credit` per `pack.promotion.voucher_restoration_policy`; UI-02 compensation symmetry. **N4 obligation lifecycle clarified** — three sources (bos/agent/supplier) create `cost_share_receivable` Obligations; tenant_share absorbed; Cash Engine settles via AR flow (no new payment rail). **N5 targeting via read-only projections** — 4 orthogonal dimensions (customer/item/site/time-window) resolve at command time via Party/Item/Site registry/wall-clock reads (CN-4-005 read-side); never inter-engine commands. **10 promotion kinds catalogue**: line_discount / bill_discount / bogo / bundle (atomic per Q10) / voucher / loyalty_redemption / time_based / customer_specific / campaign_funded / **manual_override** (Q3 — cashier ad-hoc requires `reason_ref` + elevated principal per CN-4-007 + Decision Journal entry per CN-4-013). **7 pack hooks**: stacking_policy (Q2 — best_only default); calculation_precedence (PR7); voucher_restoration_policy (N3); voucher_namespace (Q5 — tenant default, campaign opt-in); voucher_breakage_account (Q9); loyalty.earn_basis (Q4 — bill_total default); cost_share_cadence (Q6 — monthly_net default). Voucher lifecycle as Documents (issued → redeemed → restored | burned → expired per terminal-fold; pack-driven uniqueness namespace; expired → voucher_breakage_account writeback). Loyalty model as Obligation primitive with earn/redeem events + refund compensation. Cost-share split with vertical-aware GL routing (per CTR-030 expansion). Outreach hard-block (`promotion.outreach.blocked.v1` on missing/revoked consent). Tax treatment per voucher/loyalty redemption with pack-defined treatment refs. Refund compensation with partial-proportional + bundle-atomic semantics (Q7 + Q10). Cross-engine subscriptions (Checkout / Accounting / Cash / Reporting / Channel-adapters). UI-01/02/04/08/10 enforcement. **Mama Amina October Soda Promotion** 8-phase worked example (campaign definition → voucher issuance → customer redemption → cost-share journaling → Cash AR settlement → bottle refund + voucher partial restoration → expiry + breakage writeback → period-close reporting). **§19 Open Items** — D-DISC-001 (tenant-customer promotion UX → Term 3 per Concept Lead) + D-DISC-002 (POS self-service expansion → Term 5/6 per Concept Lead) explicit; CTR-021/024/029/030 expansion items; manual override governance + loyalty point tiers + voucher transferability + Phase 1 DC additions for UI-10 and UI-08 deferred. **CTR-021 expansion note added** (consent hard-block operationalised at command time; outreach send/blocked/failed events; Term 7 adapter contract). **CTR-024 expansion note added** (site_id required for site-scope promotions + agent_share routing via vertical's agent assignment per site). **CTR-029 expansion note added** (7 promotion pack hooks per jurisdiction + voucher/loyalty Document template_refs). **CTR-030 expansion note added** (vertical promotion-trigger event contracts + cost-share GL account mapping per vertical per jurisdiction). No new CTRs; no Charter conflict.

## 2026-05-30

**Term 5 — Universal Engines (10/~15)**
- **Term 5 CN-5-006 (Reporting & BI Engine)** ratified and merged (commit `f785a10`) — Term 5's seventh engine (796 lines, 19 sections). The read-side / analytics layer that consumes events from every other engine and produces projections, KPIs, snapshots, formal Documents, statutory summaries, and exports. **Seven laws R1–R7:** read-side only (NO business truth emission — only meta-events about reports/snapshots/Documents/exports/alerts; never journals, never cash, never stock, never direct notifications); all projections derive from event store (CN-4-010 inheritance; rebuildable; deterministic); pack-driven templates (Law 5); multi-scope (tenant default + site operational + platform reserved for Term 1 billing aggregation per CN-5-101 §7); single ledger + dual-standard views via pack mapping (no parallel ledger — consistent with CN-5-001 Q5); **R6 (N3 — doctrine-critical) Snapshots ≠ Statements** — a CN-4-018 snapshot is a non-truth performance cache (discardable, rebuildable); a CN-4-012 Document statement (period-end P&L, balance sheet, statutory summary) is a formal hashed/numbered/template-versioned/pack-frozen artefact verifiable years later — distinct primitives serving distinct purposes; BI Advisor integration is read-only (Law 3; CN-5-010 wires the Advisor; CN-5-006 provides read surface). Multi-scope manifest with **N3 `reporting.statement.issue.request` + `reporting.statement.issued.v1`** (formal Documents via CN-4-012); meta-only emits restricted to reports/snapshots/Documents/exports/alerts; massive subscription fan-in across Accounting/Cash/Inventory/Procurement/HR/Checkout (all `kind: projection` or compensation). **N1 Live Reports vs Period-Locked Documents** distinction explicit (§5 side-by-side table) — live reports are transient read-time evaluations against current projections; period-locked Statements are immutable Documents. Financial reports (P&L, balance sheet, cash flow) via pack templates from CN-5-001 §6 — live view + Statement issuance at period close. Operational reports (sales / AR-AP ageing / inventory / procurement / workforce / cash). Statutory summary reports issued as Documents per N3. KPI base library + custom definitions; **N5 alert delivery boundary** — Reporting emits `reporting.alert.triggered.v1` meta-event only; Term 3 dashboard surface + Term 7 channel adapters handle actual delivery (Reporting never sends directly). Snapshots integration with R6 distinction explicit in §10. Multi-standard via pack mapping rules. Export categories + filing-format generation deferred (CTR-035 → Open Item per Charter §1.3 — BOS does not file). **N4 vertical-dimensional reports** future via CTR-030 (v1 catalogue empty; grows as Term 6 defines verticals). UI-01/05/06/07 enforcement. Karakana year-end example (live dashboard + year-end P&L Statement Document + statutory summary Statements + filing export + BI Advisor suggestion + D-009 pack-upgrade freeze demonstration). **CTR-029 expansion note added** (statutory_summary_reports + reporting_snapshots + export_templates + alternative_standard_views + KPI base library + alert delivery routing). **CTR-030 expansion note added** (vertical dimensional report event contracts). **CTR-035 → Open Item** (filing-format adapter; Charter §1.3 deferral). No new CTRs; no Charter conflict.

**Term 5 — Universal Engines (9/~15)**
- **Term 5 CN-5-005 (HR & Payroll Engine)** ratified and merged (commit `44dcd5c`) — Term 5's sixth engine (870 lines, 19 sections). Employee management + payroll computation for any business, with statutory rules pack-driven and Charter §1.3 boundary explicit (BOS computes; HR officer/chartered accountant files). **Seven laws H1–H7:** employee = Party primitive + HR projection (parallel to supplier in CN-5-004 §4); assignments and compensation are versioned (full employment history); payroll is a per-period event (D-009 freeze under the pack/standard active at run time); statutory deductions are 100% pack-driven (PAYE bands, pension, NHIF, training levy per jurisdiction — Law 5 + D-004); **Charter §1.3 boundary** (BOS does not file with tax authorities); loans via Obligation primitive (kind: `employee_loan`, unifies with AR/AP/advance/layby/credit-tender; UI-08 bounds); multi-engine choreography only (no orchestrator). Multi-scope manifest (tenant default for employee identity; site overrides for attendance, site-anchored assignments, site-initiated loan requests). **N1 namespace fix**: deduction event renamed to **`hr.payroll.deduction.applied.v1`** (Charter §8.1 — engine_id `hr`); CN-5-002 §H cosmetic alignment deferred to CN-5-103 Universal Event Glossary pass. **N2 namespace fix**: periodic accrual via **`hr.accrual.run.request`** command from `system:scheduler` actor (CN-4-007); no `system.scheduler.*` event namespace (parallel to CN-5-001 N1 fix for depreciation). **N3 post-gate disbursement**: Cash subscribes **`hr.payroll.approved.v1`** (after authorisation), NOT `hr.payroll.computed.v1`; prevents disbursement of unapproved payroll. **N4 atomic deduction-with-payment**: `hr.payroll.deduction.applied.v1` emitted **atomically with `hr.payroll.paid.v1`** (CN-4-004 §2 multi-event emission); Obligation outstanding reduces only after payment confirmation, preventing silent over-reduction on disbursement failure. Employee management lifecycle (registered → active → on_leave → terminated/deactivated); versioned roles + site-anchored assignments + additional_sites for multi-site staff; compensation_kind variants (salary/hourly/commission/piece_rate/hybrid). Pack-driven leave (types, accrual rates, balance lifecycle, encashment-on-termination). Site-scope attendance + pack-driven overtime multipliers. Payroll computation 7-step flow (inputs → deductions → net → emit → authorise gate → per-employee deduction events → disbursement). Pack statutory schema with PAYE bands, pension, NHIF, levies, dependent allowances. Employee loan choreography recap (HR-side write-off path added for termination/default). Commission/piece/tip via vertical Pattern B subscriptions. Termination workflow with outstanding loan auto-deduct from final pay (or escalate to write-off authorisation). Karakana wafanyikazi year example (onboarding → August loan → monthly payroll → resignation April → year-end statutory reports). **CTR-029 expansion note added** (hr_leave + hr_payroll period + hr_statutory_deductions + termination_computation + overtime multipliers). **CTR-030 expansion note added** (vertical commission/piece-rate/tip event contracts). **CTR-034 → Open Item** (Term 7 filing-format adapter; Charter §1.3 deferral). No new CTRs; no Charter conflict.

**Term 5 — Universal Engines (8/~15)**
- **Term 5 CN-5-004 (Procurement Engine)** ratified and merged (commit `512d54e`) — Term 5's fifth engine (868 lines, 20 sections). The buying lifecycle for any business: Requisition → PO → GRN → Invoice → Payment-reference, with optional Phase 0 (RFQ/Quote). **Seven laws P1–P7:** lifecycle = truth (Workflow primitive, append-only); document at every binding transition (CN-4-012 with hash/number/freeze); three-way match before payment authorisation (pack-driven variance per quantity/price/item, auto-pass/flag/hard-block bands); **AP via Obligation primitive** (kind: `accounts_payable`, UI-08 bounds — unifies with AR/delivery/layby/advance/credit-tender across engines); compliance via pack (approvals, tax, supplier types, credit-term defaults); returns via Credit Note compensating document (UI-02 symmetry across Inventory + Accounting + Cash); **one lifecycle, two effects** — direct-expense and inventory-tracked procurement share the same workflow, differing only by `inventory_effect` flag at GRN (P7 unification). Multi-scope manifest (site default; tenant overrides for supplier records, credit terms, payment scheduling, high-value approvals). **N1 consolidated handler**: single `handle_cash_payment` subscription to `cash.payment.disbursed.v1` with internal routing on `obligation.kind` (payable → mark_invoice_paid; credit_refund → mark_credit_note_received) — manifest stays focused on event types, routing stays inside engine business logic. **N2 issued-vs-recorded document distinction**: BOS issues RFQ + PO + GRN + Payment Voucher + Credit Note (BOS-owned documents per CN-4-012); BOS records Quote + supplier Invoice (supplier-owned content, BOS records reference + metadata). **N3 approval timing**: RFQ is a market scan (no commitment, no pre-RFQ approval by default); approval happens at requisition (post-quote when business is about to commit). Pack may opt in to `procurement.rfq.approve.request` for jurisdictions that require pre-RFQ authorisation (e.g., government procurement). **N4 CN-5-002 §7 boundary**: formal procurement-with-supplier (has PO + invoice) → CN-5-004; petty/informal cash-and-carry (no PO) → CN-5-002 §7 `cash.expense.record.request`. Three-way match auto-pass/flag/hard-block per pack-defined variance thresholds; tenant override within ±50%. AP Obligation lifecycle (creation at invoice recording; partial settlement; UI-08 bounds; due_date derived from supplier credit terms; early-payment discounts applied at Cash settlement per pack rule). Approval primitive for single-step; Workflow primitive for multi-step (board-level). Partial flows (PO ↔ multi-GRN ↔ multi-Invoice ↔ multi-Payment) via state machine. Returns: pre-invoice (handled at GRN discrepancy) or post-invoice (Credit Note); both UI-02 symmetric. Multi-currency: AP in supplier currency; FX at-record vs at-payment per pack rule. Karakana aluminium full-lifecycle example (requisition + manager approval + PO + partial GRN 30+20 + invoice + 3-way match + 2/10 Net 30 early-payment discount + completion + defective-return variant). **CTR-029 expansion note added** (procurement_approvals.thresholds + three_way_match.variance_thresholds + supplier_credit_term_defaults + procurement_forex_recognition). **CTR-030 expansion note added** (vertical `purchase_need.recorded.v1` event contracts). **CTR-033 → Open Item** (Term 7 supplier verification adapter; defer to when packs require external verification). No new CTRs; no Charter conflict.

**Term 5 — Universal Engines (7/~15)**
- **Term 5 CN-5-002 (Cash Management Engine)** ratified and merged (commit `bc1ad39`) — Term 5's fourth engine (and largest doc to date: 1,002 lines, 22 sections). Scope expanded from "drawer + sale" to **money-position management** across 8 till kinds (physical cash drawer / mobile money wallet / card-merchant balance / petty cash float / site safe / main safe / multi-currency drawer / tip pass-through). **Seven laws C1–C7:** till = money-position abstraction (one engine serves all till kinds via `kind` attribute); site-scope default + tenant overrides for consolidations/main_safe/inter-site/equity/deposits (CN-5-101 §6 pattern); every cash event causation-chains to one of five closed authorised origins (UI-04); currency invariant per till (UI-06); session lifecycle via Workflow primitive (CN-4-011); **all debts/credits/commitments via Obligation primitive** with appropriate `kind` (AR receivable, AP payable, employee_loan, layby, advance_received, delivery — unifies cross-engine choreography on one primitive); reconciliation variance per pack threshold (auto-adjust band / anomaly band / hard-block band). Multi-scope manifest (17 commands, 25+ emits, per-vertical Pattern B subscriptions for layby/advance fulfillment). **Generic operating-expense pathway** (`cash.expense.record.request`) with 14 pack-defined categories (rent, utilities, fuel, transport, repairs, supplies, communications, staff_welfare, professional_fees, financial_charges, **insurance, marketing_advertising, bank_charges**, other — N1). **AR/AP via Obligation** (6 kinds; settlement command shapes; UI-08 bounds throughout). **Employee loan choreography** (HR creates Obligation → Cash disburses → Payroll deducts each period → Obligation settles incrementally → Accounting auto-journals each step). **Inter-till + inter-site transfers** + drops; **bank deposit handoff** to Term 7 via CTR-031 (analogous to CTR-006 payment adapter). Refund cash side as compensation (CN-5-009 pairing). **Owner equity flows** as Cash-owned commands (dual-actor audit) because cash impact is operational truth. **Multi-currency tills** opt-in per till + FX coordination with CN-5-001 §11. **Tips:** cash via `tip_passthrough_till`; digital tips via Checkout payload metadata (N2). **Layby vs advance** semantic distinction (advance = single-direction owe-goods; layby = phased accumulation → release on full; N4) + **cross-engine coupling with Inventory reservations** (CN-5-003 §9, N5). UI-04/UI-06 enforcement structural at every emission. Mama Amina full-day example (8 tills, mixed tenders, AR/AP/loan/advance/refund/reconciliation/deposit/payroll integration). **v2 `bank_account` till kind** flagged as Open Item (N3). **CTR-031 NEW** filed (Term 7 banking adapter) — distinct from CTR-006 (payment-at-tender) because deposit destination + settlement times + statement reconciliation differ. **CTR-029 expansion note added** (operating_expense_categories + cash_reconciliation thresholds + multi_currency_policy + tip_tax_routing). **CTR-030 expansion note added** (layby/advance fulfillment vertical event contracts). **CTR-032 → Open Item** (equity authorisation; not separate CTR). No Charter conflict.

**Term 5 — Universal Engines (6/~15)**
- **Term 5 CN-5-003 (Inventory Engine)** ratified and merged (commit `5e43c4d`). Universal stock-truth engine serving café, workshop, pharmacy, retail with one manifest. **Six laws I1–I6:** site-scoped stock truth (`(tenant, site, item, lot?)` tuple — complements CN-5-001 A4 tenant-scope Accounting); every movement typed at source (UI-03/DC-039); negative stock blocked by default; costing pack-driven + per-item overridable (Law 5); **recipes are Inventory engine state, not Item primitive** (avoids Foundation extension — Q2 Option β); site_id from registry (UI-09/DC-041). **Multi-scope manifest** (site default + tenant overrides for items/recipes/transfers/expiry — CN-5-101 §6 pattern). **Recipe expansion in two coexisting patterns:** Pattern A (Inventory auto-expands from versioned recipe in its projection — café latte, pharmacy kit, retail bundle); Pattern B (vertical emits `<vertical>.ingredient.consumed.v1` — workshop parametric cuts, restaurant substitutions, hotel conditional). **`expansion_mode: auto | vertical_managed` per-line signaling** (N1) on the bill payload disambiguates — `vertical_managed` skips Pattern A deduction and waits for Pattern B. **Recipe versioning** with D-009-analog freeze (recipe-version active at sale moment applies on replay). Eight movement types (receive/deduct/transfer/adjust/reserve/release/write-off/offcut) with umbrella `inventory.adjustment.recorded.v1` reaching Accounting. **Lots & expiry opt-in** per item attribute; scheduler-driven expiry runs (`accounting.depreciation.run`-style pattern: `inventory.lot.expiry_run.request` by `system:scheduler`). **Costing methods** pack-declared with **standard-dependent allowed_methods** — IFRS packs `[fifo, weighted_average]` (IAS 2 2003 prohibits LIFO), US GAAP packs may include LIFO. **N2:** lot-tracked + weighted-average **decouples** physical pick (FIFO-by-expiry) from cost recording (pool weighted-average). **Reservations** via Workflow primitive + **hard-hold semantics** (deduction targeting reserved qty rejected unless naming the reservation). **Offcuts as sub-items** (lazy registration on first cut — full lifecycle vocabulary). **Substitutions** captured via `substituted_for + reason` metadata (Pattern B only). **Multi-site transfers** 3-phase (initiate→in-transit→receive); **N4** in-transit semantics: stock shown as `reserved` at from-site, NOT a separate "in-transit pool"; analytical in-transit view = query over open transfers. **Backorder opt-in** (default blocked I3) — **N3** backorder wraps the **Obligation primitive** (kind: `delivery`), coherent with CN-5-009 credit pattern; UI-08 bounds apply. **N5** cycle counts: `count.request` payload supports optional scope (full / item-list / category-list / lot-list). Four-vertical example (café latte / workshop window with offcuts and reservation / pharmacy dose with FIFO-by-expiry / retail tomato atomic). **CTR-027** used for UI-09 enforcement. **CTR-029 expansion note added** — pack section gains `inventory_costing` subsection (per-jurisdiction approval covers it). **CTR-030 expansion note added** — Pattern B vertical event contracts (`restaurant.ingredient.consumed.v1`, `hotel.amenity.consumed.v1`, `workshop.cut.executed.v1`, `workshop.parametric.consumed.v1`) and `expansion_mode` per-line attribute. No new CTRs (Q2 Option β avoided Foundation extension). No Charter conflict.

**Term 5 — Universal Engines (5/~15) — first per-engine doc**
- **Term 5 CN-5-001 (Accounting Engine)** ratified and merged (commit `37c67ce`). Term 5's first per-engine doc and the auto-journaling backbone. **Five laws A1–A5:** standards in packs (Law 5 + D-009 freeze — IFRS/TFRS/GAAP rules live in compliance packs, never in code); auto-journal completeness (every monetary business event → exactly one journal — UI-07 trial-balance root); balanced at emission (bus rejects unbalanced posts; CN-4-011 Ledger fold); tenant scope with site as analytical dimension only (D-007 #13 — Ledger has no close-period; period close is Accounting's; CN-5-101 tenant scope); corrections via compensating journals + closed-period inviolability (UI-05 + UI-02; no re-open). **Manifest:** 7 commands (chart configure/account add/account deactivate/journal post system-only/adjustment post authorised/period close + `accounting.opening_balances.set.request`), 8 emits (incl. `accounting.depreciation.posted.v1` per N1 namespace fix — no `kernel.depreciation.*`), 13 auto-journal subscriptions covering revenue/refund/cash/procurement/inventory/payroll/depreciation/FX/promotion. **Chart of accounts model** — pack base + tenant sub-account customisation within standard limits; 5 account types; analytical dimensions per account (site_id, cost_center, …). **Pack accounting-section schema (CN-4-015 extension via CTR-029)** — 6 subsections: chart_of_accounts_base, recognition_rules (accrual/cash-basis/hybrid per pack, tenant election where allowed), reporting_templates, depreciation, forex, **`period_calendar`** (fiscal_year_start, reporting_frequencies, close_lag_days — N2). **Opening balances (N3)** — dedicated one-time `accounting.opening_balances.set.request` command (dual-actor audit; pack validates), NOT via adjustment.post which is current-period-only. **Corrections (§9, N4)** — explicit distinction: `causation_id` = forward trigger (what invoked the handler, UI-01); `compensates_event_id` = backward reversal link (what is being undone, UI-02). Charter §1.3 boundary made explicit (§12) — management truth, audit-ready data; **no statutory filing** (chartered accountant uses BOS data; BOS does not file/certify/submit). FX revaluation pattern via `<fx-adapter>.fx.rate.recorded.v1` (Term 7 adapter; no `kernel.fx.*`). Mama Amina FY2026-07→2027-06 example (onboarding opening balances → operational year → monthly closes → November correction post-close → year-end → statutory filing OUTSIDE BOS). **CTR-029** (→ Term 1, accounting-standard pack-section content/governance) and **CTR-030** (→ Term 6, vertical payload journal mapping) stay OPEN; CTR-027 unchanged. No Charter conflict.

**Term 5 — Universal Engines (4/~15) — first concrete engine, revenue hinge**
- **Term 5 CN-5-009 (Universal Checkout/Tender Engine)** ratified and merged (commit `979b2f8`) — Term 5's first concrete engine doc, the **revenue hinge** that every downstream universal engine depends on. Implements **D-001** atop CN-4-021 value shapes (CN-5-009 is the engine; Foundation provides the shapes). **Five laws K1–K5:** vertical-blind (DC-022 enforces non-branching on `source_tag`); authoritative inputs only (recomputes line totals; trust no upstream cache); tax at settlement, pack-frozen (D-009 + CN-4-015); tender lifecycle = events (not mutable state); settlement **explicitly triggered** (cashier-submitted `checkout.settle.request`, never auto) and **atomic** (CN-4-004 §2). **Manifest** with `scope_policy: site` default, per-vertical `bill.ready` subscriptions, per-adapter outcome subscriptions, eight emitted event types. **Settlement workflow Phase 1–4:** Ingest → Collect Tenders (sync cash immediate; async via Term 7 adapters with `callback_correlation_id`) → Adapter Round-Trip → Finalise (atomic `settled` + `receipt.issued` + `obligation.created` for credit). **Refund = command + compensation event** (`compensates_event_id` non-null; UI-02 symmetry; partial-refund per-portion symmetry → Open Item for Architect). Receipt issuance via CN-4-012 (template + numbering + hash; DEGRADED block-reserved). Credit tender → Obligation primitive (CN-4-011, UI-08 bounds). **Currency check explicit at `checkout.tender.add.request`** (M1, UI-06). `source_tag` non-branching restated with DC-022 reference (M4). DEGRADED behaviour brief (M3). Karakana settlement example incl. partial-refund propagation. **CTR-003 → CLOSED** (Term 5 accepts Checkout-as-engine, Foundation delivered). **CTR-001** advanced (Term 5 demonstrates downstream usability of CN-4-021 shapes; awaits Term 6 acceptance). **CTR-005 → NEGOTIATING** (Term 5 commits always-on; awaits Term 1 + Term 6). **CTR-006 → NEGOTIATING** (proposed `callback_correlation_id` contract published for Term 7). **CTR-028** already OPEN (tender method registry). No Charter conflict.

**Term 5 — Universal Engines (3/~15)**
- **Term 5 CN-5-102 (Engine Invariants)** ratified and merged (commit `5e2ee08`). Term 5's cross-engine state-shape doctrine — distinct from subscription patterns (CN-5-100, behavior) and scope policy (CN-5-101, where state lives). **Boundaries B1–B3:** cross-engine only; state-shape not behavior; inheritance not re-statement. **Initial catalog of 10 Universal-engine Invariants (UI-01..10)** each with statement/scope/spans/enforcement/DC-mapping/violation/reasoning: UI-01 causation_id integrity (command-origin = null; subscription-derivative = source-id); UI-02 compensation symmetry; UI-03 inventory movement carries typed source ref; UI-04 cash tender causation chain; UI-05 closed-period inviolability **including compensating events** (corrections post current period with causation back); UI-06 tenant functional-currency invariant (tenant property, distinct from per-settlement DC-037); UI-07 trial balance per tenant; UI-08 no engine bypasses Obligation primitive's bounds via raw mutation; UI-09 site_id in tenant site registry; UI-10 promotion cost-share sums. **Three-layer enforcement** (runtime / CI integration / CI schema) with multi-layer composition. **Three-pattern violation** (hard-fail / post-hoc anomaly / mode escalation) — multi-pattern per invariant per stage. **DC ramp-up — Phase 0 via CTR-025:** DC-038 (UI-01), DC-039 (UI-03), DC-040 (UI-06 extending DC-037 to tenant scope), DC-041 (UI-09); Phase 1 (UI-02/04/05/07/08/10) awaits per-engine docs and per-engine CTRs. **Living catalog discipline** mirrors CN-4-019 §5. Mama Amina period-close example (engine-contract tone). **CTR-025/026/027** all OPEN. **MASTER-GLOSSARY** gains "Universal-engine Invariant (UI-NN)" entry. No Charter conflict.

**Term 5 — Universal Engines (2/~15)**
- **Term 5 CN-5-101 (Scope Policy Application)** ratified and merged (commit `5f04bff`). Term 5's policy applying CN-4-005 §1.1 (post-CTR-023 amendment) to universal engines. **Three scope levels** — **`site`** (intra-tenant location partition; `site_id` in payload, NOT envelope), **`tenant`** (isolation boundary, "business-wide"), **`platform`** (parallel, rare, dual-audited). Tags-not-levels for region/zone grouping. **Five laws:** no cross-tenant ever (L1); site is payload not envelope (L2); scope declared per operation, not per engine (L3, via the CN-4-005 §1.1 mechanism); site dispatch per `(tenant_id, site_id)` tuple (L4); platform on a universal engine is `requires`-rare with `platform_authorised: true` + dual-trail audit + Term 1 governance (L5). **Runtime dispatch algorithm** with explicit **rejection on missing `site_id`** (no silent fall-back to tenant — preserves isolation). **Multi-scope engine pattern** (canonical Cash manifest sketch: `site` default with `tenant` overrides for consolidated position and cross-site transfer). **Reporting → Term 1 chain (Q3 Chaguo C)** — Reporting emits per-tenant; Term 1 aggregates at platform scope under CTR-016 governance; universal engines stay off platform scope by default. **Neutrality enforced** — payload field is `site_id`, invariant across verticals (never branch/outlet/property/etc., D-004). Mama Amina two-branches example with engine-contract tone (incl. doctrine-failure-caught scenario). **CTR footprint:** CTR-023 was already CLOSED; CTR-024 (→ Term 6, `site_id` payload discipline) stays OPEN; CTR-016 (billing aggregation folded in) stays OPEN. No new CTRs; no Charter conflict.

**Foundation living-document amendment (Charter §11)**
- **CN-4-005 (Engine Contract Model)** amended additively (commit `76dd67d`) per **CTR-023** from Term 5. `scope_policy` becomes the engine **default**; `subscribes_to` and `commands` may each declare an optional `scope_ref` override at one of three level names — **`site` / `tenant` / `platform`** — replacing the informal "branch/business" wording. New **§1.1 "Scope Levels and Per-Operation Override"** added (levels table, default+override mechanism, additive guarantee, provenance from CN-5-100/CN-5-101). **`site` is a payload concept** (intra-tenant location partition); CN-4-002 envelope unchanged. Manifests with only `scope_policy` remain fully valid (no breaking change); engine isolation (Law 2) unaffected. Authored by Term 4 (D-006) on a Concept-Lead-authorised re-activation. **CTR-023 → CLOSED.**

## 2026-05-28

**Term 5 — Universal Engines BEGINS**
- **Term 5 CN-5-100 (Subscription Wiring Patterns)** ratified and merged (commit `8f23d9b`) — Term 5's first concept doc, the cross-engine wiring contract every other Term 5 doc builds on. **Five named patterns** (P1 fan-out, P2 projection-only, P3 command-emitting, P4 compensation, P5 choreography-sequence). **Five laws** (events-only; declarative subscriptions in the manifest; choreography-only/no orchestrator; subscribers emit events only through the bus; idempotency is a framework property via processed_position). **Subscription declaration** extends CN-4-005's `subscribes_to` with per-entry `{event_type, version, handler, kind, scope_ref}`. **Replay semantics (Q2):** `kind ∈ {projection, command_emitting, compensation}` — command_emitting/compensation handlers are **live-only, not re-run on replay** (their derivative events are first-class store events replayed as themselves; grounded in CN-4-009 "replay never emits events"). **Failure/freshness (Q3):** per-subscriber stale-but-available + at-least-once redelivery + resume from processed_position + CN-4-017 lag escalation; a failing subscriber lags but never invalidates truth. **No private events (Q4)** — manifest `emits` marks cross-engine contract events (additive-only forever, CN-4-002). **No ordering between fan-out subscribers** (emergent choreography); **causation_id linkage** makes the choreography traceable end-to-end without an orchestrator (audit substitute, CN-4-008). Fan-in = CN-4-005 status-event + local-projection (not a new pattern) → CN-5-104. Mama Amina retail-sale example (engine-contract tone, D-004 #4). No new CTRs; no Charter conflict.

## 2026-05-27

**Merged to `main` — Term 4 Phase 3 (13/13) — FOUNDATION CONCEPT PHASE COMPLETE (23/23 CN docs + 11 brief sections)**
- **Term 4 CN-4-018 (Snapshot Storage)** ratified and merged (commit `08c6e04`) — **the final Foundation doc**. Snapshots are non-truth performance caches (D-007 #10): derived, disposable, point-in-time, **version-tagged**. **Three-layer non-truth defence** (type-level marker + CI DC-025/026 + runtime guard) — and the marker **survives serialisation/storage** (deserialised as a non-truth-marked type, never plain authoritative data; the defence holds at the storage boundary). **Snapshot operations (create/refresh/dispose) are cache operations, NOT events** — they add no truth, keeping the event store clean of cache churn (contrast: registry/journal/audit ARE event-sourced). **Atomic capture** at a single consistent `global_position` (no torn reads). **Invalidation** on hash-chain breach (CN-4-003) at/before position, or when the recorded handler/projection version no longer matches the active set (detectable, not manual). Snapshot vs Projection vs Checkpoint distinction; tenant-scoped (DC-008); resilience-aware (DEGRADED may serve stale snapshots with disclosed freshness). No new CTRs; no Charter conflict.
- **Milestone:** **Foundation's full concept proposal is complete** — brief Sections 1–11 and all 23 CN concept documents (Phase 1 doctrine ×7, Phase 2 decision-driven ×3, Phase 3 parallel ×13), each discussed, Overseer-reviewed, and ratified by the Concept Lead. The BOS Kernel is now fully specified at the concept level: event sourcing, store contract, hash chain, command bus, isolation, identity, audit, replay, projections, primitives, saleable line/tender, advisor framework, kernel boundary & doctrine gate, AI guardrails & journal, time authority, document engine, compliance DSL, security, resilience, snapshots, extension points, and doctrine enforcement (37 checks).

**Merged to `main` — Term 4 Phase 3 (12/13)**
- **Term 4 CN-4-017 (Resilience Modes)** ratified and merged (commit `47aa5e2`). The NORMAL / DEGRADED / READ_ONLY state machine that many docs referenced (bus write-gate, replay availability, rebuild-during-operation, numbering block-reservation, rate-limit tightening, registration gating, advisor freshness). **Auto NORMAL→DEGRADED only** (time-sensitive, by `system:resilience-monitor`); all other transitions **human-gated** (D-007 #9). **Runtime layer, not replay-deterministic:** health checks are ephemeral live measurements; the `kernel.resilience.mode_changed.v1` event is a recorded fact (replay sees the transition, doesn't re-run checks) — consistent with CN-4-016. **Hash-chain breach (CN-4-003) is a health dimension → auto NORMAL→DEGRADED; READ_ONLY stays human-gated even for breach** — resolves the CN-4-003 §9 open item. **Three modes only** — planned maintenance is a human-gated NORMAL→READ_ONLY distinguished by `trigger_type` (planned_maintenance vs emergency), not a fourth mode. Transition event carries **impaired_dimensions** so components can respond proportionately. Per-component behaviour table (10 components × 3 modes). Health checks = config (no hard thresholds, D-004). **In-flight command semantics:** evaluated against the mode at processing time; committed events not retroactively affected. No new CTRs; no Charter conflict.

**Merged to `main` — Term 4 Phase 3 (11/13)**
- **Term 4 CN-4-016 (Security Primitives)** ratified and merged (commit `05bf146`). The Kernel's defensive building blocks beyond isolation (CN-4-006) and identity (CN-4-007): rate limits, anomaly detection, scope guards — covering three threat classes (abuse / attack / leakage). **Runtime layer, not replay-deterministic:** rate-limit counters and anomaly pattern-state are **ephemeral runtime state, not event-sourced truth**; the rejections (`kernel.command.rejected.v1`, `security.rate_limit_exceeded`) and **anomaly alerts** (`kernel.anomaly.detected.v1`, emitted by **`system:anomaly-detector`**) are recorded as facts and visible on replay, but the detectors do not re-run during replay. **Rate limits** are deterministic/blocking, per-principal + per-tenant aggregate ceiling, count attempts (legit retries accommodated), configurable, applied at the command-bus boundary (CN-4-004), audited. **Anomaly detection** is heuristic/advisory (alerts, never auto-blocks — avoids false-positive lockouts), reads projections only. **Scope guards** are the shared non-bypassable middleware pattern behind CN-4-006/005/022 guards (defence-in-depth with CI, D-007 #5). **Auth boundary:** the Kernel defends against what **authenticated principals** do; pre-auth brute-force/credential-stuffing/DDoS belong to the authentication layer (Term 7 mechanism / Term 1 policy). Resilience interaction (tighter limits in DEGRADED) deferred to CN-4-017. No new CTRs; no Charter conflict.

**Merged to `main` — Term 4 Phase 3 (10/13)**
- **Term 4 CN-4-015 (Compliance DSL Framework)** ratified and merged (commit `fc2ea2f`). The language compliance packs are written in (Law 5: compliance configured, not coded). Declarative, **sandboxed**, versioned, neutral. **Sandbox (D-007 #8):** no side effects, no system access, no network, **no ambient time, no randomness** — time/data arrive only as input via the clock protocol (CN-4-014); deterministic, resource-bounded. **Pack version is authoritative for freeze (D-009);** a rule's `effective-from` is scheduling of a known change *within* a deployed pack, **resolved against the transaction's business date** (CN-4-014, not record time) — both pack version and business date are stored, so replay is deterministic. **Tenant↔jurisdiction binding:** set at registration by the regional agent (Law 6, CN-4-006); the pack version active at emission governs and is stamped as `pack_version_ref`. **Extensions are CODE** — they pass the **doctrine gate** (CN-4-023), are **version-frozen** like packs (D-009, replay uses the historical extension version), run under the same sandbox constraints, and are guarded against overuse. **Pack approval gate** (Term 1 Compliance Officer, human, D-009-sensitive) is separate from the doctrine gate. **Buggy-pack correction (N5):** corrective pack version + compensating events; original events stand under the original pack (no history rewrite). Rwanda digital-services-levy example. No new CTRs; no Charter conflict.

**Merged to `main` — Term 4 Phase 3 (9/13)**
- **Term 4 CN-4-012 (Document Engine & Numbering)** ratified and merged (commit `4d3c898`). Hash-verified, immutable, legally-defensible documents (Document primitive terminal fold, CN-4-011). Six-phase lifecycle: template selection → content assembly → **compliance binding** (pack version at issuance, D-009) → numbering → hash → issuance (`document.issued.v1`). **Two-layer integrity:** the document hash (over content + template-version ref + pack-version ref + number) is stored **inside the issuance event**, so it is itself chain-protected (CN-4-003) — the document hash enables standalone/offline verification, the event chain protects it in-store. **Integrity vs authenticity:** the hash proves the content is unchanged (integrity) but not that BOS issued it (authenticity) — offline authenticity may need a **cryptographic signature**; flagged to Term 7 via CTR-017. **Numbering** is tenant-isolated, branch/terminal-scoped, sequential, deterministic, **block-reserved for DEGRADED** (D-007 #12); reconciliation is **compliance-pack-driven** (gap-free jurisdictions continue the sequence; gap-tolerant mark reserved-but-unused with an auditable reason). The hash covers **content + version references, not rendered output** — re-display uses the referenced template version. **Template versioning + freeze** (old documents never re-rendered; old templates retained). **Corrections/cancellations = new documents** referencing the original (D-009). **CTR-017 → NEGOTIATING** (Foundation verification logic delivered; integrity-vs-authenticity/signature question raised for Term 7). No new CTRs; no Charter conflict.

**Merged to `main` — Term 4 Phase 3 (8/13)**
- **Term 4 CN-4-014 (Time Authority)** ratified and merged (commit `d50508d`). Deterministic time for a replayable system. **Three injected Kernel clocks** — SystemClock (runtime, the only place real time enters), FixedClock (tests), ReplayClock (replay, time from event timestamps); no `datetime.now()` in event-producing logic (DC-017/018). **No SimulationClock** — what-if/profit simulation is an advisor-layer concern (CN-4-022, Law 3), outside the event store, never writes events. **Record time vs business time** (envelope timestamp vs payload effective date), both deterministic on replay. **Tz-database version freeze (a D-009 analog):** fiscal zone stored at registration with zone name + **resolved UTC offset + tz-database version**; historical events/properties are interpreted under the historical tz-db version so day boundaries never shift retroactively when the IANA database updates. **Timestamps are descriptive, not authoritative for ordering** — `global_position` is the total order; clock skew/NTP/leap-seconds tolerated (aligns with CN-4-009). Clock-rule scope: event-producing logic must use injected clocks; read-only "as of now" queries may use SystemClock. Fiscal-zone change = new event + freeze. Platform cross-tenant time reference zone deferred to Term 1/Architect. **Correction:** the four preceding Phase-3 changelog headers were off by one (CN-4-004 was the 4th Phase-3 doc, not 5th) — now corrected to 4/5/6/7. No new CTRs; no Charter conflict.

**Merged to `main` — Term 4 Phase 3 (7/13)**
- **Term 4 CN-4-009 (Replay Engine)** ratified and merged (commit `0d80051`). The mechanics of reconstructing state from events (proof of CN-4-001 Assertion 6). Four replay types: full / scoped (tenant, aggregate) / point-in-time / projection rebuild. **Determinism** via five requirements — same events, same `global_position` order, same **rules-data** (D-009 pack-version freeze), same **rules-code** (fold/handler code stays stable through additive versioning; changes become new event-type versions with **version-aware handlers**, CN-4-010 — determinism through version-aware handlers, *not* "code freeze"), same time (CN-4-014). **Defensible/audit replay includes hash-chain verification** (CN-4-003) for the scope; output states whether the chain was intact (routine rebuilds skip verification for speed). **Point-in-time cutoff is `global_position`** (a timestamp request resolves once to the position of the last event at/before it). **Checkpoints** are non-truth (CN-4-018), verified at creation, and **invalidated by a hash breach** at/before their position. **No-skip / compensating-events-only** correction (O9). **Replay never emits events** — pure reconstruction; persisting a rebuilt projection is a derived write, not event emission (may be deferred in READ_ONLY). No new CTRs; no Charter conflict.

**Merged to `main` — Term 4 Phase 3 (6/13)**
- **Term 4 CN-4-010 (Projection Framework)** ratified and merged (commit `1d86db7`). Formalises read-models that several merged docs already rely on (audit log CN-4-008, Decision Journal CN-4-013, engine registry CN-4-020). Projections are derived, rebuildable, deterministic, disposable, queryable, **position-aware**. **Position tracking & freshness:** each projection exposes `processed_position` / `processed_timestamp` — the **source of the Advisor Framework's `freshness_indicator`** (CN-4-022 N4). The processed_position is a **high-water mark** for idempotency — the framework skips re-deliveries (`global_position ≤ high-water mark`), so handlers don't reinvent idempotency (CN-4-005). **Deterministic `global_position` processing order** (rebuild reproducibility). **Unhandled event version is never silently skipped** — it is upcast (CN-4-002 §3) or fails as a doctrine violation (an incomplete projection violates "derived from events", CN-4-001). **Rebuild without downtime:** stale-but-available → historical replay → **catch-up to live tail** → atomic switchover (no gap, Section 8 #4). Tenant-scoped enforcement by the framework, not handlers. No new CTRs; no Charter conflict.

**Merged to `main` — Term 4 Phase 3 (5/13)**
- **Term 4 CN-4-008 (Audit Log Primitive)** ratified and merged (commits `5528290` + `9e80895`). "The append-only journal beneath every other audit log." General 9-field audit schema (audit_entry_id, event_ref, action_type, actor, tenant_scope, target, outcome, timestamp, correlation_id); specialised logs (Decision Journal, CN-4-013) extend it. **The audit log is a projection** (CN-4-010) over events — replayable, deterministic, integrity inherited from the hash chain, never out of sync (no second source of truth). **Two parallel trails** (tenant + platform), dual-actor actions appear in both (CN-4-007 §3). **Audit-completeness invariant** — every state change → bus → event → audit entry; structurally guaranteed; a formal DC will be added to CN-4-019 via the living catalog. Seven query dimensions + pagination. Overseer-review fixes: **(A)** cross-scope / platform-scope **reads ARE audited** (Section 8 #10, DC-011/020) — the "reads not audited" exemption is only in-scope owner reads; **(B)** audit entry is **per-event** (multi-event commands → multiple entries sharing one `correlation_id`); **(C)** retention operates on the projection/archival layer — **source events are never deleted** (CN-4-001); right-to-erasure is the separate crypto-shredding future decision (CN-4-006 §9). No new CTRs; no Charter conflict.

**Merged to `main` — Term 4 Phase 3 (4/13)**
- **Term 4 CN-4-004 (Command Bus & Policy Engine)** ratified and merged (commit `d23635c`). The **sole entry point for state changes** — no state change happens outside the bus. Flow: submit → envelope validation → policy evaluation → accept (emit event[s]) or reject (`kernel.command.rejected.v1`). **Resilience-mode gate** (the physical site of CN-4-017 write-mode enforcement: NORMAL full / DEGRADED gated / **READ_ONLY rejects all writes**). Four policy categories — **kernel → doctrine → engine → compliance**, kernel/doctrine first with **fail-fast**; kernel/doctrine never overridable. Policies are **pure evaluators** (side-effect-free, deterministic — replay/audit safe); compliance policies run in the **sandboxed evaluator** (D-007 #8). **Freeze doctrine:** compliance policies use the pack version active at command-processing; emitted events stamp `pack_version_ref` (D-009). **Atomic multi-event emission** (all events from one command, or none). **`correlation_id` lineage** on emitted + rejection events (full request→outcome trace). **System principals are subject to all policies — not a backdoor** (CN-4-007 §4). Rejections are auditable events with **cross-tenant privacy** (generic "scope mismatch" — no tenant-existence leakage). Idempotency via correlation_id + write-once. No new CTRs; no Charter conflict.

**Merged to `main` — Term 4 Phase 3 (2/13 and 3/13)**
- **Term 4 CN-4-003 (Hash Chain & Tamper Detection)** ratified and merged (commit `e7f63f5`). The event store's integrity proof. **Hash formula:** `event_hash(N) = hash(envelope excl. event_hash + payload + previous_hash)` — deterministic, no circular dependency; `previous_hash` counted once (DC-031 must independently recompute). **Genesis anchor:** a well-known, permanent, per-tenant constant. **Chain topology — decided:** **per-tenant chains anchored to `global_position`** (option b), the serialisation trade-off addressed explicitly — parallel writes per tenant (no cross-tenant hash dependency), cross-tenant ordering via global_position not hash linkage, aligns with CN-4-006 isolation. **Tampered vs unverifiable:** a break at N = suspected-tampered; events after N = unverifiable (not necessarily altered) until investigated. **Five-step breach protocol** (alert / quarantine / scope assessment / Term 1 escalation / **no silent recovery** — evidence preserved, no deletion, no auto-heal); breach may trigger DEGRADED via CN-4-017 rules, never a unilateral halt. TRA/Mzee Hassan example. No new CTRs; no Charter conflict.
- **Term 4 CN-4-019 (Doctrine Enforcement Checks)** ratified and merged (commits `4552c63` + `5781017`). The invariant test catalog — **37 doctrine checks (DC-001…DC-037)** covering **all 15 Section 8 criteria** plus structural invariants, organised by category, each mapped to Section 8 # / check type / source CN doc. New checks beyond the original draft: **DC-033** (Decision Journal completeness — closes the Section 8 #6 gap), **DC-034** (additive-only schema versioning), **DC-035** (stream-ordering / optimistic-concurrency integrity), **DC-036** (journal events emitted by a system principal only — protects the CN-4-013 "advisor never writes" resolution), **DC-037** (single-settlement single-currency). Three check types — **Static 9 / Integration 19 / Schema-manifest 9** (= 37; §3 corrected to match §2). Law 1–5 each mapped to checks; **Law 6 has no Kernel-testable check** (regional distribution is Term 1/2 policy — correct and deliberate). Living catalog: add = normal gate, modify/remove = Concept-Lead-approved doctrine-affecting change. Audit-completeness check deferred to the living catalog pending CN-4-008. No new CTRs.

## 2026-05-26

**Merged to `main` — Term 4 Phase 3 begins (1/13 parallel docs)**
- **Term 4 CN-4-013 (AI Guardrails & Decision Journal)** ratified and merged (commit `46a9536`). **Guardrails:** six forbidden operations + **three-deep enforcement** (Kernel-structural: advisor principal has no write path; runtime scope guard; CI doctrine checks — defence-in-depth, not redundancy). **Decision Journal** = a **specialised audit log** extending CN-4-008 (append-only, immutable, queryable, tamper-evident), realised as a CN-4-010 **projection** over an **event-sourced** foundation. **Paradox resolved:** journal entries are events (`kernel.advisor.suggestion.recorded.v1` / `…decision.recorded.v1`) emitted by the **Advisor Framework as a system principal** (`system:advisor-framework`, CN-4-007) — **the advisor never writes**; the Kernel records on its behalf. Full 14-field schema: `data_ref` is **point-in-time / replay-stable** (projection at a global_position/snapshot id, never a live query); `human_decision` lifecycle pending→accepted/rejected (appended decision event), **"ignored" derived** (computed absence, no phantom actor); `draft_command_ref` immutable (snapshot opt-in); `freshness_indicator` retained for legal defensibility; **no full chain-of-thought** (D-007 #6). Previously-suggested check = read against the projection. **CTR-009 → CLOSED** (explainability schema delivered and accepted). No new CTRs; no Charter conflict.

**Merged to `main` — Term 4 Phase 2 COMPLETE (3/3 decision-driven docs)**
- **Term 4 CN-4-023 (Kernel Boundary & Doctrine Gate)** ratified and merged (commit `3db3af7`) — **the final Phase 2 doc**. **Part A (D-002B):** Developer-AI is out-of-kernel — never runs in production, never writes events/state, produces code proposals only; **no special treatment** (same gate/review as human PRs); **synthetic/anonymised test data** (tenant isolation at the build layer, CN-4-006); not an advisor (CN-4-022 is runtime). **Part B — the doctrine gate:** runs on every PR/CI/engine-registration/Developer-AI proposal; **no override**, **fail-closed** (gate cannot run = blocked, never pass); **doctrine-check updates** require a heightened process (Concept Lead/governance approval, recorded as a doctrine-affecting change, audited — the wall cannot erode silently); **two distinct gates** (code doctrine gate here vs compliance-pack approval gate, CN-4-015 + Term 1, D-009-sensitive). **Build-provenance attestation** per release (commit hash, checks run + results, approver, timestamp, gate version) — immutable, tamper-evident (may mirror CN-4-008), making the Kernel's own lineage as defensible as its business data. CN-4-019 = the tests; CN-4-023 = the wall. **CTR-008** Foundation side delivered (boundary + gate; awaiting Term 1 governance + attestation retention). No new CTRs; no Charter conflict. **Phase 2 complete: CN-4-021/022/023.** Next: Phase 3 (13 parallel docs).

**Merged to `main` — Term 4 Phase 2 (2/3)**
- **Term 4 CN-4-022 (Advisor Framework & Role-Scope Binding)** ratified and merged (commit `1198e3e`). The single runtime Advisor Framework (D-002A, D-005). Contract = **audience** (three Charter §3 categories — Platform/Agent/Tenant, structural not roles) + **scope** (tenant / aggregate-opt-in / platform) + **model** (id/version/config_ref, swappable). **Runtime scope enforcement by the Kernel (D-007 #6)** covering **all advisor input including prompt assembly** (no out-of-band scope leakage; distinct from Term 7 prompt-injection defence). Consent-dependent scopes **re-evaluated every invocation** (revoke-wins, CN-4-011). Advisory-only (Law 3): reads projections only, replay-safe, **may produce inert draft commands** (no effect until a human submits via the command bus). **Dual-actor audit** — advisor principal + invoking human recorded together (CN-4-007). Journal fields **contributed to CN-4-013** (schema owner): incl. invoking_principal, prompt_ref, model_config_ref; **no full chain-of-thought** (CTR-009). Role→scope→advisor binding (Foundation = mechanism). Resolved N1 (cold-start data-sufficiency), N3 (model-swap, no re-consent), N4 (DEGRADED freshness / READ_ONLY block), q10 (benchmarking opt-in, anonymised aggregate). **CTR-007** resolution delivered (pending Terms 5/2/3); **CTR-014** Foundation side delivered (model plug-in point; awaiting Term 1 governance). No new CTRs; no Charter conflict.

**Merged to `main` — Term 4 Phase 2 begins (1/3)**
- **Term 4 CN-4-021 (Saleable Line & Tender Value Shapes)** ratified and merged (commit `5aad12d`) — first Phase 2 (CTR-driven) doc. The two value shapes (D-001, D-007 #1) connecting verticals to Universal Checkout. **Saleable Line:** authoritative inputs (quantity, unit_price pre-tax, discount_refs, tax_treatment_ref) vs **non-authoritative** derived `line_total` cache (Checkout must recompute, inputs win); **tax = input ref only**, computed at settlement by Checkout under the pack version active at emission (D-009, CN-4-015, Law 5 — verticals never compute tax); `source_tag` opaque + **non-branching guarantee** (doctrine-check enforceable, Law 2). **Tender:** {tender_id, method, amount, external_ref} — **no status field** (lifecycle-free; outcomes event-sourced by Checkout/Term 7); `method` = **registered category tag** (controlled vocabulary, extensible without Kernel change); **credit tender → Obligation primitive** (CN-4-011). **Currency invariant** (one settlement, one currency; cross-border = exception). Value-shapes≠primitives table; vertical→checkout flow; Karakana example with VAT computed at settlement. **CTR-001 & CTR-003** resolutions delivered (pending Terms 6/5 acceptance). No new CTRs; no Charter conflict.

**Merged to `main` — Term 4 Phase 1 COMPLETE (7/7 doctrine-foundation docs)**
- **Term 4 CN-4-020 (Extension Points)** ratified and merged (commit `0069608`) — **the final Phase 1 doc**. Two-phase onboarding: a **build-time doctrine gate** (CI per CN-4-019/CN-4-023; human review for Developer-AI pipeline, D-002B) followed by **runtime registration** (manifest submission → compatibility checks → **atomic** subscription wiring → activation, all-or-nothing rollback). Six compatibility checks; **event-type ownership transferable on sunset** (not permanently locked). **Registry is event-sourced** (`kernel.engine.registered/updated/deactivated.v1`; a CN-4-010 projection — replayable, auditable, deterministic; replaces a mutable registry store). **Manifest-update / re-registration** flow (version bump → re-check → additive wiring). Deregistration (4 steps, no event deletion; tenant notification = Term 1/3 hand-off). Resilience-mode gating (NORMAL allowed / DEGRADED human-gated / READ_ONLY blocked, D-007 #9). **Capability = advertise-only** (dependencies stay event-type-based; D-007 #11 without re-coupling). Registration is **platform-scoped + audited** (CN-4-006/007/008). **CTR-022 filed** (→ Term 1, activation & tenant-availability governance); **CTR-018 → NEGOTIATING** (contract delivered, pending Term 6 acceptance). **Phase 1 doctrine foundation is complete: CN-4-001/002/006/007/005/011/020.** Next: Phase 2 (CTR-driven) — CN-4-021, CN-4-022, CN-4-023.
- **Term 4 CN-4-011 (Primitive Catalog)** ratified and merged (commit `1054cb7`). The nine primitives — Ledger (no close-period, D-007 #13), Item/Service, **Party** (renamed from "actor" to the Charter §4.1 name; business entity, distinct from CN-4-007's system actor), Document (**terminal fold** — `issued` once; corrections = new linked documents per D-009), Inventory Movement, Obligation, Approval, Workflow, Consent (purpose+scope keying, revoke-wins, D-008) — each with shape, legal operations, and compensating-fold logic. Distinctiveness (Consent≠Approval by legal weight; Workflow≠Approval by complexity, D-007 #2); neutrality (D-004); engine-namespaced events (not primitive-namespaced); the Purchase Order composition example (§8). Four open items to Architect/Terms 5/6. **MASTER-GLOSSARY** gains **Party** and **Actor** entries (distinction recorded). No new CTRs; no Charter conflict.
- **Term 4 CN-4-005 (Engine Contract Model)** ratified and merged (commit `2d616aa`). The 10-field engine manifest (declarative; `requires` ⊆ `subscribes_to`; references event types, never engine names; deprecation marker within `emits`); Law 2 isolation enforced at both layers — runtime guards + CI doctrine checks (D-007 #5); subscription framework (emitter-unaware, event-type-based, new subscriber = manifest update only; at-least-once + ordered-within-stream + idempotent-expected, exactly-once = Architect); request-reply possible-but-discouraged (temporal coupling); the status-event + local-projection pattern (resolves O7/O8); pre-write malformed-event validation with hash-chain backstop and structural-vs-business-error distinction (O9/O10). Five open items assigned; CN-4-005/CN-4-020 boundary made explicit. No new CTRs; no Charter conflict.

## 2026-05-24

**Decision recorded**
- **D-009 — Temporal Interpretation / "Freeze" Doctrine.** Events and issued documents are interpreted under the rules/rates/pack-version active at emission/issuance; never retroactively reinterpreted. Implemented in CN-4-001/012/015; affects Terms 1/5/7. Arose from Term 4 Section 7 (edge cases O7, O15).
- **D-008 — Conversational / Messaging Channels.** A cross-term capability (not a new Term) for acquiring and serving customers over SMS, WhatsApp, Telegram, and email. Term 7 owns the channel adapters/gateway; Term 3 the tenant↔customer UX; Term 2 the agent use; Term 5 outreach/promotions; Term 4's consent primitive gates marketing. Guardrails: consent-first + opt-out, AI advisory/human-gated, channels via the Term 7 gateway. Boundary object BO-7; seeds CTR-019/020/021.

**Merged to `main`**
- **Term 4 CN-4-007 (Identity Primitive)** ratified and merged (commit `afccbba`). Four principal types (human, advisor, system, machine); authority chain (Kernel carries structure, not a permission engine); dual-actor record (resolves CN-4-006 R4); system-as-actor via the command bus (resolves Section 11 q4); advisor identity distinct from CN-4-022. Delegation flagged as a future decision (not a fifth type).
- **Term 4 CN-4-006 (Multi-Tenant Isolation)** ratified and merged (commit `0421bb4`). Seven-layer isolation + tenant scope object (non-bypassable); platform scope as a read-only parallel scope (dual-actor audit; policy = Term 1 via CTR-016); GDPR export scoping (cost = Architect); 7 leak-detection tests incl. snapshot isolation; data isolation = concept, resource isolation = Architect. Flags **right-to-erasure vs append-only** as a future decision (crypto-shredding direction).
- **Term 4 CN-4-002 (Event Store Contract)** ratified and merged (commit `50c2ce6`). The 15-field event envelope (engine-agnostic; pack_version_ref nullable for system events; record-time timestamp with business dates in payload; dedicated compensates_event_id; registered metadata), ordering (per-stream sequence + global_position + optimistic concurrency, resolving O1), additive-only schema versioning, and the append-only guarantee.
- **Term 4 CN-4-001 (Event Sourcing Doctrine)** ratified and merged (commit `587ea6c`) — the first CN concept document. Six doctrine assertions, the Three Whys with alternatives rejected, implications (compensating fold D-007 #3, freeze D-009, determinism), the Mzee Hassan/TRA example, and boundaries. Glossary terms **Compensating Event, Fold, Replay** added to `MASTER-GLOSSARY.md`.

**Decision recorded**
- **Term 4 Sections 12–14 intentionally omitted** (Concept Lead approved): §12 (Notes on Existing Repository) is reference-only; §13 (First Tasks) is superseded by Section 6's writing order; §14 (Restraint) is folded into Section 1 (Mission) and Section 4 (Scope). Term 4's brief-level proposal is therefore Sections 1–11. Next: the 23 CN concept documents, in the Section 6 order (Phase 1 → 2 → 3).

**Merged to `main`**
- **Term 4 Section 11 (Open Questions)** ratified and merged (commit `9090ed5`) — the **final brief-level section**. 7 resolved by decisions, 8 open assigned to CN docs (3 from Brief §11 + 5 new), 0 needs-ruling, 0 new CTRs. **Term 4's brief-level proposal (Sections 1–11) is complete.**
- **Term 4 Section 10 (Relationships with Other Terms)** ratified and merged (commit `0728389`). Depends-on (Term 7 only), depended-on-by (consistent with Section 5), the live cross-term picture (11 CTRs: 7 inbound incl. CTR-021 + 4 outbound; BO-1/2/3/5/6/7), and hand-off statements.
- **Term 4 Section 9 (Architect's Role)** ratified and merged (commit `6936b61`). Concept→Architect mapping for all 23 CN docs, 12 directives (6 from Brief refined + 6 from decisions), a "Too Detailed for Concept" boundary, and the Phase 1→2→3 sequence note. Architects framed as a future audience.
- **Term 4 Section 8 (Success Criteria)** ratified and merged (commit `87e35fe`). 15 machine-provable criteria (9 refined from Brief §8 + 6 from decisions), each with a "How Verified" column leaning on CN-4-019 invariant tests; a "Belongs Elsewhere" table excludes UX/pack-content/performance/cross-term items.
- **Term 4 Section 7 (Edge Cases)** ratified and merged (commit `8cc1f50`). 34 edge cases sorted: 19 resolved by decisions (D-007/D-002A/D-008/D-009/Charter/Section 3), 15 assigned to CN docs; no needs-ruling, no new CTRs.
- **Term 4 Section 6 (Key Concepts)** ratified and merged (commit `42b9d06`). The 23-doc CN catalogue grouped by clusters A–E with Decisions + CTR columns, overlap note, writing order (Phase 1 doctrine → Phase 2 CTR-driven → Phase 3 parallel), and cross-term summary. Supersedes Brief §13's First Tasks.
- **Term 4 Section 5 (Audiences Served)** ratified and merged (commit `1d2bbf8`). Audience table (Terms 1,2,3,5,6,7 + Architects as a future audience), promise referenced from Section 1, and an extensibility note (new consumers via CN-4-020; new materials via CTR + restraint test). No new CTRs.
- **Term 4 Section 4 (Scope · Out)** ratified and merged (commit `970709b`). Consolidated OUT list (26 items, 7 categories) with owners, the restraint statement, and three borderline rulings (Auth, prompt-injection, partitioning) — all OUT. No new CTRs.
- **Term 4 Section 3 (Scope · In)** ratified by the Concept Lead and merged (commit `3b8129d`). Encodes all 13 D-007 resolutions across five clusters; cross-term items (CTR-001/003/007/016/017/018) marked pending acceptance by Terms 1/5/6/7.

**Decision recorded**
- **D-007 — Foundation Scope Resolutions (Section 3).** 13 ratified resolutions for what the Kernel provides, incl. Saleable Line/Tender as value shapes (Checkout = Term 5 engine), the nine primitives (Consent ≠ Approval), compensating-event fold semantics, a distinct "advisor" principal + first-class platform scope, runtime advisor-scope enforcement, Foundation verification logic vs Term 7 surface, DSL grammar + sandboxed evaluator, resilience auto/human transitions, and **removal of "close period" from the Ledger primitive** (neutrality). Seeds CTR-016/017/018; resolves CTR-009; partially resolves CTR-007; proposes CTR-001/003.

**Merged to `main`**
- **Term 4 Sections 1 (Mission) & 2 (Mission Question)** ratified by the Concept Lead and merged to `main` (commit `65cb2a0`). Overseer review confirmed D-004/D-006 compliance; Term 4 applied two fixes (Parent links; "cracks" → "holds" consistency). First Term proposal sections to land on `main`.

**Ratified**
- **Term 4 Section 2 (Mission Question)** wording ratified by the Concept Lead — version E with "holds": *"How do we build a kernel that holds under decades of pressure — new verticals, new countries, AI that must remain advisory — and still proves to a court that nothing was ever silently changed?"* Charter §5.2 summary updated to match (de-numbered). Term 4 authors the file on its branch (D-006).

**Decisions recorded**
- **D-005 — AI Mode (Universal Per-Role Dashboard Advisor).** Every dashboard, for every role (tenant, platform steward, regional agent), gets an "AI Mode" built on the one Advisor Framework (D-002A) — prompt + proactive role-scoped advice, advisory-only, journaled, explainable. Enabled for all roles from the start. Seeds CTR-014, CTR-015.
- **D-006 — Collaboration Operating Model.** `main` is the single shared source of truth; each Term authors only its own files on a branch from `main`; the Overseer publishes shared docs, reviews, merges, answers Terms' questions, and keeps every Term current — and does NOT author Term content.

**Removed**
- The subagent-authored `terms/term-4/proposals/SECTION-1-MISSION.md` and `SECTION-2-MISSION-QUESTION.md` were discarded; Term 4 authors its own work on its branch (D-006).

**Shared docs published to `main`** so every Term session can read them.

## 2026-05-23 (Term 4 §1 review)

**Ratified**
- **Term 4 Section 1 (Mission)** — ratified by the Concept Lead. First proposal section to pass. Recorded in `terms/term-4/proposals/SECTION-1-MISSION.md`.

**Decision recorded**
- **D-004 — Concept-Writing Principles.** From the Term 4 Mission review: a Mission carries only the unchanging; no unverifiable hard numbers (supersedes the "10 years" vs "20 years" conflict in Term 4 Brief §1/§2); "invisible" is scoped to runtime users; audience/tone follow each Brief §5; Foundation Mission must elevate legal defensibility, neutrality-by-restraint, and the six Laws as technical invariants. D1–D4 rulings recorded.

**Glossary created**
- `MASTER-GLOSSARY.md` (Term 7 / CN-7-003), seeded with the Foundation-vs-Kernel ruling and core cross-Term terms.

## 2026-05-23 (later)

**Collaboration protocol clarified**
- `CROSS-TERM-COLLABORATION.md` — §5 rewritten to a discuss-then-write workflow (Terms bring points section by section to the Concept Lead; the proposal is written only after agreement). Added §6 Authority & Approval (Concept Lead is final; Overseer/Term 7 coordinates and must also agree; no complete system proposal until all seven Terms pass) and §7 the AI-agent operating model (one Term = one agent, one owner per file, CTR as the only message bus, per-Term folders, git branches).
- Terms 1–7 addenda updated to state the discuss-and-agree-first rule and the Concept Lead's final authority.

## 2026-05-23

**Added — concept-environment scaffolding**
- `DECISION-LOG.md` — binding cross-Term decisions (Term 7 / CN-7-007).
- `CROSS-TERM-COLLABORATION.md` — interaction map, boundary objects, and the Cross-Term Request (CTR) process.
- `CROSS-TERM-REQUEST-REGISTER.md` — living register of CTRs, seeded from D-001..D-003.
- This `CHANGELOG.md` (promised in Charter §11).

**Decisions recorded**
- **D-001 — Universal Checkout.** POS becomes a two-layer design: a universal Checkout/Tender layer over a shared Saleable Line abstraction, with each vertical feeding the lines. Lets any business type use one checkout.
- **D-002 — AI.** (A) One runtime Advisor Framework (advisory-only, scoped, model-swappable, Decision-Journal-logged) for tenants, agents, and platform. (B) A build-time Developer-AI environment that lives outside the kernel, briefs Claude Code (the implementer) to make GitHub changes, and releases only after human review + tests/doctrine invariants pass.
- **D-003 — Distribution.** Multiple regional agents per region (Law 6 intact) plus a referral program as an acquisition layer, not a replacement for compliance-accountable agency.

**Charter amendments**
- §2.4 added — pointer to standing decisions (Decision Log).
- §6.3 rule 6 added — Cross-Term Request requirement before the Architect phase.
- §11 updated — Decision Log + CHANGELOG now exist and are referenced.

**Term Briefs**
- Terms 1–7 each gained a "Standing Decisions & Cross-Term Work" addendum mapping D-001..D-003 to new concept docs and CTRs.
