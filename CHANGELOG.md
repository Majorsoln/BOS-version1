# Changelog

> Records every change to the Charter and to cross-Term decisions.
> Managed by Term 7 — Integration & Coherence (Charter §11).

## 2026-05-30

**Term 5 — Universal Engines (10/~15)**
- **Term 5 CN-5-006 (Reporting & BI Engine)** ratified and merged (commit `f785a10`) — Term 5's seventh engine (796 lines, 19 sections). The read-side / analytics layer that consumes events from every other engine and produces projections, KPIs, snapshots, formal Documents, statutory summaries, and exports. **Seven laws R1–R7:** read-side only (NO business truth emission — only meta-events about reports/snapshots/Documents/exports/alerts; never journals, never cash, never stock, never direct notifications); all projections derive from event store (CN-4-010 inheritance; rebuildable; deterministic); pack-driven templates (Law 5); multi-scope (tenant default + site operational + platform reserved for Term 1 billing aggregation per CN-5-101 §7); single ledger + dual-standard views via pack mapping (no parallel ledger — consistent with CN-5-001 Q5); **R6 (N3 — doctrine-critical) Snapshots ≠ Statements** — a CN-4-018 snapshot is a non-truth performance cache (discardable, rebuildable); a CN-4-012 Document statement (period-end P&L, balance sheet, statutory summary) is a formal hashed/numbered/template-versioned/pack-frozen artefact verifiable years later — distinct primitives serving distinct purposes; BI Advisor integration is read-only (Law 3; CN-5-010 wires the Advisor; CN-5-006 provides read surface). Multi-scope manifest with **N3 `reporting.statement.issue.request` + `reporting.statement.issued.v1`** (formal Documents via CN-4-012); meta-only emits restricted to reports/snapshots/Documents/exports/alerts; massive subscription fan-in across Accounting/Cash/Inventory/Procurement/HR/Checkout (all `kind: projection` or compensation). **N1 Live Reports vs Period-Locked Documents** distinction explicit (§5 side-by-side table) — live reports are transient read-time evaluations against current projections; period-locked Statements are immutable Documents. Financial reports (P&L, balance sheet, cash flow) via pack templates from CN-5-001 §6 — live view + Statement issuance at period close. Operational reports (sales / AR-AP ageing / inventory / procurement / workforce / cash). Statutory summary reports issued as Documents per N3. KPI base library + custom definitions; **N5 alert delivery boundary** — Reporting emits `reporting.alert.triggered.v1` meta-event only; Term 3 dashboard surface + Term 7 channel adapters handle actual delivery (Reporting never sends directly). Snapshots integration with R6 distinction explicit in §10. Multi-standard via pack mapping rules. Export categories + filing-format generation deferred (CTR-035 → Open Item per Charter §1.3 — BOS does not file). **N4 vertical-dimensional reports** future via CTR-030 (v1 catalogue empty; grows as Term 6 defines verticals). UI-01/05/06/07 enforcement. Karakana year-end example (live dashboard + year-end P&L Statement Document + statutory summary Statements + filing export + BI Advisor suggestion + D-009 pack-upgrade freeze demonstration). **CTR-029 expansion note added** (statutory_summary_reports + reporting_snapshots + export_templates + alternative_standard_views + KPI base library + alert delivery routing). **CTR-030 expansion note added** (vertical dimensional report event contracts). **CTR-035 → Open Item** (filing-format adapter; Charter §1.3 deferral). No new CTRs; no Charter conflict.

**Term 5 — Universal Engines (9/~15)**
- **Term 5 CN-5-005 (HR & Payroll Engine)** ratified and merged (commit `44dcd5c`) — Term 5's sixth engine (870 lines, 19 sections). Employee management + payroll computation for any business, with statutory rules pack-driven and Charter §1.3 boundary explicit (BOS computes; HR officer/chartered accountant files). **Seven laws H1–H7:** employee = Party primitive + HR projection (parallel to supplier in CN-5-004 §4); assignments and compensation are versioned (full employment history); payroll is a per-period event (D-009 freeze under the pack/standard active at run time); statutory deductions are 100% pack-driven (PAYE bands, pension, NHIF, training levy per jurisdiction — Law 5 + D-004); **Charter §1.3 boundary** (BOS does not file with tax authorities); loans via Obligation primitive (kind: `employee_loan`, unifies with AR/AP/advance/layby/credit-tender; UI-08 bounds); multi-engine choreography only (no orchestrator). Multi-scope manifest (tenant default for employee identity; site overrides for attendance, site-anchored assignments, site-initiated loan requests). **N1 namespace fix**: deduction event renamed to **`hr.payroll.deduction.applied.v1`** (Charter §8.1 — engine_id `hr`); CN-5-002 §H cosmetic alignment deferred to CN-5-103 Universal Event Glossary pass. **N2 namespace fix**: periodic accrual via **`hr.accrual.run.request`** command from `system:scheduler` actor (CN-4-007); no `system.scheduler.*` event namespace (parallel to CN-5-001 N1 fix for depreciation). **N3 post-gate disbursement**: Cash subscribes **`hr.payroll.approved.v1`** (after authorisation), NOT `hr.payroll.computed.v1`; prevents disbursement of unapproved payroll. **N4 atomic deduction-with-payment**: `hr.payroll.deduction.applied.v1` emitted **atomically with `hr.payroll.paid.v1`** (CN-4-004 §2 multi-event emission); Obligation outstanding reduces only after payment confirmation, preventing silent over-reduction on disbursement failure. Employee management lifecycle (registered → active → on_leave → terminated/deactivated); versioned roles + site-anchored assignments + additional_sites for multi-site staff; compensation_kind variants (salary/hourly/commission/piece_rate/hybrid). Pack-driven leave (types, accrual rates, balance lifecycle, encashment-on-termination). Site-scope attendance + pack-driven overtime multipliers. Payroll computation 7-step flow (inputs → deductions → net → emit → authorise gate → per-employee deduction events → disbursement). Pack statutory schema with PAYE bands, pension, NHIF, levies, dependent allowances. Employee loan choreography recap (HR-side write-off path added for termination/default). Commission/piece/tip via vertical Pattern B subscriptions. Termination workflow with outstanding loan auto-deduct from final pay (or escalate to write-off authorisation). Karakana wafanyikazi year example (onboarding → August loan → monthly payroll → resignation April → year-end statutory reports). **CTR-029 expansion note added** (hr_leave + hr_payroll period + hr_statutory_deductions + termination_computation + overtime multipliers). **CTR-030 expansion note added** (vertical commission/piece-rate/tip event contracts). **CTR-034 → Open Item** (Term 7 filing-format adapter; Charter §1.3 deferral). No new CTRs; no Charter conflict.

**Term 5 — Universal Engines (8/~15)**
- **Term 5 CN-5-004 (Procurement Engine)** ratified and merged (commit `512d54e`) — Term 5's fifth engine (868 lines, 20 sections). The buying lifecycle for any business: Requisition → PO → GRN → Invoice → Payment-reference, with optional Phase 0 (RFQ/Quote). **Seven laws P1–P7:** lifecycle = truth (Workflow primitive, append-only); document at every binding transition (CN-4-012 with hash/number/freeze); three-way match before payment authorisation (pack-driven variance per quantity/price/item, auto-pass/flag/hard-block bands); **AP via Obligation primitive** (kind: `accounts_payable`, UI-08 bounds — unifies with AR/delivery/layby/advance/credit-tender across engines); compliance via pack (approvals, tax, supplier types, credit-term defaults); returns via Credit Note compensating document (UI-02 symmetry across Inventory + Accounting + Cash); **one lifecycle, two effects** — direct-expense and inventory-tracked procurement share the same workflow, differing only by `inventory_effect` flag at GRN (P7 unification). Multi-scope manifest (site default; tenant overrides for supplier records, credit terms, payment scheduling, high-value approvals). **N1 consolidated handler**: single `handle_cash_payment` subscription to `cash.payment.disbursed.v1` with internal routing on `obligation.kind` (payable → mark_invoice_paid; credit_refund → mark_credit_note_received) — manifest stays focused on event types, routing stays inside engine business logic. **N2 issued-vs-recorded document distinction**: BOS issues RFQ + PO + GRN + Payment Voucher + Credit Note (BOS-owned documents per CN-4-012); BOS records Quote + supplier Invoice (supplier-owned content, BOS records reference + metadata). **N3 approval timing**: RFQ is a market scan (no commitment, no pre-RFQ approval by default); approval happens at requisition (post-quote when business is about to commit). Pack may opt in to `procurement.rfq.approve.request` for jurisdictions that require pre-RFQ authorisation (e.g., government procurement). **N4 CN-5-002 §7 boundary**: formal procurement-with-supplier (has PO + invoice) → CN-5-004; petty/informal cash-and-carry (no PO) → CN-5-002 §7 `cash.expense.record.request`. Three-way match auto-pass/flag/hard-block per pack-defined variance thresholds; tenant override within ±50%. AP Obligation lifecycle (creation at invoice recording; partial settlement; UI-08 bounds; due_date derived from supplier credit terms; early-payment discounts applied at Cash settlement per pack rule). Approval primitive for single-step; Workflow primitive for multi-step (board-level). Partial flows (PO ↔ multi-GRN ↔ multi-Invoice ↔ multi-Payment) via state machine. Returns: pre-invoice (handled at GRN discrepancy) or post-invoice (Credit Note); both UI-02 symmetric. Multi-currency: AP in supplier currency; FX at-record vs at-payment per pack rule. Karakana aluminium full-lifecycle example (requisition + manager approval + PO + partial GRN 30+20 + invoice + 3-way match + 2/10 Net 30 early-payment discount + completion + defective-return variant). **CTR-029 expansion note added** (procurement_approvals.thresholds + three_way_match.variance_thresholds + supplier_credit_term_defaults + procurement_forex_recognition). **CTR-030 expansion note added** (vertical `purchase_need.recorded.v1` event contracts). **CTR-033 → Open Item** (Term 7 supplier verification adapter; defer to when packs require external verification). No new CTRs; no Charter conflict.

**Term 5 — Universal Engines (7/~15)**
- **Term 5 CN-5-002 (Cash Management Engine)** ratified and merged (commit `bc1ad39`) — Term 5's fourth engine (and largest doc to date: 1,002 lines, 22 sections). Scope expanded from "drawer + sale" to **money-position management** across 8 till kinds (physical cash drawer / mobile money wallet / card-merchant balance / petty cash float / site safe / main safe / multi-currency drawer / tip pass-through). **Seven laws C1–C7:** till = money-position abstraction (one engine serves all till kinds via `kind` attribute); site-scope default + tenant overrides for consolidations/main_safe/inter-site/equity/deposits (CN-5-101 §6 pattern); every cash event causation-chains to one of five closed authorised origins (UI-04); currency invariant per till (UI-06); session lifecycle via Workflow primitive (CN-4-011); **all debts/credits/commitments via Obligation primitive** with appropriate `kind` (AR receivable, AP payable, employee_loan, layby, advance_received, delivery — unifies cross-engine choreography on one primitive); reconciliation variance per pack threshold (auto-adjust band / anomaly band / hard-block band). Multi-scope manifest (17 commands, 25+ emits, per-vertical Pattern B subscriptions for layby/advance fulfillment). **Generic operating-expense pathway** (`cash.expense.record.request`) with 14 pack-defined categories (rent, utilities, fuel, transport, repairs, supplies, communications, staff_welfare, professional_fees, financial_charges, **insurance, marketing_advertising, bank_charges**, other — N1). **AR/AP via Obligation** (6 kinds; settlement command shapes; UI-08 bounds throughout). **Employee loan choreography** (HR creates Obligation → Cash disburses → Payroll deducts each period → Obligation settles incrementally → Accounting auto-journals each step). **Inter-till + inter-site transfers** + drops; **bank deposit handoff** to Term 7 via CTR-031 (analogous to CTR-006 payment adapter). Refund cash side as compensation (CN-5-009 pairing). **Owner equity flows** as Cash-owned commands (dual-actor audit) because cash impact is operational truth. **Multi-currency tills** opt-in per till + FX coordination with CN-5-001 §11. **Tips:** cash via `tip_passthrough_till`; digital tips via Checkout payload metadata (N2). **Layby vs advance** semantic distinction (advance = single-direction owe-goods; layby = phased accumulation → release on full; N4) + **cross-engine coupling with Inventory reservations** (CN-5-003 §9, N5). UI-04/UI-06 enforcement structural at every emission. Mama Amina full-day example (8 tills, mixed tenders, AR/AP/loan/advance/refund/reconciliation/deposit/payroll integration). **v2 `bank_account` till kind** flagged as Open Item (N3). **CTR-031 NEW** filed (Term 7 banking adapter) — distinct from CTR-006 (payment-at-tender) because deposit destination + settlement times + statement reconciliation differ. **CTR-029 expansion note added** (operating_expense_categories + cash_reconciliation thresholds + multi_currency_policy + tip_tax_routing). **CTR-030 expansion note added** (layby/advance fulfillment vertical event contracts). **CTR-032 → Open Item** (equity authorisation; not separate CTR). No Charter conflict.

**Term 5 — Universal Engines (6/~15)**
- **Term 5 CN-5-003 (Inventory Engine)** ratified and merged (commit `5e43c4d`). Universal stock-truth engine serving café, workshop, pharmacy, retail with one manifest. **Six laws I1–I6:** site-scoped stock truth (`(tenant, site, item, lot?)` tuple — complements CN-5-001 A4 tenant-scope Accounting); every movement typed at source (UI-03/DC-039); negative stock blocked by default; costing pack-driven + per-item overridable (Law 5); **recipes are Inventory engine state, not Item primitive** (avoids Foundation extension — Q2 Option β); site_id from registry (UI-09/DC-041). **Multi-scope manifest** (site default + tenant overrides for items/recipes/transfers/expiry — CN-5-101 §6 pattern). **Recipe expansion in two coexisting patterns:** Pattern A (Inventory auto-expands from versioned recipe in its projection — café latte, pharmacy kit, retail bundle); Pattern B (vertical emits `<vertical>.ingredient.consumed.v1` — workshop parametric cuts, restaurant substitutions, hotel conditional). **`expansion_mode: auto | vertical_managed` per-line signaling** (N1) on the bill payload disambiguates — `vertical_managed` skips Pattern A deduction and waits for Pattern B. **Recipe versioning** with D-009-analog freeze (recipe-version active at sale moment applies on replay). Eight movement types (receive/deduct/transfer/adjust/reserve/release/write-off/offcut) with umbrella `inventory.adjustment.recorded.v1` reaching Accounting. **Lots & expiry opt-in** per item attribute; scheduler-driven expiry runs (`accounting.depreciation.run`-style pattern: `inventory.lot.expiry_run.request` by `system:scheduler`). **Costing methods** pack-declared with **standard-dependent allowed_methods** — IFRS packs `[fifo, weighted_average]` (IAS 2 2003 prohibits LIFO), US GAAP packs may include LIFO. **N2:** lot-tracked + weighted-average **decouples** physical pick (FIFO-by-expiry) from cost recording (pool weighted-average). **Reservations** via Workflow primitive + **hard-hold semantics** (deduction targeting reserved qty rejected unless naming the reservation). **Offcuts as sub-items** (lazy registration on first cut — full lifecycle vocabulary). **Substitutions** captured via `substituted_for + reason` metadata (Pattern B only). **Multi-site transfers** 3-phase (initiate→in-transit→receive); **N4** in-transit semantics: stock shown as `reserved` at from-site, NOT a separate "in-transit pool"; analytical in-transit view = query over open transfers. **Backorder opt-in** (default blocked I3) — **N3** backorder wraps the **Obligation primitive** (kind: `delivery`), coherent with CN-5-009 credit pattern; UI-08 bounds apply. **N5** cycle counts: `count.request` payload supports optional scope (full / item-list / category-list / lot-list). Four-vertical example (café latte / workshop window with offcuts and reservation / pharmacy dose with FIFO-by-expiry / retail tomato atomic). **CTR-027** used for UI-09 enforcement. **CTR-029 expansion note added** — pack section gains `inventory_costing` subsection (per-jurisdiction approval covers it). **CTR-030 expansion note added** — Pattern B vertical event contracts (`restaurant.ingredient.consumed.v1`, `hotel.amenity.consumed.v1`, `workshop.cut.executed.v1`, `workshop.parametric.consumed.v1`) and `expansion_mode` per-line attribute. No new CTRs (Q2 Option β avoided Foundation extension). No Charter conflict.

**Term 5 — Universal Engines (5/~15) — first per-engine doc**
- **Term 5 CN-5-001 (Accounting Engine)** ratified and merged (commit `37c67ce`). Term 5's first per-engine doc and the auto-journaling backbone. **Five laws A1–A5:** standards in packs (Law 5 + D-009 freeze — IFRS/TFRS/GAAP rules live in compliance packs, never in code); auto-journal completeness (every monetary business event → exactly one journal — UI-07 trial-balance root); balanced at emission (bus rejects unbalanced posts; CN-4-011 Ledger fold); tenant scope with site as analytical dimension only (D-007 #13 — Ledger has no close-period; period close is Accounting's; CN-5-101 tenant scope); corrections via compensating journals + closed-period inviolability (UI-05 + UI-02; no re-open). **Manifest:** 7 commands (chart configure/account add/account deactivate/journal post system-only/adjustment post authorised/period close + `accounting.opening_balances.set.request`), 8 emits (incl. `accounting.depreciation.posted.v1` per N1 namespace fix — no `kernel.depreciation.*`), 13 auto-journal subscriptions covering revenue/refund/cash/procurement/inventory/payroll/depreciation/FX/promotion. **Chart of accounts model** — pack base + tenant sub-account customisation within standard limits; 5 account types; analytical dimensions per account (site_id, cost_center, …). **Pack accounting-section schema (CN-4-015 extension via CTR-029)** — 6 subsections: chart_of_accounts_base, recognition_rules (accrual/cash-basis/hybrid per pack, tenant election where allowed), reporting_templates, depreciation, forex, **`period_calendar`** (fiscal_year_start, reporting_frequencies, close_lag_days — N2). **Opening balances (N3)** — dedicated one-time `accounting.opening_balances.set.request` command (dual-actor audit; pack validates), NOT via adjustment.post which is current-period-only. **Corrections (§9, N4)** — explicit distinction: `causation_id` = forward trigger (what invoked the handler, UI-01); `compensates_event_id` = backward reversal link (what is being undone, UI-02). Charter §1.3 boundary made explicit (§12) — management truth, audit-ready data; **no statutory filing** (chartered accountant uses BOS data; BOS does not file/certify/submit). FX revaluation pattern via `<fx-adapter>.fx.rate.recorded.v1` (Term 7 adapter; no `kernel.fx.*`). Mama Amina FY2026-07→2027-06 example (onboarding opening balances → operational year → monthly closes → November correction post-close → year-end → statutory filing OUTSIDE BOS). **CTR-029** (→ Term 1, accounting-standard pack-section content/governance) and **CTR-030** (→ Term 6, vertical payload journal mapping) stay OPEN; CTR-027 unchanged. No Charter conflict.

**Term 5 — Universal Engines (4/~15) — first concrete engine, revenue hinge**
- **Term 5 CN-5-009 (Universal Checkout/Tender Engine)** ratified and merged (commit `979b2f8`) — Term 5's first concrete engine doc, the **revenue hinge** that every downstream universal engine depends on. Implements **D-001** atop CN-4-021 value shapes (CN-5-009 is the engine; Foundation provides the shapes). **Five laws K1–K5:** vertical-blind (DC-022 enforces non-branching on `source_tag`); authoritative inputs only (recomputes line totals; trust no upstream cache); tax at settlement, pack-frozen (D-009 + CN-4-015); tender lifecycle = events (not mutable state); settlement **explicitly triggered** (cashier-submitted `checkout.settle.request`, never auto) and **atomic** (CN-4-004 §2). **Manifest** with `scope_policy: site` default, per-vertical `bill.ready` subscriptions, per-adapter outcome subscriptions, eight emitted event types. **Settlement workflow Phase 1–4:** Ingest → Collect Tenders (sync cash immediate; async via Term 7 adapters with `callback_correlation_id`) → Adapter Round-Trip → Finalise (atomic `settled` + `receipt.issued` + `obligation.created` for credit). **Refund = command + compensation event** (`compensates_event_id` non-null; UI-02 symmetry; partial-refund per-portion symmetry → Open Item for Architect). Receipt issuance via CN-4-012 (template + numbering + hash; DEGRADED block-reserved). Credit tender → Obligation primitive (CN-4-011, UI-08 bounds). **Currency check explicit at `checkout.tender.add.request`** (M1, UI-06). `source_tag` non-branching restated with DC-022 reference (M4). DEGRADED behaviour brief (M3). Karakana settlement example incl. partial-refund propagation. **CTR-003 → CLOSED** (Term 5 accepts Checkout-as-engine, Foundation delivered). **CTR-001** advanced (Term 5 demonstrates downstream usability of CN-4-021 shapes; awaits Term 6 acceptance). **CTR-005 → NEGOTIATING** (Term 5 commits always-on; awaits Term 1 + Term 6). **CTR-006 → NEGOTIATING** (proposed `callback_correlation_id` contract published for Term 7). **CTR-028** already OPEN (tender method registry). No Charter conflict.

**Term 5 — Universal Engines (3/~15)**
- **Term 5 CN-5-102 (Engine Invariants)** ratified and merged (commit `5e2ee08`). Term 5's cross-engine state-shape doctrine — distinct from subscription patterns (CN-5-100, behavior) and scope policy (CN-5-101, where state lives). **Boundaries B1–B3:** cross-engine only; state-shape not behavior; inheritance not re-statement. **Initial catalog of 10 Universal-engine Invariants (UI-01..10)** each with statement/scope/spans/enforcement/DC-mapping/violation/reasoning: UI-01 causation_id integrity (command-origin = null; subscription-derivative = source-id); UI-02 compensation symmetry; UI-03 inventory movement carries typed source ref; UI-04 cash tender causation chain; UI-05 closed-period inviolability **including compensating events** (corrections post current period with causation back); UI-06 tenant functional-currency invariant (tenant property, distinct from per-settlement DC-037); UI-07 trial balance per tenant; UI-08 no engine bypasses Obligation primitive's bounds via raw mutation; UI-09 site_id in tenant site registry; UI-10 promotion cost-share sums. **Three-layer enforcement** (runtime / CI integration / CI schema) with multi-layer composition. **Three-pattern violation** (hard-fail / post-hoc anomaly / mode escalation) — multi-pattern per invariant per stage. **DC ramp-up — Phase 0 via CTR-025:** DC-038 (UI-01), DC-039 (UI-03), DC-040 (UI-06 extending DC-037 to tenant scope), DC-041 (UI-09); Phase 1 (UI-02/04/05/07/08/10) awaits per-engine docs and per-engine CTRs. **Living catalog discipline** mirrors CN-4-019 §5. Mama Amina period-close example (engine-contract tone). **CTR-025/026/027** all OPEN. **MASTER-GLOSSARY** gains "Universal-engine Invariant (UI-NN)" entry. No Charter conflict.

**Term 5 — Universal Engines (2/~15)**
- **Term 5 CN-5-101 (Scope Policy Application)** ratified and merged (commit `5f04bff`). Term 5's policy applying CN-4-005 §1.1 (post-CTR-023 amendment) to universal engines. **Three scope levels** — **`site`** (intra-tenant location partition; `site_id` in payload, NOT envelope), **`tenant`** (isolation boundary, "business-wide"), **`platform`** (parallel, rare, dual-audited). Tags-not-levels for region/zone grouping. **Five laws:** no cross-tenant ever (L1); site is payload not envelope (L2); scope declared per operation, not per engine (L3, via the CN-4-005 §1.1 mechanism); site dispatch per `(tenant_id, site_id)` tuple (L4); platform on a universal engine is `requires`-rare with `platform_authorised: true` + dual-trail audit + Term 1 governance (L5). **Runtime dispatch algorithm** with explicit **rejection on missing `site_id`** (no silent fall-back to tenant — preserves isolation). **Multi-scope engine pattern** (canonical Cash manifest sketch: `site` default with `tenant` overrides for consolidated position and cross-site transfer). **Reporting → Term 1 chain (Q3 Chaguo C)** — Reporting emits per-tenant; Term 1 aggregates at platform scope under CTR-016 governance; universal engines stay off platform scope by default. **Neutrality enforced** — payload field is `site_id`, invariant across verticals (never branch/outlet/property/etc., D-004). Mama Amina two-branches example with engine-contract tone (incl. doctrine-failure-caught scenario). **CTR footprint:** CTR-023 was already CLOSED; CTR-024 (→ Term 6, `site_id` payload discipline) stays OPEN; CTR-016 (billing aggregation folded in) stays OPEN. No new CTRs; no Charter conflict.

**Foundation living-document amendment (Charter §11)**
- **CN-4-005 (Engine Contract Model)** amended additively (commit `76dd67d`) per **CTR-023** from Term 5. `scope_policy` becomes the engine **default**; `subscribes_to` and `commands` may each declare an optional `scope_ref` override at one of three level names — **`site` / `tenant` / `platform`** — replacing the informal "branch/business" wording. New **§1.1 "Scope Levels and Per-Operation Override"** added (levels table, default+override mechanism, additive guarantee, provenance from CN-5-100/CN-5-101). **`site` is a payload concept** (intra-tenant location partition); CN-4-002 envelope unchanged. Manifests with only `scope_policy` remain fully valid (no breaking change); engine isolation (Law 2) unaffected. Authored by Term 4 (D-006) on a Concept-Lead-authorised re-activation. **CTR-023 → CLOSED.**

## 2026-05-28

**Term 5 — Universal Engines BEGINS**
- **Term 5 CN-5-100 (Subscription Wiring Patterns)** ratified and merged (commit `8f23d9b`) — Term 5's first concept doc, the cross-engine wiring contract every other Term 5 doc builds on. **Five named patterns** (P1 fan-out, P2 projection-only, P3 command-emitting, P4 compensation, P5 choreography-sequence). **Five laws** (events-only; declarative subscriptions in the manifest; choreography-only/no orchestrator; subscribers emit events only through the bus; idempotency is a framework property via processed_position). **Subscription declaration** extends CN-4-005's `subscribes_to` with per-entry `{event_type, version, handler, kind, scope_ref}`. **Replay semantics (Q2):** `kind ∈ {projection, command_emitting, compensation}` — command_emitting/compensation handlers are **live-only, not re-run on replay** (their derivative events are first-class store events replayed as themselves; grounded in CN-4-009 "replay never emits events"). **Failure/freshness (Q3):** per-subscriber stale-but-available + at-least-once redelivery + resume from processed_position + CN-4-017 lag escalation; a failing subscriber lags but never invalidates truth. **No private events (Q4)** — manifest `emits` marks cross-engine contract events (additive-only forever, CN-4-002). **No ordering between fan-out subscribers** (emergent choreography); **causation_id linkage** makes the choreography traceable end-to-end without an orchestrator (audit substitute, CN-4-008). Fan-in = CN-4-005 status-event + local-projection (not a new pattern) → CN-5-104. Mama Amina retail-sale example (engine-contract tone, D-004 #4). No new CTRs; no Charter conflict.

## 2026-05-27

**Merged to `main` — Term 4 Phase 3 (13/13) — FOUNDATION CONCEPT PHASE COMPLETE (23/23 CN docs + 11 brief sections)**
- **Term 4 CN-4-018 (Snapshot Storage)** ratified and merged (commit `08c6e04`) — **the final Foundation doc**. Snapshots are non-truth performance caches (D-007 #10): derived, disposable, point-in-time, **version-tagged**. **Three-layer non-truth defence** (type-level marker + CI DC-025/026 + runtime guard) — and the marker **survives serialisation/storage** (deserialised as a non-truth-marked type, never plain authoritative data; the defence holds at the storage boundary). **Snapshot operations (create/refresh/dispose) are cache operations, NOT events** — they add no truth, keeping the event store clean of cache churn (contrast: registry/journal/audit ARE event-sourced). **Atomic capture** at a single consistent `global_position` (no torn reads). **Invalidation** on hash-chain breach (CN-4-003) at/before position, or when the recorded handler/projection version no longer matches the active set (detectable, not manual). Snapshot vs Projection vs Checkpoint distinction; tenant-scoped (DC-008); resilience-aware (DEGRADED may serve stale snapshots with disclosed freshness). No new CTRs; no Charter conflict.
- **Milestone:** **Foundation's full concept proposal is complete** — brief Sections 1–11 and all 23 CN concept documents (Phase 1 doctrine ×7, Phase 2 decision-driven ×3, Phase 3 parallel ×13), each discussed, Overseer-reviewed, and ratified by the Concept Lead. The BOS Kernel is now fully specified at the concept level: event sourcing, store contract, hash chain, command bus, isolation, identity, audit, replay, projections, primitives, saleable line/tender, advisor framework, kernel boundary & doctrine gate, AI guardrails & journal, time authority, document engine, compliance DSL, security, resilience, snapshots, extension points, and doctrine enforcement (37 checks).

**Merged to `main` — Term 4 Phase 3 (12/13)**
- **Term 4 CN-4-017 (Resilience Modes)** ratified and merged (commit `47aa5e2`). The NORMAL / DEGRADED / READ_ONLY state machine that many docs referenced (bus write-gate, replay availability, rebuild-during-operation, numbering block-reservation, rate-limit tightening, registration gating, advisor freshness). **Auto NORMAL→DEGRADED only** (time-sensitive, by `system:resilience-monitor`); all other transitions **human-gated** (D-007 #9). **Runtime layer, not replay-deterministic:** health checks are ephemeral live measurements; the `kernel.resilience.mode_changed.v1` event is a recorded fact (replay sees the transition, doesn't re-run checks) — consistent with CN-4-016. **Hash-chain breach (CN-4-003) is a health dimension → auto NORMAL→DEGRADED; READ_ONLY stays human-gated even for breach** — resolves the CN-4-003 §9 open item. **Three modes only** — planned maintenance is a human-gated NORMAL→READ_ONLY distinguished by `trigger_type` (planned_maintenance vs emergency), not a fourth mode. Transition event carries **impaired_dimensions** so components can respond proportionately. Per-component behaviour table (10 components × 3 modes). Health checks = config (no hard thresholds, D-004). **In-flight command semantics:** evaluated against the mode at processing time; committed events not retroactively affected. No new CTRs; no Charter conflict.

**Merged to `main` — Term 4 Phase 3 (11/13)**
- **Term 4 CN-4-016 (Security Primitives)** ratified and merged (commit `05bf146`). The Kernel's defensive building blocks beyond isolation (CN-4-006) and identity (CN-4-007): rate limits, anomaly detection, scope guards — covering three threat classes (abuse / attack / leakage). **Runtime layer, not replay-deterministic:** rate-limit counters and anomaly pattern-state are **ephemeral runtime state, not event-sourced truth**; the rejections (`kernel.command.rejected.v1`, `security.rate_limit_exceeded`) and **anomaly alerts** (`kernel.anomaly.detected.v1`, emitted by **`system:anomaly-detector`**) are recorded as facts and visible on replay, but the detectors do not re-run during replay. **Rate limits** are deterministic/blocking, per-principal + per-tenant aggregate ceiling, count attempts (legit retries accommodated), configurable, applied at the command-bus boundary (CN-4-004), audited. **Anomaly detection** is heuristic/advisory (alerts, never auto-blocks — avoids false-positive lockouts), reads projections only. **Scope guards** are the shared non-bypassable middleware pattern behind CN-4-006/005/022 guards (defence-in-depth with CI, D-007 #5). **Auth boundary:** the Kernel defends against what **authenticated principals** do; pre-auth brute-force/credential-stuffing/DDoS belong to the authentication layer (Term 7 mechanism / Term 1 policy). Resilience interaction (tighter limits in DEGRADED) deferred to CN-4-017. No new CTRs; no Charter conflict.

**Merged to `main` — Term 4 Phase 3 (10/13)**
- **Term 4 CN-4-015 (Compliance DSL Framework)** ratified and merged (commit `fc2ea2f`). The language compliance packs are written in (Law 5: compliance configured, not coded). Declarative, **sandboxed**, versioned, neutral. **Sandbox (D-007 #8):** no side effects, no system access, no network, **no ambient time, no randomness** — time/data arrive only as input via the clock protocol (CN-4-014); deterministic, resource-bounded. **Pack version is authoritative for freeze (D-009);** a rule's `effective-from` is scheduling of a known change *within* a deployed pack, **resolved against the transaction's business date** (CN-4-014, not record time) — both pack version and business date are stored, so replay is deterministic. **Tenant↔jurisdiction binding:** set at registration by the regional agent (Law 6, CN-4-006); the pack version active at emission governs and is stamped as `pack_version_ref`. **Extensions are CODE** — they pass the **doctrine gate** (CN-4-023), are **version-frozen** like packs (D-009, replay uses the historical extension version), run under the same sandbox constraints, and are guarded against overuse. **Pack approval gate** (Term 1 Compliance Officer, human, D-009-sensitive) is separate from the doctrine gate. **Buggy-pack correction (N5):** corrective pack version + compensating events; original events stand under the original pack (no history rewrite). Rwanda digital-services-levy example. No new CTRs; no Charter conflict.

**Merged to `main` — Term 4 Phase 3 (9/13)**
- **Term 4 CN-4-012 (Document Engine & Numbering)** ratified and merged (commit `4d3c898`). Hash-verified, immutable, legally-defensible documents (Document primitive terminal fold, CN-4-011). Six-phase lifecycle: template selection → content assembly → **compliance binding** (pack version at issuance, D-009) → numbering → hash → issuance (`document.issued.v1`). **Two-layer integrity:** the document hash (over content + template-version ref + pack-version ref + number) is stored **inside the issuance event**, so it is itself chain-protected (CN-4-003) — the document hash enables standalone/offline verification, the event chain protects it in-store. **Integrity vs authenticity:** the hash proves the content is unchanged (integrity) but not that BOS issued it (authenticity) — offline authenticity may need a **cryptographic signature**; flagged to Term 7 via CTR-017. **Numbering** is tenant-isolated, branch/terminal-scoped, sequential, deterministic, **block-reserved for DEGRADED** (D-007 #12); reconciliation is **compliance-pack-driven** (gap-free jurisdictions continue the sequence; gap-tolerant mark reserved-but-unused with an auditable reason). The hash covers **content + version references, not rendered output** — re-display uses the referenced template version. **Template versioning + freeze** (old documents never re-rendered; old templates retained). **Corrections/cancellations = new documents** referencing the original (D-009). **CTR-017 → NEGOTIATING** (Foundation verification logic delivered; integrity-vs-authenticity/signature question raised for Term 7). No new CTRs; no Charter conflict.

**Merged to `main` — Term 4 Phase 3 (8/13)**
- **Term 4 CN-4-014 (Time Authority)** ratified and merged (commit `d50508d`). Deterministic time for a replayable system. **Three injected Kernel clocks** — SystemClock (runtime, the only place real time enters), FixedClock (tests), ReplayClock (replay, time from event timestamps); no `datetime.now()` in event-producing logic (DC-017/018). **No SimulationClock** — what-if/profit simulation is an advisor-layer concern (CN-4-022, Law 3), outside the event store, never writes events. **Record time vs business time** (envelope timestamp vs payload effective date), both deterministic on replay. **Tz-database version freeze (a D-009 analog):** fiscal zone stored at registration with zone name + **resolved UTC offset + tz-database version**; historical events/properties are interpreted under the historical tz-db version so day boundaries never shift retroactively when the IANA database updates. **Timestamps are descriptive, not authoritative for ordering** — `global_position` is the total order; clock skew/NTP/leap-seconds tolerated (aligns with CN-4-009). Clock-rule scope: event-producing logic must use injected clocks; read-only "as of now" queries may use SystemClock. Fiscal-zone change = new event + freeze. Platform cross-tenant time reference zone deferred to Term 1/Architect. **Correction:** the four preceding Phase-3 changelog headers were off by one (CN-4-004 was the 4th Phase-3 doc, not 5th) — now corrected to 4/5/6/7. No new CTRs; no Charter conflict.

**Merged to `main` — Term 4 Phase 3 (7/13)**
- **Term 4 CN-4-009 (Replay Engine)** ratified and merged (commit `0d80051`). The mechanics of reconstructing state from events (proof of CN-4-001 Assertion 6). Four replay types: full / scoped (tenant, aggregate) / point-in-time / projection rebuild. **Determinism** via five requirements — same events, same `global_position` order, same **rules-data** (D-009 pack-version freeze), same **rules-code** (fold/handler code stays stable through additive versioning; changes become new event-type versions with **version-aware handlers**, CN-4-010 — determinism through version-aware handlers, *not* "code freeze"), same time (CN-4-014). **Defensible/audit replay includes hash-chain verification** (CN-4-003) for the scope; output states whether the chain was intact (routine rebuilds skip verification for speed). **Point-in-time cutoff is `global_position`** (a timestamp request resolves once to the position of the last event at/before it). **Checkpoints** are non-truth (CN-4-018), verified at creation, and **invalidated by a hash breach** at/before their position. **No-skip / compensating-events-only** correction (O9). **Replay never emits events** — pure reconstruction; persisting a rebuilt projection is a derived write, not event emission (may be deferred in READ_ONLY). No new CTRs; no Charter conflict.

**Merged to `main` — Term 4 Phase 3 (6/13)**
- **Term 4 CN-4-010 (Projection Framework)** ratified and merged (commit `1d86db7`). Formalises read-models that several merged docs already rely on (audit log CN-4-008, Decision Journal CN-4-013, engine registry CN-4-020). Projections are derived, rebuildable, deterministic, disposable, queryable, **position-aware**. **Position tracking & freshness:** each projection exposes `processed_position` / `processed_timestamp` — the **source of the Advisor Framework's `freshness_indicator`** (CN-4-022 N4). The processed_position is a **high-water mark** for idempotency — the framework skips re-deliveries (`global_position ≤ high-water mark`), so handlers don't reinvent idempotency (CN-4-005). **Deterministic `global_position` processing order** (rebuild reproducibility). **Unhandled event version is never silently skipped** — it is upcast (CN-4-002 §3) or fails as a doctrine violation (an incomplete projection violates "derived from events", CN-4-001). **Rebuild without downtime:** stale-but-available → historical replay → **catch-up to live tail** → atomic switchover (no gap, Section 8 #4). Tenant-scoped enforcement by the framework, not handlers. No new CTRs; no Charter conflict.

**Merged to `main` — Term 4 Phase 3 (5/13)**
- **Term 4 CN-4-008 (Audit Log Primitive)** ratified and merged (commits `5528290` + `9e80895`). "The append-only journal beneath every other audit log." General 9-field audit schema (audit_entry_id, event_ref, action_type, actor, tenant_scope, target, outcome, timestamp, correlation_id); specialised logs (Decision Journal, CN-4-013) extend it. **The audit log is a projection** (CN-4-010) over events — replayable, deterministic, integrity inherited from the hash chain, never out of sync (no second source of truth). **Two parallel trails** (tenant + platform), dual-actor actions appear in both (CN-4-007 §3). **Audit-completeness invariant** — every state change → bus → event → audit entry; structurally guaranteed; a formal DC will be added to CN-4-019 via the living catalog. Seven query dimensions + pagination. Overseer-review fixes: **(A)** cross-scope / platform-scope **reads ARE audited** (Section 8 #10, DC-011/020) — the "reads not audited" exemption is only in-scope owner reads; **(B)** audit entry is **per-event** (multi-event commands → multiple entries sharing one `correlation_id`); **(C)** retention operates on the projection/archival layer — **source events are never deleted** (CN-4-001); right-to-erasure is the separate crypto-shredding future decision (CN-4-006 §9). No new CTRs; no Charter conflict.

**Merged to `main` — Term 4 Phase 3 (4/13)**
- **Term 4 CN-4-004 (Command Bus & Policy Engine)** ratified and merged (commit `d23635c`). The **sole entry point for state changes** — no state change happens outside the bus. Flow: submit → envelope validation → policy evaluation → accept (emit event[s]) or reject (`kernel.command.rejected.v1`). **Resilience-mode gate** (the physical site of CN-4-017 write-mode enforcement: NORMAL full / DEGRADED gated / **READ_ONLY rejects all writes**). Four policy categories — **kernel → doctrine → engine → compliance**, kernel/doctrine first with **fail-fast**; kernel/doctrine never overridable. Policies are **pure evaluators** (side-effect-free, deterministic — replay/audit safe); compliance policies run in the **sandboxed evaluator** (D-007 #8). **Freeze doctrine:** compliance policies use the pack version active at command-processing; emitted events stamp `pack_version_ref` (D-009). **Atomic multi-event emission** (all events from one command, or none). **`correlation_id` lineage** on emitted + rejection events (full request→outcome trace). **System principals are subject to all policies — not a backdoor** (CN-4-007 §4). Rejections are auditable events with **cross-tenant privacy** (generic "scope mismatch" — no tenant-existence leakage). Idempotency via correlation_id + write-once. No new CTRs; no Charter conflict.

**Merged to `main` — Term 4 Phase 3 (2/13 and 3/13)**
- **Term 4 CN-4-003 (Hash Chain & Tamper Detection)** ratified and merged (commit `e7f63f5`). The event store's integrity proof. **Hash formula:** `event_hash(N) = hash(envelope excl. event_hash + payload + previous_hash)` — deterministic, no circular dependency; `previous_hash` counted once (DC-031 must independently recompute). **Genesis anchor:** a well-known, permanent, per-tenant constant. **Chain topology — decided:** **per-tenant chains anchored to `global_position`** (option b), the serialisation trade-off addressed explicitly — parallel writes per tenant (no cross-tenant hash dependency), cross-tenant ordering via global_position not hash linkage, aligns with CN-4-006 isolation. **Tampered vs unverifiable:** a break at N = suspected-tampered; events after N = unverifiable (not necessarily altered) until investigated. **Five-step breach protocol** (alert / quarantine / scope assessment / Term 1 escalation / **no silent recovery** — evidence preserved, no deletion, no auto-heal); breach may trigger DEGRADED via CN-4-017 rules, never a unilateral halt. TRA/Mzee Hassan example. No new CTRs; no Charter conflict.
- **Term 4 CN-4-019 (Doctrine Enforcement Checks)** ratified and merged (commits `4552c63` + `5781017`). The invariant test catalog — **37 doctrine checks (DC-001…DC-037)** covering **all 15 Section 8 criteria** plus structural invariants, organised by category, each mapped to Section 8 # / check type / source CN doc. New checks beyond the original draft: **DC-033** (Decision Journal completeness — closes the Section 8 #6 gap), **DC-034** (additive-only schema versioning), **DC-035** (stream-ordering / optimistic-concurrency integrity), **DC-036** (journal events emitted by a system principal only — protects the CN-4-013 "advisor never writes" resolution), **DC-037** (single-settlement single-currency). Three check types — **Static 9 / Integration 19 / Schema-manifest 9** (= 37; §3 corrected to match §2). Law 1–5 each mapped to checks; **Law 6 has no Kernel-testable check** (regional distribution is Term 1/2 policy — correct and deliberate). Living catalog: add = normal gate, modify/remove = Concept-Lead-approved doctrine-affecting change. Audit-completeness check deferred to the living catalog pending CN-4-008. No new CTRs.

## 2026-05-26

**Merged to `main` — Term 4 Phase 3 begins (1/13 parallel docs)**
- **Term 4 CN-4-013 (AI Guardrails & Decision Journal)** ratified and merged (commit `46a9536`). **Guardrails:** six forbidden operations + **three-deep enforcement** (Kernel-structural: advisor principal has no write path; runtime scope guard; CI doctrine checks — defence-in-depth, not redundancy). **Decision Journal** = a **specialised audit log** extending CN-4-008 (append-only, immutable, queryable, tamper-evident), realised as a CN-4-010 **projection** over an **event-sourced** foundation. **Paradox resolved:** journal entries are events (`kernel.advisor.suggestion.recorded.v1` / `…decision.recorded.v1`) emitted by the **Advisor Framework as a system principal** (`system:advisor-framework`, CN-4-007) — **the advisor never writes**; the Kernel records on its behalf. Full 14-field schema: `data_ref` is **point-in-time / replay-stable** (projection at a global_position/snapshot id, never a live query); `human_decision` lifecycle pending→accepted/rejected (appended decision event), **"ignored" derived** (computed absence, no phantom actor); `draft_command_ref` immutable (snapshot opt-in); `freshness_indicator` retained for legal defensibility; **no full chain-of-thought** (D-007 #6). Previously-suggested check = read against the projection. **CTR-009 → CLOSED** (explainability schema delivered and accepted). No new CTRs; no Charter conflict.

**Merged to `main` — Term 4 Phase 2 COMPLETE (3/3 decision-driven docs)**
- **Term 4 CN-4-023 (Kernel Boundary & Doctrine Gate)** ratified and merged (commit `3db3af7`) — **the final Phase 2 doc**. **Part A (D-002B):** Developer-AI is out-of-kernel — never runs in production, never writes events/state, produces code proposals only; **no special treatment** (same gate/review as human PRs); **synthetic/anonymised test data** (tenant isolation at the build layer, CN-4-006); not an advisor (CN-4-022 is runtime). **Part B — the doctrine gate:** runs on every PR/CI/engine-registration/Developer-AI proposal; **no override**, **fail-closed** (gate cannot run = blocked, never pass); **doctrine-check updates** require a heightened process (Concept Lead/governance approval, recorded as a doctrine-affecting change, audited — the wall cannot erode silently); **two distinct gates** (code doctrine gate here vs compliance-pack approval gate, CN-4-015 + Term 1, D-009-sensitive). **Build-provenance attestation** per release (commit hash, checks run + results, approver, timestamp, gate version) — immutable, tamper-evident (may mirror CN-4-008), making the Kernel's own lineage as defensible as its business data. CN-4-019 = the tests; CN-4-023 = the wall. **CTR-008** Foundation side delivered (boundary + gate; awaiting Term 1 governance + attestation retention). No new CTRs; no Charter conflict. **Phase 2 complete: CN-4-021/022/023.** Next: Phase 3 (13 parallel docs).

**Merged to `main` — Term 4 Phase 2 (2/3)**
- **Term 4 CN-4-022 (Advisor Framework & Role-Scope Binding)** ratified and merged (commit `1198e3e`). The single runtime Advisor Framework (D-002A, D-005). Contract = **audience** (three Charter §3 categories — Platform/Agent/Tenant, structural not roles) + **scope** (tenant / aggregate-opt-in / platform) + **model** (id/version/config_ref, swappable). **Runtime scope enforcement by the Kernel (D-007 #6)** covering **all advisor input including prompt assembly** (no out-of-band scope leakage; distinct from Term 7 prompt-injection defence). Consent-dependent scopes **re-evaluated every invocation** (revoke-wins, CN-4-011). Advisory-only (Law 3): reads projections only, replay-safe, **may produce inert draft commands** (no effect until a human submits via the command bus). **Dual-actor audit** — advisor principal + invoking human recorded together (CN-4-007). Journal fields **contributed to CN-4-013** (schema owner): incl. invoking_principal, prompt_ref, model_config_ref; **no full chain-of-thought** (CTR-009). Role→scope→advisor binding (Foundation = mechanism). Resolved N1 (cold-start data-sufficiency), N3 (model-swap, no re-consent), N4 (DEGRADED freshness / READ_ONLY block), q10 (benchmarking opt-in, anonymised aggregate). **CTR-007** resolution delivered (pending Terms 5/2/3); **CTR-014** Foundation side delivered (model plug-in point; awaiting Term 1 governance). No new CTRs; no Charter conflict.

**Merged to `main` — Term 4 Phase 2 begins (1/3)**
- **Term 4 CN-4-021 (Saleable Line & Tender Value Shapes)** ratified and merged (commit `5aad12d`) — first Phase 2 (CTR-driven) doc. The two value shapes (D-001, D-007 #1) connecting verticals to Universal Checkout. **Saleable Line:** authoritative inputs (quantity, unit_price pre-tax, discount_refs, tax_treatment_ref) vs **non-authoritative** derived `line_total` cache (Checkout must recompute, inputs win); **tax = input ref only**, computed at settlement by Checkout under the pack version active at emission (D-009, CN-4-015, Law 5 — verticals never compute tax); `source_tag` opaque + **non-branching guarantee** (doctrine-check enforceable, Law 2). **Tender:** {tender_id, method, amount, external_ref} — **no status field** (lifecycle-free; outcomes event-sourced by Checkout/Term 7); `method` = **registered category tag** (controlled vocabulary, extensible without Kernel change); **credit tender → Obligation primitive** (CN-4-011). **Currency invariant** (one settlement, one currency; cross-border = exception). Value-shapes≠primitives table; vertical→checkout flow; Karakana example with VAT computed at settlement. **CTR-001 & CTR-003** resolutions delivered (pending Terms 6/5 acceptance). No new CTRs; no Charter conflict.

**Merged to `main` — Term 4 Phase 1 COMPLETE (7/7 doctrine-foundation docs)**
- **Term 4 CN-4-020 (Extension Points)** ratified and merged (commit `0069608`) — **the final Phase 1 doc**. Two-phase onboarding: a **build-time doctrine gate** (CI per CN-4-019/CN-4-023; human review for Developer-AI pipeline, D-002B) followed by **runtime registration** (manifest submission → compatibility checks → **atomic** subscription wiring → activation, all-or-nothing rollback). Six compatibility checks; **event-type ownership transferable on sunset** (not permanently locked). **Registry is event-sourced** (`kernel.engine.registered/updated/deactivated.v1`; a CN-4-010 projection — replayable, auditable, deterministic; replaces a mutable registry store). **Manifest-update / re-registration** flow (version bump → re-check → additive wiring). Deregistration (4 steps, no event deletion; tenant notification = Term 1/3 hand-off). Resilience-mode gating (NORMAL allowed / DEGRADED human-gated / READ_ONLY blocked, D-007 #9). **Capability = advertise-only** (dependencies stay event-type-based; D-007 #11 without re-coupling). Registration is **platform-scoped + audited** (CN-4-006/007/008). **CTR-022 filed** (→ Term 1, activation & tenant-availability governance); **CTR-018 → NEGOTIATING** (contract delivered, pending Term 6 acceptance). **Phase 1 doctrine foundation is complete: CN-4-001/002/006/007/005/011/020.** Next: Phase 2 (CTR-driven) — CN-4-021, CN-4-022, CN-4-023.
- **Term 4 CN-4-011 (Primitive Catalog)** ratified and merged (commit `1054cb7`). The nine primitives — Ledger (no close-period, D-007 #13), Item/Service, **Party** (renamed from "actor" to the Charter §4.1 name; business entity, distinct from CN-4-007's system actor), Document (**terminal fold** — `issued` once; corrections = new linked documents per D-009), Inventory Movement, Obligation, Approval, Workflow, Consent (purpose+scope keying, revoke-wins, D-008) — each with shape, legal operations, and compensating-fold logic. Distinctiveness (Consent≠Approval by legal weight; Workflow≠Approval by complexity, D-007 #2); neutrality (D-004); engine-namespaced events (not primitive-namespaced); the Purchase Order composition example (§8). Four open items to Architect/Terms 5/6. **MASTER-GLOSSARY** gains **Party** and **Actor** entries (distinction recorded). No new CTRs; no Charter conflict.
- **Term 4 CN-4-005 (Engine Contract Model)** ratified and merged (commit `2d616aa`). The 10-field engine manifest (declarative; `requires` ⊆ `subscribes_to`; references event types, never engine names; deprecation marker within `emits`); Law 2 isolation enforced at both layers — runtime guards + CI doctrine checks (D-007 #5); subscription framework (emitter-unaware, event-type-based, new subscriber = manifest update only; at-least-once + ordered-within-stream + idempotent-expected, exactly-once = Architect); request-reply possible-but-discouraged (temporal coupling); the status-event + local-projection pattern (resolves O7/O8); pre-write malformed-event validation with hash-chain backstop and structural-vs-business-error distinction (O9/O10). Five open items assigned; CN-4-005/CN-4-020 boundary made explicit. No new CTRs; no Charter conflict.

## 2026-05-24

**Decision recorded**
- **D-009 — Temporal Interpretation / "Freeze" Doctrine.** Events and issued documents are interpreted under the rules/rates/pack-version active at emission/issuance; never retroactively reinterpreted. Implemented in CN-4-001/012/015; affects Terms 1/5/7. Arose from Term 4 Section 7 (edge cases O7, O15).
- **D-008 — Conversational / Messaging Channels.** A cross-term capability (not a new Term) for acquiring and serving customers over SMS, WhatsApp, Telegram, and email. Term 7 owns the channel adapters/gateway; Term 3 the tenant↔customer UX; Term 2 the agent use; Term 5 outreach/promotions; Term 4's consent primitive gates marketing. Guardrails: consent-first + opt-out, AI advisory/human-gated, channels via the Term 7 gateway. Boundary object BO-7; seeds CTR-019/020/021.

**Merged to `main`**
- **Term 4 CN-4-007 (Identity Primitive)** ratified and merged (commit `afccbba`). Four principal types (human, advisor, system, machine); authority chain (Kernel carries structure, not a permission engine); dual-actor record (resolves CN-4-006 R4); system-as-actor via the command bus (resolves Section 11 q4); advisor identity distinct from CN-4-022. Delegation flagged as a future decision (not a fifth type).
- **Term 4 CN-4-006 (Multi-Tenant Isolation)** ratified and merged (commit `0421bb4`). Seven-layer isolation + tenant scope object (non-bypassable); platform scope as a read-only parallel scope (dual-actor audit; policy = Term 1 via CTR-016); GDPR export scoping (cost = Architect); 7 leak-detection tests incl. snapshot isolation; data isolation = concept, resource isolation = Architect. Flags **right-to-erasure vs append-only** as a future decision (crypto-shredding direction).
- **Term 4 CN-4-002 (Event Store Contract)** ratified and merged (commit `50c2ce6`). The 15-field event envelope (engine-agnostic; pack_version_ref nullable for system events; record-time timestamp with business dates in payload; dedicated compensates_event_id; registered metadata), ordering (per-stream sequence + global_position + optimistic concurrency, resolving O1), additive-only schema versioning, and the append-only guarantee.
- **Term 4 CN-4-001 (Event Sourcing Doctrine)** ratified and merged (commit `587ea6c`) — the first CN concept document. Six doctrine assertions, the Three Whys with alternatives rejected, implications (compensating fold D-007 #3, freeze D-009, determinism), the Mzee Hassan/TRA example, and boundaries. Glossary terms **Compensating Event, Fold, Replay** added to `MASTER-GLOSSARY.md`.

**Decision recorded**
- **Term 4 Sections 12–14 intentionally omitted** (Concept Lead approved): §12 (Notes on Existing Repository) is reference-only; §13 (First Tasks) is superseded by Section 6's writing order; §14 (Restraint) is folded into Section 1 (Mission) and Section 4 (Scope). Term 4's brief-level proposal is therefore Sections 1–11. Next: the 23 CN concept documents, in the Section 6 order (Phase 1 → 2 → 3).

**Merged to `main`**
- **Term 4 Section 11 (Open Questions)** ratified and merged (commit `9090ed5`) — the **final brief-level section**. 7 resolved by decisions, 8 open assigned to CN docs (3 from Brief §11 + 5 new), 0 needs-ruling, 0 new CTRs. **Term 4's brief-level proposal (Sections 1–11) is complete.**
- **Term 4 Section 10 (Relationships with Other Terms)** ratified and merged (commit `0728389`). Depends-on (Term 7 only), depended-on-by (consistent with Section 5), the live cross-term picture (11 CTRs: 7 inbound incl. CTR-021 + 4 outbound; BO-1/2/3/5/6/7), and hand-off statements.
- **Term 4 Section 9 (Architect's Role)** ratified and merged (commit `6936b61`). Concept→Architect mapping for all 23 CN docs, 12 directives (6 from Brief refined + 6 from decisions), a "Too Detailed for Concept" boundary, and the Phase 1→2→3 sequence note. Architects framed as a future audience.
- **Term 4 Section 8 (Success Criteria)** ratified and merged (commit `87e35fe`). 15 machine-provable criteria (9 refined from Brief §8 + 6 from decisions), each with a "How Verified" column leaning on CN-4-019 invariant tests; a "Belongs Elsewhere" table excludes UX/pack-content/performance/cross-term items.
- **Term 4 Section 7 (Edge Cases)** ratified and merged (commit `8cc1f50`). 34 edge cases sorted: 19 resolved by decisions (D-007/D-002A/D-008/D-009/Charter/Section 3), 15 assigned to CN docs; no needs-ruling, no new CTRs.
- **Term 4 Section 6 (Key Concepts)** ratified and merged (commit `42b9d06`). The 23-doc CN catalogue grouped by clusters A–E with Decisions + CTR columns, overlap note, writing order (Phase 1 doctrine → Phase 2 CTR-driven → Phase 3 parallel), and cross-term summary. Supersedes Brief §13's First Tasks.
- **Term 4 Section 5 (Audiences Served)** ratified and merged (commit `1d2bbf8`). Audience table (Terms 1,2,3,5,6,7 + Architects as a future audience), promise referenced from Section 1, and an extensibility note (new consumers via CN-4-020; new materials via CTR + restraint test). No new CTRs.
- **Term 4 Section 4 (Scope · Out)** ratified and merged (commit `970709b`). Consolidated OUT list (26 items, 7 categories) with owners, the restraint statement, and three borderline rulings (Auth, prompt-injection, partitioning) — all OUT. No new CTRs.
- **Term 4 Section 3 (Scope · In)** ratified by the Concept Lead and merged (commit `3b8129d`). Encodes all 13 D-007 resolutions across five clusters; cross-term items (CTR-001/003/007/016/017/018) marked pending acceptance by Terms 1/5/6/7.

**Decision recorded**
- **D-007 — Foundation Scope Resolutions (Section 3).** 13 ratified resolutions for what the Kernel provides, incl. Saleable Line/Tender as value shapes (Checkout = Term 5 engine), the nine primitives (Consent ≠ Approval), compensating-event fold semantics, a distinct "advisor" principal + first-class platform scope, runtime advisor-scope enforcement, Foundation verification logic vs Term 7 surface, DSL grammar + sandboxed evaluator, resilience auto/human transitions, and **removal of "close period" from the Ledger primitive** (neutrality). Seeds CTR-016/017/018; resolves CTR-009; partially resolves CTR-007; proposes CTR-001/003.

**Merged to `main`**
- **Term 4 Sections 1 (Mission) & 2 (Mission Question)** ratified by the Concept Lead and merged to `main` (commit `65cb2a0`). Overseer review confirmed D-004/D-006 compliance; Term 4 applied two fixes (Parent links; "cracks" → "holds" consistency). First Term proposal sections to land on `main`.

**Ratified**
- **Term 4 Section 2 (Mission Question)** wording ratified by the Concept Lead — version E with "holds": *"How do we build a kernel that holds under decades of pressure — new verticals, new countries, AI that must remain advisory — and still proves to a court that nothing was ever silently changed?"* Charter §5.2 summary updated to match (de-numbered). Term 4 authors the file on its branch (D-006).

**Decisions recorded**
- **D-005 — AI Mode (Universal Per-Role Dashboard Advisor).** Every dashboard, for every role (tenant, platform steward, regional agent), gets an "AI Mode" built on the one Advisor Framework (D-002A) — prompt + proactive role-scoped advice, advisory-only, journaled, explainable. Enabled for all roles from the start. Seeds CTR-014, CTR-015.
- **D-006 — Collaboration Operating Model.** `main` is the single shared source of truth; each Term authors only its own files on a branch from `main`; the Overseer publishes shared docs, reviews, merges, answers Terms' questions, and keeps every Term current — and does NOT author Term content.

**Removed**
- The subagent-authored `terms/term-4/proposals/SECTION-1-MISSION.md` and `SECTION-2-MISSION-QUESTION.md` were discarded; Term 4 authors its own work on its branch (D-006).

**Shared docs published to `main`** so every Term session can read them.

## 2026-05-23 (Term 4 §1 review)

**Ratified**
- **Term 4 Section 1 (Mission)** — ratified by the Concept Lead. First proposal section to pass. Recorded in `terms/term-4/proposals/SECTION-1-MISSION.md`.

**Decision recorded**
- **D-004 — Concept-Writing Principles.** From the Term 4 Mission review: a Mission carries only the unchanging; no unverifiable hard numbers (supersedes the "10 years" vs "20 years" conflict in Term 4 Brief §1/§2); "invisible" is scoped to runtime users; audience/tone follow each Brief §5; Foundation Mission must elevate legal defensibility, neutrality-by-restraint, and the six Laws as technical invariants. D1–D4 rulings recorded.

**Glossary created**
- `MASTER-GLOSSARY.md` (Term 7 / CN-7-003), seeded with the Foundation-vs-Kernel ruling and core cross-Term terms.

## 2026-05-23 (later)

**Collaboration protocol clarified**
- `CROSS-TERM-COLLABORATION.md` — §5 rewritten to a discuss-then-write workflow (Terms bring points section by section to the Concept Lead; the proposal is written only after agreement). Added §6 Authority & Approval (Concept Lead is final; Overseer/Term 7 coordinates and must also agree; no complete system proposal until all seven Terms pass) and §7 the AI-agent operating model (one Term = one agent, one owner per file, CTR as the only message bus, per-Term folders, git branches).
- Terms 1–7 addenda updated to state the discuss-and-agree-first rule and the Concept Lead's final authority.

## 2026-05-23

**Added — concept-environment scaffolding**
- `DECISION-LOG.md` — binding cross-Term decisions (Term 7 / CN-7-007).
- `CROSS-TERM-COLLABORATION.md` — interaction map, boundary objects, and the Cross-Term Request (CTR) process.
- `CROSS-TERM-REQUEST-REGISTER.md` — living register of CTRs, seeded from D-001..D-003.
- This `CHANGELOG.md` (promised in Charter §11).

**Decisions recorded**
- **D-001 — Universal Checkout.** POS becomes a two-layer design: a universal Checkout/Tender layer over a shared Saleable Line abstraction, with each vertical feeding the lines. Lets any business type use one checkout.
- **D-002 — AI.** (A) One runtime Advisor Framework (advisory-only, scoped, model-swappable, Decision-Journal-logged) for tenants, agents, and platform. (B) A build-time Developer-AI environment that lives outside the kernel, briefs Claude Code (the implementer) to make GitHub changes, and releases only after human review + tests/doctrine invariants pass.
- **D-003 — Distribution.** Multiple regional agents per region (Law 6 intact) plus a referral program as an acquisition layer, not a replacement for compliance-accountable agency.

**Charter amendments**
- §2.4 added — pointer to standing decisions (Decision Log).
- §6.3 rule 6 added — Cross-Term Request requirement before the Architect phase.
- §11 updated — Decision Log + CHANGELOG now exist and are referenced.

**Term Briefs**
- Terms 1–7 each gained a "Standing Decisions & Cross-Term Work" addendum mapping D-001..D-003 to new concept docs and CTRs.
